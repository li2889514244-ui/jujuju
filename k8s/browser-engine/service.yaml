apiVersion: v1
kind: Service
metadata:
  name: browser-engine
  namespace: matrixflow
  labels:
    app.kubernetes.io/name: browser-engine
    app.kubernetes.io/part-of: matrixflow-erp
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3001
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: browser-engine
