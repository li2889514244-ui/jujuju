apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: matrixflow
  labels:
    app.kubernetes.io/name: frontend
    app.kubernetes.io/part-of: matrixflow-erp
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: frontend
