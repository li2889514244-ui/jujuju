- 2026-09-01 12:07 Asia/Shanghai: 业绩天梯历史数据修正（经运营确认）。(1) 用 `scripts/fix-ladder-snapshot.py` 补齐历史月快照：2026-07 → 卢慧 1260 + 王晶 1600（此前为空）；2026-08 → 补王晶 1600（此前只有卢慧）。修正后 7/8 月王晶/卢慧订单恢复归因（王晶 8 月 75 单、7 月 120 单；卢慧 7 月 300+ 单不再落入未匹配）。(2) 通过正式 API `POST /performance-ladder/teachers` 重新添加王晶老师（aliases 王晶/晶哥/晶哥来了/电影文化，月目标 1600，id `pltea_1788235639450_51hr7bsg`），9 月及以后的王晶订单恢复正常归因；添加操作自动把王晶写入 2026-09 当月快照。
- 2026-09-01 11:55 Asia/Shanghai: 业绩天梯月份切换上线后自查修复轮（已部署，详见 `docs/deployment-log.md`）。修复 3 个真实问题：① 快速连续切月时全局刷新动画会永久卡住——loadingStore 是引用计数，被防串月守卫丢弃的旧请求不配对 stop 导致计数泄漏，现改为每次 start 必配对 stop（旧请求不再触碰 loading 显示态）；② 两个标签页同时首次打开同一历史月时后端 500——`getMonthSnapshot` 首次创建快照未捕获 P2002 唯一约束竞态，现冲突后重读已创建快照返回，`captureCurrentMonthSnapshot` upsert 竞态回退 update；③ 历史月跨月退款遗漏——售后查询原按「月初~月末」过滤，8 月订单 9 月退款的记录查不到、历史月有效订单高估（抖店实测 8 月 3 笔、7 月 10 笔），现只设 begin（月首至今）。新增后端竞态单测 3 项（套件 12/12）。同时核查生产数据发现历史快照缺口：8 月快照缺王晶（该老师 8/24 被删，8 月实际 75 单：抖 51 + 微 24）、7 月快照为空（卢慧配置 8/21 才创建，被「排除晚于月末创建老师」保护排除，7 月实际卢慧 300+ 单）——**需运营确认历史目标值后人工修正**，新增运维工具 `scripts/fix-ladder-snapshot.py`（list/set/remove/reset，走服务器 Prisma Client）。其余已知小问题（左箭头无限回退、跨午夜 todayMonth 不刷新、非+8 时区边界、快照无页面内修正 UI）已评估暂不修。
- 2026-09-02 Asia/Shanghai: 系统健康中心 Phase 1 收尾（本地完成，待部署）。在已有 Phase 1 骨架（SystemEvent/SystemIncident、requestId、前端错误采集、后端拦截器采集、总览页+故障中心）基础上修复三个复查发现的问题：(1) 故障严重度只升不降（CRITICAL 不会被后续 ERROR 事件降级）；(2) 总览 P0/P1/未解决计数改为真实 count（不再从 Top8 截断列表统计）；(3) 总体状态基于“当前未恢复故障”而非 24h 粘性计数。另新增每分钟故障恢复巡检 sweepIncidentRecovery（OPEN 静默10分钟→RECOVERING，恢复窗口内无复发→RESOLVED；COMPANION 来源排除，避免与伴侣恢复逻辑冲突；阈值 INCIDENT_RECOVER_AFTER_MINUTES/INCIDENT_RESOLVE_AFTER_MINUTES 可配）；慢接口严重度对齐（≥1s WARNING、≥3s HTTP_SLOW）；前端事件时间戳钳制（早于7天/晚于5分钟回落服务器时间）；前端版本注入 __APP_VERSION__（npm 包版本或显式 VITE_APP_VERSION），错误事件携带真实前端版本。无数据库迁移（纯应用层）。验证：后端全量单测 29 套 279 项全绿（新增 system-health.service.spec 17 项）；前端 vitest 71/71、vue-tsc 通过；nest build 通过。变更文件：backend/src/modules/system-health/system-health.service.ts、backend/test/mocks/prisma.mock.ts、backend/test/unit/system-health.service.spec.ts、frontend/vite.config.ts、frontend/src/env.d.ts、frontend/src/utils/frontend-monitor.ts、docs/system-health-center-architecture-report.md。
- 2026-09-01 Asia/Shanghai: 业绩天梯月份切换能力（本地完成，未部署生产）。页面顶部新增轻量月份切换器（‹ 2026年09月 › + 弹窗选月/切年/本月/上月快捷入口），URL 保存 `?month=YYYY-MM`（无参数默认当前自然月，刷新/复制链接保持所选月份），未来月份禁用；切换时保留旧数据 + Loading 覆盖、失败显示「YYYY年MM月数据加载失败」而不显示 0，并用最新请求守卫（requestSequence）防止快速切月串数据。当前月展示冲刺口径（已完成/还差/剩余日均/剩余天数），历史月展示月度结果（最终完成/目标/完成率/是否达标），不再按“今天”计算历史月剩余天数。老师/平台等筛选在切月时保留。历史目标/归因规则原数据模型只存当前值（无 effectiveMonth），本次新增 `PerformanceLadderMonthSnapshot` 月度快照表 + `GET /performance-ladder/month-snapshot/:month`：当前月返回实时配置；历史月返回冻结快照（首次查看时按当时配置生成，并排除该月结束后才创建的老师）；更新目标/规则时自动 upsert 当月快照。后端未部署前前端自动兜底为当前配置并明确提示「目标快照不可用」。注意：本功能上线前的历史月份没有真实快照，首次查看会以查看当时的配置生成快照（页面标注快照生成时间）；历史订单数据完整性取决于各店铺订单的本地采集覆盖范围，已删除店铺的历史订单无法回溯。验证：后端 nest build + Jest 35 套 343 项全绿（新增 9 项快照单测）；前端 vue-tsc、vitest 71/71（新增月份工具 16 项 + 守卫 3 项 + 切换器组件 10 项）、vite build 通过；迁移 `202609010001_performance_ladder_month_snapshot` 已写好但未应用。变更文件：`backend/prisma/schema.prisma` + 迁移、`performance-ladder.service.ts/.controller.ts`、`backend/test/unit/performance-ladder-month-snapshot.spec.ts`、`frontend/src/views/monetization/PerformanceLadderView.vue`、`frontend/src/components/common/MonthSwitcher.vue`、`frontend/src/utils/monthRange.ts`、`frontend/src/utils/requestGuard.ts`、`frontend/src/api/performance-ladder.ts`、`frontend/vite.config.ts`（vitest css 配置）、3 个前端测试文件。（后端 + 迁移 + 伴侣 3.2.103，详见 `docs/deployment-log.md` 15:55 条目；前端未动）。此前 16:30 的本地实现条目描述的所有功能已按运营确认发布：迁移 `202608260002_add_monitor_phase2` 已应用、后端部署成功、伴侣 3.2.103 三件套发布（sha256 见部署日志）、本机端到端验证崩溃检测与故障自动恢复闭环全部通过。
- 2026-08-26 16:30 Asia/Shanghai: 伴侣监控中心 Phase 2 闭环改造（本地实现 + 自动化测试，已随 15:55 发布上线）。(1) 启动诊断与 UI 模式：伴侣心跳新增 `uiMode` + `startupDiagnostic`（fallbackReason/fallbackAt/webview2RuntimeVersion）——`webview_window.py` 在每次浏览器降级打开时把原因写入共享状态（pywebview-unavailable / edgechromium-deps-missing / native-render-failed / native-start-failed），原生窗口启动成功标记 webview，并读注册表获取系统 WebView2 Runtime 版本；管理员从此能直接看出「为什么某台电脑以浏览器形式打开」。数据库 CompanionDevice 新增 uiMode/startupDiagnostic 字段。(2) Event + Incident 故障闭环：新增 CompanionEvent（原始事件，BOOT_STARTED/CRASH_SUSPECTED/TASK_STUCK/各告警类型，只在新发作段起点记一条）与 CompanionIncident（open → recovering → resolved：首次发生建单，持续发生 occurrences 递增，不再发生时先进 recovering、连续 2 分钟无复发自动 resolved 并记录 durationSeconds/发生次数，同时自动关闭同类型 Phase-1 未处理告警；recovering 中复发回到 open 并计数）。新增 GET /companion-monitor/incidents 与 /events 查询接口（不新增前端页面）。Phase-1 告警行保留（向后兼容）。(3) bootId + seq + 崩溃检测：伴侣每次进程启动生成一次 bootId，心跳 seq 进程内单调递增，正常退出时 atexit 发送 exitState=clean 信标。后端防旧心跳覆盖：同 bootId 且 seq 未递增 → 丢弃；不同 bootId 且 startedAt 更早 → 丢弃；新 boot 正常接收并 bootCount+1、记 BOOT_STARTED 事件；上次运行无 clean 信标且最后心跳距新启动 ≤10 分钟 → CRASH_SUSPECTED 故障。心跳历史表增加 bootId/seq 列。(4) lastProgressAt + STUCK 检测：伴侣每次心跳比较任务详情快照，快照变化即更新 lastProgressAt（无需在各任务代码里埋点）；后端 TASK_TIMEOUT（纯运行时长报警）替换为 TASK_STUCK——任务运行期间持续有进度不报警，只有「无进展时长」超过阈值（采集15/同步10/上传10/更新30 分钟）才告警；旧版本伴侣无 lastProgressAt 时退化为按 taskStartedAt 判定。(5) record_sync 失败路径复查（确认真实缺陷并修复）：原实现只有抖店同步成功路径调用 record_sync，失败路径全部直接抛异常 → lastSync.success 永远不可能为 false → 「连续3次同步失败」告警无法真实触发。修复：`_run_doudian_store_sync` 改为失败打点包装器（任何异常记录 success=false + 错误码后原样上抛，覆盖店铺不存在/未绑定/采集失败/上传失败/重绑失败等所有出口）；采集器 post-stats 上传也补了 record_sync（sent/failed>0 才记录，保持 null 语义），采集上传失败同样计入连续同步失败计数。测试与验证：后端 tsc 通过；companion-monitor 单测 30/30（新增 13 项 Phase 2：防旧心跳覆盖×2、新 boot 递增、崩溃检测×2、退出信标、故障聚合、恢复闭环×3、STUCK 判定×2、incidents/events 查询、清理×4）；全后端单测 27 套 253 项绿；伴侣全源码 py_compile 0 失败；新增 tests/test_heartbeat.py 10/10；编码回归 13/13；进程安全回归 11/11。数据库迁移 `202608260002_add_monitor_phase2` 已写好但**未在生产应用**（本地 prisma generate 完成）。变更文件：backend/prisma/schema.prisma + 迁移、backend/src/modules/companion-monitor/{service,controller}.ts、backend/test/unit/companion-monitor.service.spec.ts、backend/test/mocks/prisma.mock.ts、desktop-companion/{companion_state,companion_heartbeat,webview_window,companion_app,companion_collector}.py、desktop-companion/tests/test_heartbeat.py。
- 2026-08-26 14:45 Asia/Shanghai: Implemented and shipped the 伴侣监控中心 (companion monitoring center) end-to-end (backend + frontend + Pixingyun Mate 3.2.102, packaged and deployed together — details in `docs/deployment-log.md` 14:45 entry). Super-admin/owner/admin now see every installed companion on the website without checking each PC: device list (设备/使用人/版本/在线状态/当前任务/平台状态/最后采集/最后同步/最近异常/最后心跳) with 全部/在线/离线/异常/版本过旧/正在采集/正在同步 filters, health overview (正常/异常/离线/版本过旧 counts + 今日采集成功率/今日同步成功率 + 最新版本), open-alert panel with acknowledge, per-device 7/30-day heartbeat+alert history, 30s auto-refresh. Companion sends a 45s heartbeat with 15+ fields (device id/name, login user, version, task status+detail+startedAt, per-platform login status 抖音/视频号/抖店, last collection, last sync, last error, auto-update status, recent 403/404/500 counts, own-process CPU/memory/uptime — own PID only). 8 alert rules (连续3次同步失败/超24h离线/心跳超5分钟/任务超时/自动更新失败/版本过旧/403·404·500异常增多/大量登录态失效) evaluated every minute with dedup; offline thresholds 0-2min在线/2-5min不稳定/>5min离线; 30-day history retained (cleaned daily 03:30). Privacy red lines enforced: only the companion's own process/tasks are reported (no other software, no browsing history, no web/screen content, no remote kill/uninstall). Backend unit tests 16/16 (full suite 27 suites / 239 tests green), migration `202608260001_add_companion_monitor` applied on the server, backend backup `/opt/matrixflow/releases/backend/20260826143115`. Post-deploy hotfix: `lastSync.success=null` (no sync yet) was counted as a sync failure — now only explicit false counts (regression test added, 17/17); backend redeployed (backup `20260826144401`), inflated counters cleared. Companion artifacts: installer + portable + lite ZIPs (see `docs/deployment-log.md` 14:45 entry for hashes); local `D:\Pixingyun` runs 3.2.102 and its heartbeats are live on production (device online, overview normal=1).
- 2026-08-25 18:22 Asia/Shanghai: Follow-up to the 17:33 commerce gating (details in `docs/deployment-log.md` 18:22 entry) — "members still see the pages" turned out to be two MANAGER (管理者) accounts (`nana`, `1321427839@qq.com`) which are above 组长 by design; all 17 MEMBER accounts are correctly blocked. Shipped a frontend-only hardening: `AppLayout` refreshes user info from `/auth/me` on boot so role changes made in 超管后台 take effect on the next page load (no more stale persisted roles). Frontend redeployed `assets/js/index-D7VcOktl.js`; Cloudflare cache purged.
- 2026-08-25 17:33 Asia/Shanghai: Added the 组长 (GROUP_LEADER) role and made 微信小店/抖店/业绩天梯 visible only to 组长及以上 (details in `docs/deployment-log.md` 17:33 entry). New enum value + migration `20260824200000_add_group_leader_role`, team roleHierarchy ranks 组长 2.5, the three commerce routes (plus their hidden source-detail sub-routes) carry `roles: [SUPER_ADMIN, OWNER, ADMIN, MANAGER, GROUP_LEADER]` for sidebar + route-guard gating (MEMBER/VIEWER redirect to 仪表盘); no other permissions/sections changed — 组长 keeps the member permission tier and gains no team/MCP/admin rights. 超管后台 can assign 组长; 个人设置 shows the label. Backend deployed with migration (backup `/opt/matrixflow/releases/backend/20260825173232`), frontend public entry `assets/js/index-CcKAkRK8.js`; post-deploy diagnose OK.
- 2026-08-24 16:05 Asia/Shanghai: Published Pixingyun Mate `3.2.87` — fixes the Doudian sync timeout that repeatedly showed "Page.wait_for_timeout: Connection closed while reading from the driver" on the big store (唐商披星). Root cause: full order+aftersale pagination takes ~7.5-8 min, right at the 480s collect timeout, which cancels the browser mid-wait and surfaces as a driver disconnect. Fixes: per-page waits tightened (settle 1500→900ms, networkidle 2000→1200ms, source-capture retries 6→3), collect timeout raised 480→660s, and the disconnect error now maps to a friendly 抖店采集超时 prompt. Also performed the operator-requested Doudian data reset (full backup → wipe orders → re-collect via companion sync → restore the pre-window 06-12~06-20 band from the 08-20 DB dump with ON CONFLICT merge); final state identical to pre-wipe (唐商披星 733 orders, 披星教育 147 orders, aftersales 667, orphans 447). Installer `pixingyun-mate-setup-3.2.87.exe` (451,906,532 B) and portable ZIP `pixingyun-mate-portable-3.2.87.zip` (348,082,956 B) published; manifest 3.2.87 kind=portable; local install replaced (backup `D:\Pixingyun.bak-3287`) and runs 3.2.87. Details in `docs/deployment-log.md` 08-24 15:53 entry.
- 2026-08-24 19:06 Asia/Shanghai: Global 刷新中 overlay image fixed at a 200px circle (details in `docs/deployment-log.md` 19:06 entry): `min(200px, 35vw)` responsive sizing (small screens shrink to avoid overflow), 2.4s slow continuous spin, 20px image-text gap, `border-radius: 50%` + `object-fit: cover` kept; refresh logic untouched. Frontend redeployed, public entry `assets/js/index-BPo7jbfN.js`, Cloudflare cache purged.
- 2026-08-24 15:07 Asia/Shanghai: Fixed 业绩天梯 three reported bugs and deployed (details in `docs/deployment-log.md` 15:07 entry): (1) double 'Teacher not found' toast — a deleted teacher (王晶) was DELETEd again via a stale reference, and the same 404 was toasted by both the axios interceptor and the component; now teacher messages are Chinese, teacher APIs are silent, delete is optimistic with 404 treated as already-deleted, and stale teacher ids self-heal (create on non-`pltea_` id / recreate on PATCH 404). (2) 有效订单 tile was bound to the refund count (showed 255) — rebound to valid count (1059); 总订单 tile sub-line replaced the literal `?? + ??` placeholder with 有效 X + 退款 Y; source-detail drawer duplicate 退款订单 label fixed to 有效订单. (3) Whole-page mojibake audit clean (only that one literal `??` existed). Deployed backend + frontend: public entry `assets/js/index-DwNm7xmO.js`, ladder chunk `PerformanceLadderView-s1uXaUtr.js`; post-deploy diagnose DIAGNOSE OK. New read-only diagnostic `scripts/diag-ladder-teachers.py`.
- 2026-08-24 14:58 Asia/Shanghai: Added the daily Doudian reconciliation (每日自动对账, details in `docs/deployment-log.md` 08-24 14:58 entry): new `backend/src/modules/scheduler/daily-reconciliation.scheduler.ts` (cron 09:10 Beijing) compares the page caliber against an independent pure-SQL recount for each store's previous day (count/total/valid/refunded/gross/refund + 总订单=有效+退款 invariant), pushes 🔴/✅ via the existing Feishu pipeline, manual trigger `POST /api/v1/scheduler/trigger/reconciliation` + `scripts/trigger-reconciliation-once.py` helper; pure comparison logic in `reconcile-compare.ts` with unit tests. Also fixed four pre-existing failing backend test suites (test-only: missing mocks/signatures) so the full suite is green (33 suites / 303 tests). Backend deployed (backup `/opt/matrixflow/releases/backend/20260824145543`), live-triggered reconciliation passed for both stores and pushed the ✅ Feishu message. WeChat counting logic untouched.
- 2026-08-24 14:33 Asia/Shanghai: Built the Doudian data-correctness prevention layer after the 14:25 counting fix (details in `docs/deployment-log.md` 08-24 14:33 entry): new single-source-of-truth `docs/订单口径规范.md` (totalOrderCount = validOrderCount + refundedOrderCount; 退款按订单日归集, never by refund update_time; implementation locations for all pages); `scripts/diagnose-production.py` data sanity now also enforces zero duplicates in order/aftersale tables (Doudian + WeChat), zero orphan orders/aftersales, zero negative aftersale amounts, and zero orders with createTime<=0, plus an informational `cross_day_doudian_refunds` metric (cross-day refunds are expected and归订单日); the api-consistency check now fetches each Doudian store's 30-day summary over the authenticated API and fails on any store where total != valid + refunded; DoudianView KPI sub adds 退款按订单日归集 hint. Frontend redeployed `assets/js/index-Y-HPc1-x.js`. WeChat counting logic untouched.
- 2026-08-24 14:25 Asia/Shanghai: Fixed the Doudian order counting caliber (user-reported wrong 8/21 披星教育 and 8/22 唐商披星 data; details in `docs/deployment-log.md` 08-24 14:25 entry): refunds are now attributed by ORDER create date (cross-day refunds归订单日), 有效订单 = 营收订单 − 退款订单, 总订单 = 有效 + 退款 (no double counting). Backend + frontend deployed; unit tests updated (backend 4/4, frontend 42/42); post-deploy diagnose OK.
- 2026-08-24 12:53 Asia/Shanghai: Published Pixingyun Mate `3.2.86` superseding `3.2.85` — fixed the zip self-update apply writer (Path.write_text bom TypeError) that broke in-app updates in 3.2.84/3.2.85; companions on those versions must update manually once (download ZIP and unzip over install dir, or run the installer), from 3.2.86 auto-update works again. ZIP 261,843,481 bytes SHA256 bb50cd45a1835a6488476b3bb73b06c225f25e251d50bc2cba597c7fdaca9bde; installer 396,556,744 bytes SHA256 1b7670df648ba03ba648ad0d2e5308802894a9bc7b7921da0806a9446735bdaf.
- 2026-08-24 12:16 Asia/Shanghai: Published Pixingyun Mate `3.2.85` fixing the Doudian upload outage caused by system-proxy hijack (Clash). All companion outbound HTTP now bypasses the system proxy; network errors are classified with friendly per-code messages; Doudian scheduler fast-retries 5 minutes after network failures. ZIP 261,843,957 bytes SHA256 1030a8534db701f166dbde7930cf96e4fd290a77692ec3130c27c856d4934c48; installer 396,588,862 bytes SHA256 90d720fcc23db57c80b109f05f1c5f2592764ab7773cd707366fb8fe202ade92. Local D:\Pixingyun also deployed.
- 2026-08-24 11:16 Asia/Shanghai: Deployed 刷新动画图片多图片库 + 随机播放升级（details in `docs/deployment-log.md` 08-24 11:16 entry）: 设置页支持多图上传/缩略图圆形预览/单张删除/清空/查看大图/随机播放开关/指定默认图；刷新中随机选图、长刷新每 2.5s 淡入淡出轮播、避免连续重复；组织级持久化复用 OrganizationSetting；旧单图配置自动迁移；无图时用默认 Logo。
- 2026-08-21 19:14 Asia/Shanghai: Deployed frontend-only 全站操作动效 polish（details in `docs/deployment-log.md` 19:14 entry）: 统一动效令牌与缓动、Dialog/下拉/Message 过渡、按钮按压、sidebar 平滑收展、数字滚动、刷新压暗呼吸、删除收起动画、prefers-reduced-motion 支持；业务逻辑零改动。
- 2026-08-21 19:02 Asia/Shanghai: Deployed the site-wide unified loading experience and organization-level 刷新动画图片 setting (details in `docs/deployment-log.md` 19:02 entry). Global LoadingOverlay + Pinia loading store + 设置页上传/预览/恢复默认 + OrganizationSetting 表/接口/公开图片目录，全站数据页统一接入，业务数据逻辑未动。
- 2026-08-21 18:28 Asia/Shanghai: Deployed frontend-only 出单来源 unification and ladder ranking responsive layout (details in `docs/deployment-log.md` 18:28 entry). 微信/抖店出单来源及两个来源详情页统一为天梯口径：主数字=有效订单(去退款)，副文案 总订单X·退款Y，total=valid+refunded；天梯排行改容器查询响应式（3/2/1列、卡片内上下结构、ellipsis+title、clamp()主数字）。
- 2026-08-21 17:58 Asia/Shanghai: Deployed the Doudian stale-binding recovery chain and published Pixingyun Mate `3.2.83` (details in `docs/deployment-log.md` 17:58 entry). Backend structured `DOUDIAN_STORE_NOT_FOUND` (404) + `errorCode` passthrough in the exception filter; companion auto-relinks by unique store-name match when the saved cloud id is dead, sanitizes legacy raw error text, and ships friendly messages only.
- 2026-08-21 17:25 Asia/Shanghai: Deployed the post-audit defensive fixes (backend + frontend; nothing published for the companion; Doudian/WeChat store counting semantics were deliberately left untouched per operator instruction). Deploy records are in `docs/deployment-log.md` (2026-08-21 17:25 entry).
- 2026-08-21 17:20 Asia/Shanghai: Applied local-only defensive fixes after the full project audit (nothing deployed, nothing published; Doudian/WeChat store counting semantics were deliberately left untouched per operator instruction). Changes: (1) `frontend/src/views/monetization/DoudianView.vue` — `loadData` now uses a latest-request-wins `requestId` guard and captures store id/range/mode at start, the duplicate `@change="loadData"` on both store selects was removed (watchers already cover changes), and `storeAlertTitle` no longer hides `syncError` when cached data exists (appends 当前展示历史缓存数据); (2) `frontend/src/views/monetization/DoudianSourceDetailView.vue` — same latest-request-wins guard; (3) `frontend/src/views/accounts/AccountListView.vue` — `handleBatchDelete` now removes successfully-deleted ids from `selectedIds` so the floating bar no longer shows stale selections; (4) `backend/src/modules/analytics/analytics.service.ts` — removed the median×20 outlier zeroing in `getAccountDetailList` (real viral days are no longer silently zeroed; raw increments are used directly) and `numberOrNull` gained `allowNegative` so WeChat Video period-metric `net_fans` can be negative (掉粉 shows -300 instead of 0); (5) `backend/src/modules/platforms/platforms.service.ts` — delta fill allows negative followers increment (`followers` only, other fields stay clamped) and added an `: string[]` annotation on the avatar-cache readdir so the platforms unit suite compiles. Reverted during the round (restored byte-equivalent): backend `doudian-store-metrics.ts` summary counting, frontend DoudianView `orderStats` fallback, and the two 业绩天梯 hero tile bindings — these were rolled back to keep current Doudian/WeChat store numbers unchanged. Verification: backend `nest build` passed; frontend `vue-tsc` typecheck passed; frontend vitest 42/42 passed; frontend vite build passed; targeted backend jest (analytics, doudian-store-metrics, doudian-browser.controller) passed 37/37; `platforms.service.spec.ts` has 1 pre-existing failing test (`stores follower period metrics...` expects metadata.periodMetrics that the working tree no longer writes — unrelated to this round). The UI template closure is now balanced so the startup script runs outside Vue `<template>` content; local `D:\Pixingyun` renders the dashboard correctly and the public companion manifest points to `3.2.81`.
- 2026-08-21 16:06 Asia/Shanghai: Replaced the Pixingyun Mate login-template hotfix with clean version `3.2.80`. Local validation caught that the previous `3.2.79` artifact had mojibake-corrupted platform-name literals in `companion_state.py`, so the platform names were changed to Unicode escapes, the companion was rebuilt/republished, and the local `D:\Pixingyun` install now reports `/health` version `3.2.80`.
- 2026-08-21 15:52 Asia/Shanghai: Published Pixingyun Mate `3.2.79` to fix the companion login screen occasionally exposing raw Vue template expressions such as `{{loginLoading?...}}`. The login root now uses `v-cloak`, and login/Feishu buttons use mounted-state conditional spans instead of ternary mustache text. Public update manifest and both download artifacts are live and verified.
- 2026-08-21 15:26 Asia/Shanghai: Deployed frontend-only Performance Ladder order semantics fix. Ladder target/progress/ranking/store/source views now use fixed `totalOrderCount`, `validOrderCount`, and `refundedOrderCount` semantics, with goal metrics based on valid orders. Published frontend ref `assets/js/index-C31qaCg9.js`; public health, page ref, Worker route, migrations, PM2, and data sanity checks passed.
- 2026-08-21 15:08 Asia/Shanghai: Deployed Matrix Data period semantics fix. Backend safe deploy succeeded with Prisma migrate check (no pending migrations) and PM2 restart; frontend fast deploy published `assets/js/index-BsNnq0vR.js`. Production health, public page, Worker route, migrations, PM2, and API/data sanity checks passed via `scripts/diagnose-production.py --remote`.
# Project Change Log

Latest change note: at 2026-08-26 Asia/Shanghai, published Pixingyun Mate 3.2.101 after real-machine regression of the process-safety release. Full on-machine scenarios passed with ZERO user-process loss (scan-bind open/cancel, Doudian sync/cancel for two stores, Doudian profile-occupied → friendly 「抖店浏览器启动失败，请关闭残留浏览器窗口后重试」 with the occupying Chrome surviving and PROFILE_NO_MANAGED_PROCESS logged, companion close/restart — user Chrome 20/20, Weixin 6+12, Feishu 14, Explorer 1 unchanged throughout). One defect found and fixed: stale dead records were never pruned from managed_processes.json (21 dead records accumulated across restarts); terminate_managed's already-gone path now unregisters and terminate_stale_records prunes dead PIDs, covered by new regression test test_11 (suite 11/11). Published portable/lite/installer 3.2.101 with public verification (manifest 3.2.101, magic bytes 504B0304 / 4D5A5000); local D:\Pixingyun replaced with 3.2.101 (backups: .bak-3301 = 3.2.99, .bak-3300 = 3.2.100).

Latest change note: at 2026-08-26 Asia/Shanghai, published Pixingyun Mate `3.2.100` — the P0 process-safety + updater WSH fix release. (1) Process safety: new `desktop-companion/process_registry.py` managed-process registry; every terminate/kill now goes through five-way verification (PID alive, registered, create_time matches to defeat PID reuse, executable matches, cmdline/profilePath matches) and unmanaged processes are refused with a SKIP_UNMANAGED_PROCESS guard-log entry; `browser_manager.py` profile/stale cleanups no longer scan the whole system by command-line guessing; `companion_app.py` Doudian cleanup dropped the PowerShell full-table `CommandLine -like` + `Stop-Process -Force` (which could kill any software whose command line contained the profile path); `weixin_channels_collector.py`/`collect_and_upload.py` verify CDP `/json/version` Browser-Pid ownership before `connect_over_cdp` and no longer call `Browser.close()` (which used to close the user's entire browser); `chrome_cdp.py` taskkills only registry-verified PIDs; every browser launch point registers the new process via before/after snapshots. 10/10 regression tests in `tests/test_process_safety.py`. (2) Updater: `run_update.vbs` removed — it was written as UTF-8-SIG and Windows Script Host treats the EF BB BF BOM as an invalid character (error 800A0408, reproduced locally); the updater now launches `powershell.exe` directly hidden (CREATE_NO_WINDOW + SW_HIDE), validates generated scripts before execution (`_validate_generated_script`), and reports failures as 「自动更新失败，请重新尝试。」 instead of popping repeated WSH dialogs. (3) Build: both PyInstaller specs now bundle `process_registry.py` in datas; version bumped 3.2.99 → 3.2.100. Release gates before upload: 137 source files compile clean, process-safety suite 10/10, updater end-to-end verified. Published portable ZIP (348074580 B, sha256 71692168e9a21802ffa340c47405ea15aa48f20d38182bfd90ecd782076f9189), lite ZIP (155837280 B, sha256 9f99980fc7c7033ec90f572841ee2023639905eb78451c0ebde41bc67a168325) and installer (451954913 B, sha256 d2e574d70cd438adc6b14ea226774d46b1659f64049170053ac8c549af833aa0); public latest.json verified (kind=portable with lite/installer metadata), all three downloads 200 with correct magic bytes, Cloudflare cache purged.

Latest change note: at 2026-08-24 12:16 Asia/Shanghai, published Pixingyun Mate `3.2.85` to fix the Doudian sync outage where both stores reported 上传披星云服务器失败 while logged in. Root cause: companion outbound HTTP read the Windows system proxy (Clash Verge 127.0.0.1:7897), so every upload chunk went through proxy nodes; a node outage on 2026-08-24 ~10:49-11:14 Beijing reset every request at TCP level (10054, no HTTP status), and the server received zero requests in that window (verified server-side via nginx/pm2 logs). Fixes shipped: (1) all companion outbound HTTP — Doudian upload/rebind/relink, auth login/token refresh, collector uploads, login-worker uploads — now uses no-proxy sessions (trust_env=False); (2) network failures are classified into CONNECTION_RESET/CONNECTION_REFUSED/DNS_FAILURE/TLS_ERROR/TIMEOUT/NETWORK_ERROR with friendly per-code UI messages while raw details stay in logs; (3) Doudian scheduler fast-retries 5 minutes after a network-class failure instead of waiting for the 30-minute interval. Verification: upload chunk latency dropped from ~6.5s (proxy) to ~0.6s (direct); manual sync of both stores succeeded (server 201s); public manifest version 3.2.85; portable ZIP 261,843,957 bytes SHA256 1030a8534db701f166dbde7930cf96e4fd290a77692ec3130c27c856d4934c48; installer 396,588,862 bytes SHA256 90d720fcc23db57c80b109f05f1c5f2592764ab7773cd707366fb8fe202ade92. Local D:\Pixingyun deployed from the same build (backup D:\Pixingyun.bak-proxyfix-20260824-115803). Known follow-up: the update package download itself still uses urllib with the system proxy (~200KB/s observed); Clash Verge rule enhancement for ddddkkiii.com DIRECT was added locally and applies on next Clash reload. Server stability audit (read-only): ECS is 2 vCPU/1.6GB RAM, matrixflow Node heap ~95%, PostgreSQL had not-properly-shut-down recovery events on 2026-08-14 (disk-full) and 2026-08-21; recommend memory upgrade and alerting.

Latest change note: at 2026-08-26 Asia/Shanghai, continued the nickname-hardening verification pass (local only, not deployed). Injected the page-UI noise set directly into the companion metrics nickname-extraction JS (all four strategies now reject noise words before the Python gate), verified the injected JS syntax with node --check, and verified the noise functions against 27 page words / 5 real nicknames / uid samples (all behave as expected; real nicknames never blocked). Checked the remaining extraction paths: the Douyin identity extractor uses key-based storage/JSON matching (no page-text fallback) and the generic scan heuristics are noise-filtered for all platforms; the server-side scan-bind path already applies the noise filter (and is production-disabled anyway). Full backend Jest now `304/304` (accounts.service `30/30` including the new page-title nickname regression test). Companion full compile passed and the encoding regression remains `13/13`. Nothing deployed.

Latest change note: at 2026-08-26 Asia/Shanghai, re-audited the WeChat Video nickname hardening and closed two remaining gaps (local only, not deployed). (1) Finder-uid scans with a RELIABLE nickname could still create a duplicate account keyed by `v2_...@finder` instead of reusing the existing same-name account — the scan-bind lookup now reuses the unique same-platform same-name cloud account in that case without touching its `platformUserId`. (2) `_is_unsafe_nickname_fallback` in `companion_collector.py` received the same page-UI noise words. Also added a backend regression test asserting page-title nicknames (最近视频 etc.) are never written on account creation. Production re-scan (wide patterns: finder uids, uid-like nicknames, page-UI-word nicknames, zero-fan never-collected shells) found 0 issues across 19 accounts. Verification: companion compile passed, backend `tsc` passed, backend Jest `303/303` + accounts.service `30/30` passed. Not deployed.

Latest change note: at 2026-08-26 Asia/Shanghai, hardened WeChat Video nickname/identity recognition against page-UI text being captured as account nicknames (local fix, not deployed). Root cause of the junk 最近视频 account: scan-bind identity extraction accepted a finder-format uid (`v2_...@finder`) as `platformUserId` and fell back to page text (最近视频 module title) as the nickname, and no noise filter covered it, so the cloud account was created. Fixes: (1) extended the noise-word lists with page UI titles (最近视频/最近作品/视频数据/数据概览/内容数据/作品数据/今日数据/数据趋势/热门视频/视频列表/作品列表/全部视频/全部作品/视频明细/粉丝数据/观众数据/直播数据/商品数据/订单数据/账号概览/内容洞察/互动管理/图文数据/视频动态/视频号动态/查看全部/更多) in `companion_login_worker.py` (Python noise set + injected JS noise set), `companion_metrics.py`, `companion_collector.py`, and backend `accounts/platforms/scan-bind` services; (2) added a scan-bind identity guard: when WECHAT_VIDEO identity is finder-uid-only AND the nickname is unreliable, the flow switches to local-only binding with a 昵称采集失败 sync error and never creates/updates a cloud account. Data repair: deleted the junk account `cmt8hoson08alw7s4v41gwtmh` (最近视频, `v2_060000227c00b4e03daaa37a34a752d074338ffea675@finder`, 1 zero-value DailyStats row, 1 operator row) via the accounts API; verified no other page-title-like nicknames remain. Verification: companion compile passed, backend `tsc` passed, full backend Jest `303/303` passed. Not deployed.

Latest change note: at 2026-08-23 Asia/Shanghai, corrected a regression in the 矩阵数据 period-status fix and redeployed the backend. The previous deployment marked companion-reported `periodMetrics` periods as `complete` regardless of collection freshness, so stale platform totals (e.g., collected 08-06/08-12) were wrongly treated as current data. The completeness rule now requires the metadata collection to be recent (within the last 2 days for week/month; collected today for day) — older in-window metadata is `partial`（数据不完整·更新至xx）and out-of-window metadata is `historical`（历史数据·截至xx）. Added three regression tests (stale→historical, in-window-stale→partial, fresh→complete with full coveredDays) to `backend/test/unit/analytics.service.spec.ts` (34/34 pass). Production redeploy succeeded (PM2 online, health 200, backup `/opt/matrixflow/releases/backend/20260823120951`) and a production read-only recheck found 0 accounts with stale dates marked complete.

Latest change note: at 2026-08-21 Asia/Shanghai, fixed the 矩阵数据 page period-data presentation and status judgment (frontend + backend, local then deployed). Backend `analytics.service.ts getAccountDetailList` no longer derives completeness from DailyStats row counts alone: when the companion-reported `periodMetrics` (platform-native day/week/month period totals) supplies the value, the period is marked `complete` with full `coveredDays` (week 7 / month 30 / day 1) instead of being mislabeled 数据不完整 because DailyStats rows are sparse. Frontend: the account table now only visually weakens `historical` rows (muted values via `stale-value` plus row tint), while `partial` rows display values normally with their 数据不完整 label; rows carry an explicit `periodState` field; the top-card drilldown drawer now splits into 参与汇总（N 个账号） and a collapsed 历史参考账号（N）· 不参与当前周期汇总 section. The top summary keeps the existing aggregation rule (complete + partial only; historical never participates) and the 数据总览 header now carries a tooltip stating that rule. Day=昨日 / week=最近7个自然日 / month=最近30个自然日 semantics unchanged. Verified: frontend typecheck and production build passed; backend `tsc --noEmit` passed; backend analytics Jest `31/31` passed. Duplicate-nickname accounts were audited read-only and are all cross-platform pairs (Douyin + WeChat Video), not same-platform duplicates.

Latest change note: at 2026-08-21 19:25 Asia/Shanghai, published Pixingyun Mate `3.2.84` with the Doudian auto-relink response-envelope fix. Root cause found during the 唐商披星 investigation: the local companion's saved `cloud_store_id` pointed to DoudianStore records that were deleted and re-created on 2026-08-13 (no audit log; direct DB rebuild), and the companion's `relink_companion_store` parsed the wrapped `{code,message,data}` store-list response as a bare array, so the by-name auto-relink always matched zero stores and the recovery always failed with 云端抖店店铺不存在. `relink_companion_store` now unwraps the `data` envelope (bare list, `data` list, and nested `stores/list/items` all supported). Data-level repair also completed for both local stores (唐商披星 and 披星教育): cloud stores re-bound via `POST /doudian-browser/stores/:id/rebind` (storeId unchanged, all order/product/aftersale history preserved) and the local companion config updated to the new cloud ids; verification syncs succeeded (唐商披星 600 orders/10 products/600 aftersales, 披星教育 132 orders/2 products/24 aftersales) and the stale website error display was cleared. Public manifest version `3.2.84`, kind `portable`; ZIP size `348016353` SHA256 `c79c269b85485f8d0acde22f093bd7490e80b094df94dab2d800dceae9277f46`; installer size `451901007` SHA256 `596aa2ab601dfe930bd73b74970db50e659502bc23096f2a14d2ace6106ae2f1`. Local `D:\Pixingyun` replaced with the full `3.2.84` onedir, `/health` returns `3.2.84`.

Latest change note: at 2026-08-21 17:05 Asia/Shanghai, published Pixingyun Mate `3.2.82` (Chinese encoding release) and deployed the frontend JWT UTF-8 fix. The companion release ships the unified encoding module and all mojibake fixes: unified subprocess output decoding (UTF-8 → gb18030 → replacement), UTF-8 text IO with BOM for .ps1/.vbs update scripts, tolerant legacy GBK file reads, JSON writes with `ensure_ascii=False`, and the guarded stdout reconfigure. Public manifest version `3.2.82`, kind `portable`; portable ZIP size `348012126`, SHA256 `09858a7b863af8bb89a3c9de88d86c7f23f35561e2f82adb57a394efcef7dadd`; installer size `451894387`, SHA256 `d746e17dcdb26820a2c14f1c2ccdf9abf8fee9f989bff917764c0e954996f459`. Local `D:\Pixingyun` replaced with the full `3.2.82` onedir, `/health` returns `3.2.82`, update check `available=false`. Frontend deployed with the JWT payload UTF-8 TextDecoder fix; public bundle `assets/js/index-CDX2ZDRS.js`.

Latest change note: at 2026-08-21 Asia/Shanghai, performed a project-wide Chinese mojibake governance pass (local only; nothing deployed or published). Created the unified encoding module `desktop-companion/companion_encoding.py` (decode_bytes with UTF-8 → gb18030 → replacement order and no errors='ignore'; read/write_text_file with optional BOM for .ps1/.vbs; JSON helpers with ensure_ascii=False; run_cmd capturing bytes and decoding centrally) and applied it to fix real encoding bugs: chrome_cdp netstat output was decoded as GBK with errors='ignore'; companion_video_editor capcut/ffmpeg/ffprobe output was decoded as UTF-8 with errors='replace' (corrupting GBK-emitting tools); browser_manager playwright install used text=True locale decoding; companion_login_worker wrote cookie_info.json without an encoding on Windows; companion_updater wrote .ps1/.vbs update scripts without UTF-8 BOM (PowerShell 5.1/wscript ANSI misread risk); sau_backend read cookie files without encoding; several state.json reads are now tolerant of legacy GBK files; companion_app stdout reconfigure now guards a None stdout in windowed builds; frontend `utils/jwt.ts` now decodes JWT payloads with TextDecoder UTF-8 instead of atob latin1. Added encoding regression tests: `desktop-companion/encoding_regression_test.py` (13/13 pass: JSON save→fresh-process read, subprocess UTF-8/GBK/invalid-byte decode, companion→Node JSON stack→readback, SQLite roundtrip, legacy GBK file tolerance, decode order) and extended `frontend/src/__tests__/jwt.test.ts` with Chinese payload roundtrips (5/5 pass). Scanned for historical mojibake: production PostgreSQL (23 tables, 9 mojibake patterns) and local dev SQLite found 0 hits; 8 sampled production Chinese nicknames roundtrip byte-exactly as strict UTF-8. No data was modified.

Latest change note: at 2026-08-21 Asia/Shanghai, removed the duplicate data-status tag shown after the account nickname on the 矩阵数据 dashboard account table. The stale/period status label was rendered twice — once next to the account name and once in the dedicated 数据状态 column (both used the same `periodStatus.label` value). The nickname cell now shows only the account name; data status remains in the 数据状态 column with its tooltip. Verified with `npm run typecheck --workspace=frontend`. Frontend-only change; not deployed to production.

Latest change note: at 2026-08-21 Asia/Shanghai, unified the Performance Ladder 出单来源排行 operator display with the account 主负责人 data. The ladder no longer maintains its own name-keyed `sourceOperators` map as the primary operator source. Source operators are now resolved in this order: (1) stable source IDs from order data (WeChat `source_infos.account_id` / Doudian `author_id`) matched exactly against `Account.platformUserId` with platform preference (微信小店→WECHAT_VIDEO, 抖店→DOUYIN); (2) exact normalized nickname match requiring a unique account; (3) legacy `sourceOperators` only as fallback. The operator editing input in the source detail drawer was removed and replaced with a read-only three-state display: matched account primary operator (自动同步自账号接入), matched account without primary operator (未设置主负责人), or no matched account (未匹配账号). The ladder now fetches the full account list alongside store orders to build the platform-user-id/nickname indexes. Verified with `npm run typecheck --workspace=frontend` and `npm run build --workspace=frontend`. Frontend-only change; not deployed to production.

Latest change note: at 2026-08-21 Asia/Shanghai, added `docs/project-architecture-deep-dive.md` — a consolidated deep-dive reference written after reading the core docs, Prisma schema (29 models), backend modules (auth/tenant/guards, analytics, platforms reportMetrics ingest, wechat-store sync, doudian-browser, schedulers, mcp, scan-bind gateway), frontend (router, request interceptor, dashboard composables, companion bridge), and desktop companion (collectors, local_db, updater, video editor). The document records the four core data flows, official deployment chain, business order-counting 口径, known pitfalls, and a local/remote state snapshot. Documentation-only change; production was not modified. Public cross-checks: health `200`, companion manifest `3.2.78`.

Latest change note: at 2026-08-21 14:50 Asia/Shanghai, added `docs/project-handover-text.md` as a Chinese project handover guide. It summarizes the current production topology, official frontend/backend/Cloudflare/companion deployment paths, upload locations, verification commands, rollback notes, mini-program release flow, and legacy paths/scripts that should not be treated as current production truth. This was a documentation-only change; production was not modified.

Latest change note: at 2026-08-21 14:38 Asia/Shanghai, shipped the WeChat Store/Doudian total-order display and Doudian companion binding recovery update. WeChat Store and Doudian pages now show `总订单`, `有效订单`, and `退款订单`; total order count follows the current business wording as `validOrderCount + refundedOrderCount`, and refunded orders are deduplicated by `order_id` with aftersale id as a fallback only when no order id exists. Doudian backend summary now returns `totalOrderCount`, `validOrderCount`, and `refundedOrderCount` without changing historical order/aftersale records. Doudian companion uploads now return structured `STALE_COMPANION_BINDING` 409 conflicts for stale local-profile bindings, expose `POST /doudian-browser/stores/:id/rebind`, and Pixingyun Mate 3.2.78 automatically rebinds the same cloud store once before retrying upload while hiding raw `400 Client Error` URLs from users and logging raw response details locally/server-side. Production deploy completed for backend, frontend, and companion update manifest; public frontend bundle is `assets/js/index-CCWJSjbZ.js`, public manifest is Pixingyun Mate `3.2.78`, and verification passed for public home, API health, manifest, and download HEAD checks.

Latest change note: at 2026-08-21 12:14 Asia/Shanghai, deployed the account freshness and Performance Ladder config persistence update. The companion code now separates login keepalive from collection freshness, reports collection attempt/success/failure state, and can run one guarded startup catch-up collection when local account data is stale; the companion binary/update manifest was not published. The web UI now exposes collection sync state separately from login state. Performance Ladder teacher targets, aliases, refund mode, hidden-unmatched mode, and source-account operators moved from browser localStorage into organization-scoped backend tables; localStorage is now used only for one-time admin-permission initialization when the server has no saved ladder config.

Latest change note: at 2026-08-20 19:08 Asia/Shanghai, fixed the Performance Ladder month-to-date and refund display behavior. The ladder now recalculates the current-month range on refresh and uses today's end-of-day as the query end, so same-day orders are included. Refund counts for teachers, stores, and source accounts now count refunded orders separately from effective orders, which keeps cards showing the main order number and the `go refund` number together. The obsolete project `.npmrc` `frozen-lockfile=false` entry was removed so npm no longer prints the unknown-project-config warning. Verified with frontend typecheck, frontend production build, successful deployment, public bundle `assets/js/index-YMYSWV9m.js`, and public health `200`.

Latest change note: at 2026-08-20 Asia/Shanghai, committed and pushed the full local working state to the private GitHub repository `ddddkiii/pi-xing-yun` on the `master` branch. This commit adds previously untracked source files (new Prisma migrations, backend unit tests, frontend utils/views/tests, Cloudflare Worker source, official deployment scripts, Pixingyun Mate `companion_video_editor.py`, docs) and commits all pending modified files. Excluded from tracking via `.gitignore`: local Inno Setup tooling, vendored `tools/crv-*` sources, the local installer exe, debug screenshots, and worker-cache backups.

Latest change note: at 2026-08-18 16:08 Asia/Shanghai, shipped the dashboard display and avatar persistence fixes. The multi-account table now keeps readable no-wrap cells and horizontal scrolling at narrower page widths, Douyin badges now use the same green visual treatment as Video Account badges, and the dashboard trend chart now scales its y-axis dynamically with visible points so small movements are easier to see. Collected account avatars are now cached through the backend via `/api/v1/platforms/avatar/:accountId`, preventing newly collected Douyin avatars from depending on short-lived signed remote URLs. Production verification passed with public home `200`, public HTML bundle `assets/js/index-phpyqjAz.js`, public health `200`, and missing-avatar route `404`.

Latest change note: at 2026-08-18 15:10 Asia/Shanghai, restored production WeChat Store/Video Shop order visibility after the sync pipeline had been stuck since a 2026-08-14 disk-full failure. The repair did not change WeChat Store or Doudian page counting rules. It restarted the backend to clear the stale in-memory sync lock, backfilled WeChat orders, and added source/hotpatch protection in `backend/src/modules/wechat-store/wechat-store.service.ts` plus the compiled production service: 30s upstream WeChat API timeouts, a 4-minute timeout around each store sync during `syncAllStores`, and failure-status marking when a store exceeds the timeout. Production verification showed public health `200`, all three stores `syncStatus=ok`, and current recovered counts for `Tangshang Pixing` today `11/9 effective`, `Pixing Education` today `8/5 effective`, and `Pixing Culture` yesterday `9/7 effective`.

Latest change note: at 2026-08-14 19:06 Asia/Shanghai, corrected `业绩天梯` so its default order count conforms to the 微信小店 and 抖店 page 有效订单 calculations. 微信小店 ladder orders now exclude statuses `10`, `12`, `200`, and `250`, matching the page 有效订单 logic and no longer requiring `ship_time > 0`. 抖店 remains on the shared Doudian revenue/effective-order helper. The `去退款` button now starts off, so the initial ladder total is platform 有效订单; turning it on applies an extra successful-refund deduction. Verification: `npm run typecheck --workspace=frontend`, `npm run build --workspace=frontend`, and frontend deploy passed; deployed frontend bundle `assets/js/index-0aA07nNK.js`.

Latest change note: at 2026-08-14 18:52 Asia/Shanghai, aligned `业绩天梯` source-account statistics with the existing source detail pages after data comparison found口径 drift. WeChat attribution now counts only shipped source orders (`ship_time > 0`) instead of counting every non-cancelled/non-unpaid order, preventing待发货 from inflating the ladder. Doudian attribution now reuses `getDoudianRevenueOrders` and `getDoudianSuccessfulRefundOrderIds`, matching the Doudian source-ranking revenue/refund口径. The page also exposes the counting rule in chips: `微信：已发货` and `抖店：营收订单`. Verification: `npm run typecheck --workspace=frontend`, `npm run build --workspace=frontend`, and frontend deploy passed; deployed frontend bundle `assets/js/index-BcxfOKkl.js`.

Latest change note: at 2026-08-14 18:44 Asia/Shanghai, upgraded the `业绩天梯` source-account detail drawer from a low-value attribution list into an order-flow view similar to existing source detail pages. Drawer order rows now show product image, product title, order time, status chip, shipped chip when available, order number, transaction source, and amount. WeChat Store uses product image/title, `product_price || pay_amount`, source type labels, status labels, and shipped state; Doudian uses product image/title, `pay_amount`, `doudianOrderStatus`, and author source. Verification: `npm run typecheck --workspace=frontend` and `npm run build --workspace=frontend` passed; deployed frontend bundle `assets/js/index-DDmI3lgo.js`.

Latest change note: at 2026-08-14 18:33 Asia/Shanghai, updated the standalone `业绩天梯` source ranking from a per-platform/per-store source list into a source-account attribution board. Ranking now groups by normalized source account name so identical account names are counted together across stores/platforms under the current filters. Top cards and list rows are clickable, opening a source-account detail drawer with grouped platform/store/order attribution. Each source account now has an editable operator stored in browser localStorage under `performance-ladder-source-operators`; the operator is displayed on cards and included in source search. Verification: `npm run typecheck --workspace=frontend` and `npm run build --workspace=frontend` passed; deployed frontend bundle `assets/js/index-C8p0v9R-.js`.

Latest change note: at 2026-08-14 18:21 Asia/Shanghai, polished the shared platform badge visual language so Douyin no longer appears as a near-black dot/pill on the dark account UI. The platform color source now uses visible Douyin red and TikTok cyan, and `PlatformBadge` now applies platform-colored label text plus a subtle platform-colored border while keeping the soft pill background that made WeChat Video feel comfortable. Verification: `npm run typecheck --workspace=frontend` and `npm run build --workspace=frontend` passed; deployed frontend bundle `assets/js/index-BosPr3E3.js`.

Latest change note: at 2026-08-14 Asia/Shanghai, upgraded the standalone `业绩天梯` UI from a basic data panel into a sales-report experience. The page now has a hero progress dashboard with `current / target` orders, remaining orders, remaining daily requirement, refund-exclusion and store-coverage metrics, clearer filter chips, teacher goal cards with status labels, a collapsed attribution-rules drawer for teacher targets/keywords, and a source ranking split into Top 3 podium cards plus a compact list. Verification before deployment: `npm run typecheck --workspace=frontend` passed.
Latest change note: at 2026-08-14 Asia/Shanghai, refined the monetization `业绩天梯` into a dedicated sidebar page under `商业转化`. The ladder now aggregates all WeChat Store/Video Account and Doudian stores for the current month, counts effective orders, supports a `去退款` toggle, and filters mixed source data through per-teacher recognition keywords. Default teacher profiles include 卢慧 and 王晶 with editable targets and aliases. Source ranking now hides unmatched sources by default, adds teacher/platform/search filters, and highlights the top three with medal icons plus `销冠`/`Top2`/`Top3` tags. Verification before deployment: `npm run typecheck --workspace=frontend` passed.
Latest change note: at 2026-08-14 Asia/Shanghai, revised the monetization ladder into a standalone `业绩天梯` navigation item under `商业转化` between `抖店` and `组织设置`. The new page aggregates current-month effective orders across all WeChat Store/Video Account and Doudian stores, supports a `去退款` toggle, defaults teacher targets for `卢慧` and `王晶` while allowing more teachers, and adds a source-ranking board so teams can compare which source/teacher is producing more or fewer orders. Removed the per-store ladder cards from the individual store pages. Verification: `npm run typecheck --workspace=frontend` passed.
Latest change note: at 2026-08-14 Asia/Shanghai, prepared a frontend-only monetization dashboard update for production deployment. Doudian and WeChat Store/Video Account monetization pages now include `当月` and `自定义` date filters, plus a new `业绩天梯` card with an editable monthly order target defaulting to `1600` orders. The ladder uses current-month effective/revenue orders, shows current orders, remaining orders, and progress percentage, and stores targets locally per platform store and calendar month. Verification before deployment: `npm run typecheck --workspace=frontend` passed.
Latest change note: at 2026-08-12 14:52 Asia/Shanghai, published Pixingyun Mate `3.2.67` for Win10/Win11 x64 compatibility. The compatibility gate now checks the actual release spec `pixingyun-mate-onedir.spec` instead of the old spec, and the real spec explicitly packages pywebview EdgeChromium plus pythonnet/clr loader imports. The native WebView2 window now has a guarded fallback: if pywebview/WebView2 is unavailable, fails to start, or renders blank, Pixingyun Mate opens its local UI in a dedicated app-window browser with Pixingyun-owned `ui-browser-profile`, preferring bundled Chromium so it does not depend on or modify coworkers' normal browser profiles. Public manifest version is `3.2.67`; automatic update ZIP is `pixingyun-mate-portable-3.2.67.zip`, size `66366419`, SHA256 `d1f35b1d2292f46dd0bece4216ea830661ff90b43ae547aae2274451bc609f8a`; full installer is `pixingyun-mate-setup-3.2.67.exe`, size `394604875`, SHA256 `9676e5a90947cd65ada849b0697eff0d69a18313491ca2c60f2666dd6a7660ae`. Local `D:\Pixingyun` is full `3.2.67`; `/health` reports `3.2.67`, update check is `available=false`, and bundled Chromium exists.

Latest change note: at 2026-08-12 14:22 Asia/Shanghai, published Pixingyun Mate `3.2.66` to stabilize Windows desktop/start-menu icons. The installer now creates shortcuts whose `IconFilename` points to the fixed bundled file `_internal\app_icon.ico`, and `pixingyun-mate-onedir.spec` includes `app_icon.ico` in the onedir payload. This prevents shortcuts from depending on overwritten exe icon resources or old versioned icon files such as `pixingyun-mate-3.2.56.ico`. Public manifest version is `3.2.66`; automatic update ZIP is `pixingyun-mate-portable-3.2.66.zip`, size `66343759`, SHA256 `8910300781a5ed6cdc4306ade7a359ac42a0f5c56ebcf8c2b015d4a5430ad423`; full installer is `pixingyun-mate-setup-3.2.66.exe`, size `394599436`, SHA256 `280a83129587fc4c2a468192cec0acd08a07e9bfdb22237647cddc047a4b0ea1`. No collection, Doudian, captcha, or browser lifecycle logic changed.

Latest change note: at 2026-08-12 13:06 Asia/Shanghai, published Pixingyun Mate `3.2.65` for browser lifecycle hardening and Doudian captcha compatibility. Added profile-scoped browser cleanup that only terminates Chrome/Edge processes whose command line contains a Pixingyun-owned profile path; scan-bind, Doudian, and account collection now close contexts with timeouts and clean only their own profile processes if shutdown hangs. Doudian browser resolution now uses shared system-browser discovery and retries profile-lock launches once; Doudian manual login still prefers system Chrome/Edge over bundled Chromium to reduce captcha blank-screen risk. Public manifest version is `3.2.65`; automatic update ZIP is `pixingyun-mate-portable-3.2.65.zip`, size `66345959`, SHA256 `9dbf15deef0218100ca2065df1887506f6dfaf8af8217d3eb56b993219aab24e`; full installer is `pixingyun-mate-setup-3.2.65.exe`, size `394598148`, SHA256 `73a29bfb354c1ba41466a0f67c757072b23acfbfbd03430032606a1d74ab429a`. Local `D:\Pixingyun` is full `3.2.65` and resolves bundled Chromium plus system Chrome/Edge fallbacks.

Latest change note: at 2026-08-11 20:01 Asia/Shanghai, published Pixingyun Mate `3.2.64` to correct browser packaging after `3.2.63` made the in-app ZIP too large for normal updates. The code now supports two release builds from the same source: leave `BUNDLE_PLAYWRIGHT_CHROMIUM` unset for the lightweight automatic update ZIP, and set `BUNDLE_PLAYWRIGHT_CHROMIUM=1` for the full installer that includes the dedicated Playwright Chromium under `_internal/ms-playwright/chromium-1223`. Public manifest version is `3.2.64`; main update package is `pixingyun-mate-portable-3.2.64.zip`, size `66341573`, SHA256 `2e1bb2ecd3b08a2df64d6249a46cf111dfbaa02668553c9c6c4667e4687f258a`; full installer is `pixingyun-mate-setup-3.2.64.exe`, size `394571414`, SHA256 `48a1fc74e1a5cc08464d67ff3d7199f838ab2312ee7f4b61aa1a080595b7cdd6`. Re-published manifest with ASCII notes to avoid update UI mojibake. Local `D:\Pixingyun` was replaced with the full `3.2.64` onedir and `/api/browser-info` resolves the bundled Chromium path.

Latest change note: at 2026-08-11 19:17 Asia/Shanghai, published Pixingyun Mate `3.2.63` to fix coworkers being unable to open the QR-login browser. The onedir app now bundles a dedicated Playwright Chromium and the runtime searches bundled Chromium before local/system browsers; Windows discovery now includes Chrome/Edge fixed paths, App Paths registry, and PATH. Frozen builds no longer attempt `pixingyun-mate.exe -m playwright install chromium`. Scan-bind browser startup now has explicit `starting`, `browser`, and `error` states, retries multiple candidates, and both UIs poll startup status to surface launch failures. Public in-app ZIP is `pixingyun-mate-portable-3.2.63.zip`, size `258595611`, SHA256 `7124707b1d9b83f731f884f1d452efc952a131a421fd45622bf39135a07fd34e`; public installer is `pixingyun-mate-setup-3.2.63.exe`, size `394581059`, SHA256 `cff0fb3c840fed700118fc30d0fae513a8b48aab50434510f805128927c09d52`. After a temporary Cloudflare Tunnel 502/530 during large-package verification, restarting `matrixflow-tunnel` restored healthy public downloads and production diagnose passed.

Latest change note: at 2026-08-11 18:02 Asia/Shanghai, deployed Cloudflare Worker `matrixflow-origin-proxy` version `3c4ce7fa-7f50-40ee-8671-4edc86501c89` using the existing Wrangler OAuth login. `.env.local` Cloudflare tokens only have purge/no-Workers permission, and `secrets.env` token has Workers read access but write calls are IP-restricted from the current network. The Worker now treats mutable companion aliases and `/companion-updates/*` as `no-store` and adds an internal origin cache-buster for those paths. Fixed aliases now return current `3.2.62` sizes: setup `258,658,550`, portable `66,314,931`; production diagnose passed.

Latest change note: at 2026-08-11 17:21 Asia/Shanghai, published Pixingyun Mate `3.2.62` after auditing the local companion health, update, account, cookie, Doudian, worker, and video-editor environment links. This small hardening release does not change collection parsing modules. It moves scan-bind token handoff from GET query strings to POST JSON, redacts token-like values in local Werkzeug request logs, and makes cancel-scan idempotent so accidental/repeated user clicks do not surface false `404` failures. Built and published `pixingyun-mate-portable-3.2.62.zip` plus `pixingyun-mate-setup-3.2.62.exe`; real local in-app update from `3.2.61` to `3.2.62` completed and `/health` now returns `3.2.62`. Public manifest, versioned links, and fixed setup/portable aliases are correct after the Worker no-store alias deployment.

Latest change note: at 2026-08-11 14:44 Asia/Shanghai, published Pixingyun Mate `3.2.61` and updated local `D:\Pixingyun` through real in-app updates. `3.2.61` keeps the Doudian captcha white-screen login fix, changes public companion updates to onedir ZIP packages to avoid PyInstaller onefile `_MEI...\\python312.dll` startup failures, adds updater health-check/rollback logging, cache-busts companion manifest checks, and makes `scripts/publish-companion-download.py` purge Cloudflare cache after uploads while correctly distinguishing portable ZIP vs portable EXE. Public manifest points to `pixingyun-mate-portable-3.2.61.zip` with installer metadata for `pixingyun-mate-setup-3.2.61.exe`; production diagnose passed; local `/health` is `3.2.61` and update check is `available=false`. Worker source has a no-store fix for `/companion-updates/*`, but deploy was blocked by the current Workers API token, so manifest cache safety is currently handled by client cache-busting plus publish-time purge.

Latest change note: at 2026-08-11 12:15 Asia/Shanghai, withdrew Pixingyun Mate `3.2.59` from the public update manifest after local in-app update testing produced a Windows `Failed to load Python DLL ... python312.dll` startup dialog. Public current version is back to `3.2.58`; local `D:\Pixingyun\pixingyun-mate.exe` was restored from the automatic update backup and `/health` returns `3.2.58`. The Doudian captcha white-screen code fix remains in source, but must be republished as a replacement version only after an actual in-app update and first-start health check pass.

Latest change note: at 2026-08-11 12:03 Asia/Shanghai, published Pixingyun Mate `3.2.59` for the Doudian manual-login captcha white-screen issue. Doudian manual login now prefers real system Chrome/Edge when the global companion browser path is Playwright Chromium, no longer overrides User-Agent, and no longer injects Canvas/WebGL/Audio stealth scripts into the human login window; Doudian headless collection keeps its existing guarded path. Also fixed the companion publish script so `pixingyun-mate-portable-*.exe` is classified as a portable update artifact rather than a setup installer. Public manifest points to `pixingyun-mate-portable-3.2.59.exe` with installer metadata for `pixingyun-mate-setup-3.2.59.exe`; production diagnose passed.

Latest change note: at 2026-08-10 16:24 Asia/Shanghai, published Pixingyun Mate `3.2.55` after custom AI editing retest found that DeepSeek could generate project-scoped read-only commands with a JianYing draft name that `capcut-cli info` would not resolve. Added AI command argument normalization so `auto` and known JianYing draft names are converted to absolute draft paths before capcut execution. Rebuilt and installed local `D:\Pixingyun` as `3.2.55`, repaired the startup VBS newline bug, updated the desktop shortcut icon to `pixingyun-mate-3.2.55.ico`, tested packaged custom AI `info` successfully, uploaded both versioned cloud packages, and refreshed no-BOM manifests. Versioned public links are correct; no-query fixed installer aliases may remain stale from Worker/Cloudflare cache, so use manifest/versioned installer URLs until the Worker no-store alias fix can be deployed.

Latest change note: at 2026-08-10 15:55 Asia/Shanghai, published Pixingyun Mate `3.2.54` after real local packaged testing of the AI clipping chain. Fixed standard mode so missing FFmpeg/ffprobe and Whisper produce warnings instead of crashing; existing JianYing draft text is reused when Whisper is unavailable. Fixed capcut enum parsing by preserving full stdout and replacing `(non-ascii)` pseudo-slugs with usable resource IDs/names, restoring the full 912 scene effects and 468 filters. Increased DeepSeek V4 timeout/token budgets and added empty-response protection. Tested both DeepSeek models using the ignored local key, tested packaged standard/custom flows, updated local `D:\Pixingyun` to `3.2.54`, uploaded both required packages (`pixingyun-mate-portable-3.2.54.exe` and `pixingyun-mate-setup-3.2.54.exe`), refreshed no-BOM manifests, replaced stable installer aliases, and purged Cloudflare cache. The Cloudflare token and DeepSeek key remain only in ignored local `.env.local` and must never be uploaded.

Latest change note: at 2026-08-10 15:20 Asia/Shanghai, published Pixingyun Mate `3.2.53` after auditing the AI editing chain. Fixed DeepSeek model selection by adding Flash/Pro options in the UI, passing `model` through standard/custom edit APIs, validating supported models server-side, and replacing the previous hardcoded `deepseek-chat`. Also fixed packaged AI editing module loading when `loguru` is unavailable and restricted capcut-cli invocation to an allowed command set. Uploaded both cloud packages: `pixingyun-mate-portable-3.2.53.exe` and `pixingyun-mate-setup-3.2.53.exe`, replaced origin stable `pixingyun-mate-setup.exe`, updated no-BOM update manifests, and updated local `D:\Pixingyun` to `3.2.53`. Public fixed URL `pixingyun-mate-setup.exe` still had an old Cloudflare cached response; website downloads use manifest `installer_url`, and `pixingyun-mate-setup-latest.exe` is public-new. Added a local Worker source fix to stop caching mutable download aliases, but Worker deploy/purge was blocked by the current invalid/insufficient Cloudflare token.

Latest change note: at 2026-08-10 14:49 Asia/Shanghai, completed and recorded Pixingyun Mate `3.2.52` two-package release. Rebuilt/uploaded `pixingyun-mate-setup-3.2.52.exe`, replaced stable `pixingyun-mate-setup.exe`, re-uploaded no-BOM `latest.json` / `latest-3.2.52.json` with installer metadata, updated local `D:\Pixingyun` to `3.2.52`, and refreshed deployment memory/markers.

Latest change note: at 2026-08-10 12:15 Asia/Shanghai, saved the Pixingyun Mate `3.2.51` incident memory and deployment retrospective. Added durable rules for WeChat Video compact period tabs, local shortcut/autostart/icon diagnosis, and the two-package companion release gate: portable in-app update exe plus full installer exe/stable installer alias.

Latest change note: at 2026-08-10 11:10 Asia/Shanghai, Pixingyun Mate `3.2.51` local hotfix fixes WeChat Video period selection for compact `日 / 周 / 月` segmented controls. The collector no longer treats the presence of `近30天` text inside a metric card as proof that the month period is selected, scopes period clicks to the target metric card, and supports aliases `日=昨日数据`, `周=近7天`, `月=近30天`.

Latest change note: at 2026-08-07 18:00 Asia/Shanghai, Pixingyun Mate `3.2.50` fixes the root cause of Douyin bot detection triggering captchas, and adds manual captcha solving. Bot detection root causes fixed: (1) Removed `--no-sandbox` (strongest automation signal — regular Chrome never runs without sandbox) and 8 other automation flags (`--disable-sync`, `--disable-component-update`, `--disable-background-networking`, etc.) from `STEALTH_LAUNCH_ARGS`; (2) Changed DOUYIN collection from headless mode to headless=False with offscreen hidden window (same as WECHAT_VIDEO) — headless mode has too many detection vectors (rendering differences, HeadlessChrome UA, CDP traces); (3) Stopped overriding User-Agent for DOUYIN — the fingerprint UA (Chrome/126-133) doesn't match the actual Playwright Chromium version, and Douyin detects this mismatch. Manual captcha solving: (4) When captcha is detected, the hidden browser window is automatically shown to the user via Windows API `ShowWindow(SW_SHOW)`; (5) User can manually complete slider/puzzle verification in the visible browser; (6) Code polls every 3 seconds for up to 5 minutes, detecting captcha resolution via `_is_captcha_page()`; (7) After captcha is solved, window is re-hidden and collection automatically continues; (8) If 5-minute timeout, account is skipped and scheduler continues to next account.

Latest change note: at 2026-08-07 16:10 Asia/Shanghai, Pixingyun Mate `3.2.48` completely eliminates the PowerShell window popup during updates. Root causes: (1) `CREATE_NO_WINDOW | DETACHED_PROCESS` is an invalid flag combination (Microsoft docs: incompatible), causing window leakage; (2) `cmd /c` always flashes a console window even with `CREATE_NO_WINDOW`; (3) `-WindowStyle Hidden` only takes effect AFTER PowerShell starts (brief flash); (4) `.exe` update path incorrectly used `/VERYSILENT /SUPPRESSMSGBOXES /NORESTART` (Inno Setup installer flags) on a PyInstaller portable exe. Fix: (1) Replaced CMD→PowerShell launcher with VBScript launcher — `wscript.exe` has no UI, VBScript `WshShell.Run …, 0, True` hides the window from the very first instruction; (2) Removed `DETACHED_PROCESS` flag, use only `CREATE_NO_WINDOW` + `STARTUPINFO(SW_HIDE)`; (3) Rewrote `.exe` update path as direct exe replacement (backup → copy → Unblock-File → restart), not installer mode; (4) Compatible with Windows 7/8/10/11 — VBScript is always available, no dependency on PowerShell execution policy.

Latest change note: at 2026-08-07 15:35 Asia/Shanghai, Pixingyun Mate `3.2.47` fixes the full Douyin scan-bind login to upload path. After tracing the entire business path from QR scan to website data upload, discovered that after the user clicks "已完成登录", the code immediately extracted `page_text` without waiting for the creator dashboard to load. If the user clicked too early (before the page redirected from QR login to dashboard), `has_dashboard` was False and initial collection was silently skipped. Fix: (1) Added Douyin login auto-detection in the waiting loop — checks for dashboard markers every 2s after 6s, auto-completing without requiring manual click; (2) After "已完成登录" is received, waits for dashboard page to load: polls for URL redirect from /login/qrcode, waits for networkidle, verifies dashboard markers, and auto-reloads if dashboard not detected; (3) This fixes the root cause of colleagues' "未检测到有效后台首页" and "初始采集未完成" errors.

Latest change note: at 2026-08-05 16:47 Asia/Shanghai, Pixingyun Mate `3.2.40` fixes Doudian browser anti-bot detection ("操作环境异常") by adding realistic Chrome/126 UA override, removing the suspicious `--disable-features=AutomationControlled` flag, preferring system Chrome over Playwright Chromium, cleaning up `cdc_` CDP variables in stealth patches, detecting Doudian security block pages, and making store name matching lenient to avoid false mismatch blocks.

Latest change note: at `2026-07-28 14:41 Asia/Shanghai`, Pixingyun Mate `3.2.8` was released with WeChat Video nickname overwrite protection. Production account `cmqho4tiauh6t8zofg4edpmh0` was corrected to `鍗㈡収楂樼淮鐮村眬`, the backend hotfix was deployed, the current desktop companion was restarted from the new package, the public download and update manifest were refreshed, and production diagnostics passed.

Latest change note: at `2026-07-19 00:29 Asia/Shanghai`, demo QA was tightened for real-data-only presentation. Public production registration is disabled by default and the production login page hides the register tab. Dashboard overview, platform stats, comparison, and account detail list now propagate the selected platform filter to backend queries. AI assistant is not part of the July 19 demo path. Verification: full backend Jest `27/27` suites and `284/284` tests passed, frontend `npm run test:ci` passed `7/7` files and `38/38` tests, frontend build passed, production diagnose passed, login screenshot `login-registration-disabled.png`, and authenticated `platform=DOUYIN` checks returned only `DOUYIN` data.

Every non-trivial repository change must be recorded here. This log is for code, scripts, documentation, cleanup, and workflow changes. Production deploys also need `docs/deployment-log.md`.

Latest change note: at 2026-08-06 15:55 Asia/Shanghai, Pixingyun Mate `3.2.44` released after full security audit. Fixed: (1) SSE session memory leak — worker crash left session stuck in `browser` status forever, now cleaned up on SSE close; (2) `companion_auth.py` `from_token` config key read `cfg.get('_from_token') or cfg.get('_from_token')` (duplicate) — fixed to `cfg.get('_from_token') or cfg.get('from_token')`; (3) token refresh interval hardcoded in `companion_auth.py` overrode `companion_state.py` values — removed hardcoded override; (4) fingerprint Chrome versions updated from 120-127 to 126-133; (5) stealth default UA updated from Chrome/125 to Chrome/131.

Latest change note: at 2026-08-06 15:08 Asia/Shanghai, Pixingyun Mate `3.2.43` fixes Douyin scan-bind login page crash (闪退). Root causes: (1) `--disable-features=AutomationControlled` in STEALTH_LAUNCH_ARGS was detected by Douyin's anti-bot system as an automation signal — removed globally; (2) login worker overrode User-Agent for Douyin with a fingerprint UA (Chrome/120-127) that didn't match the actual Playwright Chromium version — Douyin now excluded from UA override alongside WeChat Video; (3) `page.on('popup')` auto-closed ALL popups, killing Douyin login popups instantly — Douyin now excluded from popup auto-close; (4) added TargetClosedError handling with Chinese error message guiding users to restart or contact support.

Latest change note: at 2026-08-06 12:26 Asia/Shanghai, Pixingyun Mate `3.2.42` was published. The direct download subdomain `dl.ddddkiii.com` was reverted to grey cloud DNS-only after testing showed Cloudflare 525 SSL handshake failures when proxied (orange cloud). All download URLs were switched back to the main domain `https://ddddkiii.com/downloads/...` and `https://ddddkiii.com/companion-updates/latest.json`, which works correctly through Cloudflare proxy with no response body size limit (the 100MB limit only applies to POST request bodies, not GET downloads). Files were uploaded via SFTP (paramiko) at 7 MB/s, and direct download speed verified at 10.2 MB/s. The `dl.ddddkiii.com` subdomain and its Let's Encrypt certificate remain on the server but are not used for downloads.

Latest change note: at 2026-08-06 10:38 Asia/Shanghai, a direct download subdomain `dl.ddddkiii.com` (DNS only, no Cloudflare proxy) was added to bypass Cloudflare proxy overhead for large companion package downloads. A separate Let's Encrypt certificate was issued via DNS-01 challenge using a new Cloudflare API token, nginx config `/etc/nginx/conf.d/dl-download.conf` was created to serve `/downloads/` and `/companion-updates/` directly from the filesystem, and `latest.json` download URLs were updated to point to the direct subdomain. Companion source `companion_state.py` was updated to use the direct manifest URL for future versions. (Superseded by 12:26 change — download URLs reverted to main domain due to GFW blocking direct SSL to server IP.)

Latest change note: at 2026-08-05 10:38 Asia/Shanghai, the website/Pixingyun Mate readiness review was captured in `docs/project-memory.md`, and the README plus latest deployment marker now point future agents to that durable project memory before touching login, collection, deletion, installer, or update behavior.

Latest change note: at `2026-07-19 00:09 Asia/Shanghai`, demo QA was tightened for real-data-only presentation. Permission management now uses persisted team permissions and shows `鏆傛棤鐪熷疄鍥㈤槦鏁版嵁` when there are no production teams; the AI assistant edge page no longer derives fake anomaly samples from total followers and only fills from the real follower-trend API. Verification: backend Jest `27/27` suites and `280/280` tests passed, frontend `npm run test:ci` passed `7/7` files and `38/38` tests, frontend build passed, production diagnose passed, and Playwright screenshots under `artifacts/qa-20260719-production` showed no Mock/demo/example/test/fake data text.

| Time                           | Actor | Files / Area                                                                                                                           | Change                                                                                                                                                                                                                                                   | Reason                                                                                                                                                                                                 | Verification                                                                                                                                                                                     |
| ------------------------------ | ----- | -------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 2026-08-06 12:26 Asia/Shanghai | CatPaw | `desktop-companion/companion_state.py`, production `/opt/matrixflow/frontend-dist/companion-updates/latest.json`, production `/opt/matrixflow/frontend-dist/downloads/`, Cloudflare DNS | Published Pixingyun Mate `3.2.42`. Uploaded `pixingyun-mate-portable-3.2.42.zip` (81,661,452 bytes) and `pixingyun-mate-setup-3.2.42.exe` (268,182,320 bytes) to server via SFTP (paramiko, 7 MB/s). Created versioned copies plus `latest` aliases in `/opt/matrixflow/frontend-dist/downloads/`. Updated `latest.json` to use `https://ddddkiii.com/downloads/...` URLs (reverted from `dl.ddddkiii.com`). Updated `companion_state.py` `DEFAULT_UPDATE_MANIFEST_URL` back to `https://ddddkiii.com/companion-updates/latest.json`. Reverted `dl.ddddkiii.com` Cloudflare DNS record to grey cloud (DNS only). | `dl.ddddkiii.com` grey cloud direct SSL was blocked by GFW (SSL EOF). Attempting orange cloud proxy resulted in 525 SSL Handshake Failed (Cloudflare could not establish SSL with origin despite valid Let's Encrypt cert and correct nginx config). Main domain `ddddkiii.com` already works through Cloudflare proxy with no download size limit (100MB limit only applies to POST request bodies). | `latest.json` returns `200` with version `3.2.42`; ZIP download `200` (81MB); EXE download `200` (268MB); direct download speed 10.2 MB/s; SHA256 verified: ZIP `09a76aa13...`, EXE `02c2e0c26...`; all download URLs use `https://ddddkiii.com/downloads/...`. |
| 2026-08-06 10:38 Asia/Shanghai | CatPaw | `desktop-companion/companion_state.py`, production nginx `/etc/nginx/conf.d/dl-download.conf`, production `/opt/matrixflow/frontend-dist/companion-updates/latest.json`, Cloudflare DNS | Added direct download subdomain `dl.ddddkiii.com` (A record → 8.134.218.39, DNS only / grey cloud) to bypass Cloudflare proxy for large file downloads. Issued separate Let's Encrypt certificate via DNS-01 challenge (Cloudflare API token `cfut_4JZ8...`), created nginx server block serving `/downloads/` and `/companion-updates/` from filesystem with `sendfile` and `tcp_nopush`, updated `latest.json` and `latest-3.2.41.json` download URLs from `https://ddddkiii.com/downloads/...` to `https://dl.ddddkiii.com/downloads/...`, and updated companion `DEFAULT_UPDATE_MANIFEST_URL` to the direct domain for future versions. | Companion download speeds through Cloudflare proxy were extremely slow for users in China; a direct DNS path to the origin server avoids Cloudflare edge proxy overhead while keeping HTTPS via a valid Let's Encrypt certificate. | All 6 download links verified from server: (1) main site manifest `200`, (2) direct manifest `200`, (3) main site portable ZIP `200` 81645905 bytes, (4) direct portable ZIP `200` 81645905 bytes, (5) main site installer `200` 268203557 bytes, (6) direct installer `200` 268203557 bytes; Range requests return `206` with `X-Download-Source: direct-nginx`; manifest content confirms URLs point to `dl.ddddkiii.com`; SSL certificate verified via `openssl s_client` with Let's Encrypt chain. |
| 2026-08-05 16:47 Asia/Shanghai | CatPaw | `desktop-companion/stealth_patches.py`, `desktop-companion/doudian_store_collector.py`, `desktop-companion/companion_app.py`, `desktop-companion/companion_state.py`, deployment docs | Fixed Doudian browser anti-bot detection: added realistic Chrome/126 UA override in all three Doudian launch functions; removed `--disable-features=AutomationControlled` flag; added `_resolve_doudian_browser()` to prefer system Chrome/Edge over Playwright Chromium; added `cdc_` variable cleanup and `delete navigator.webdriver` to `stealth_patches.py`; added `_is_security_block_page()` detection in `open_login_window`, `check_login_state`, and `collect_store`; added stealth injection to `check_login_state`; made store name matching lenient (substring after normalizing spaces/hyphens); added friendly Chinese error messages for security block. Bumped to `3.2.40`, rebuilt PyInstaller + Inno Setup installer, published portable ZIP (81658124 bytes) and full installer (268185449 bytes), updated local `D:\Pixingyun`. | Doudian's anti-bot system detected Playwright Chromium via `HeadlessChrome` UA, `--disable-features` flag, and `cdc_` variables, showing "操作环境异常" and blocking login/collection. The strict store name exact-match also caused false mismatch blocks from page text extraction differences. | Python compile passed for all touched modules; PyInstaller build passed; Inno Setup build passed; public manifest returned `3.2.40` with ZIP SHA256 `59a43d4075cc414ae6fcaa2a260ee5a821e7e77512af4dec22979884d808dc71` and installer SHA256 `c240c2d0a0e477f39010a80340e45265271c1859d1d3a88c6af39a51c2da9319`; local `/health` returned `3.2.40`. |
| 2026-08-05 15:55 Asia/Shanghai | Codex | `desktop-companion/companion_app.py`, `desktop-companion/companion_state.py`, `backend/src/modules/doudian-browser/doudian-browser.service.ts`, `frontend/src/views/monetization/DoudianView.vue`, deployment docs | Kept the Doudian cross-store upload guard, translated local binding mismatch errors into Chinese operator guidance, returned backend `BadRequestException` for companion upload binding/name mismatches, added confirmation prompts before Doudian login and sync, bumped Pixingyun Mate to `3.2.39`, and deployed backend/frontend/companion artifacts. | Users can accidentally open a Doudian login window for one configured store and log into another shop. The system should stop wrong-shop data from syncing, but the UI should explain what happened and make dangerous clicks harder. | Companion Python compile passed; backend build passed; frontend typecheck/build passed; Doudian backend Jest passed `5/5`; public frontend ref is `assets/js/index-DD_9kZDj.js`; public manifest/download checks passed for `3.2.39`; local health is `3.2.39`; production diagnose passed. |
| 2026-08-05 15:36 Asia/Shanghai | Codex | `desktop-companion/companion_login_worker.py`, `desktop-companion/companion_state.py`, deployment docs | Added `_extract_wechat_video_identity()` for scan-bind, using Unicode-escaped `视频号ID` matching and DOM/text heuristics to extract uid, nickname, and avatar. If the scan only has a placeholder but the same uid already has a safe cloud nickname, the companion preserves the cloud nickname. Bumped Pixingyun Mate to `3.2.38`, published ZIP/installer, updated local `D:\Pixingyun`, and corrected production nickname to `晶哥来了-`. | `3.2.37` stopped `sph...` ID pollution but could still leave a placeholder when scan-bind missed the real nickname. The correct behavior is to capture `晶哥来了-` or preserve it once known. | Companion Python compile passed; PyInstaller/Inno builds passed; public ZIP/installer Range checks passed; local health is `3.2.38`; production account query returned `晶哥来了-`. |
| 2026-08-05 15:11 Asia/Shanghai | Codex | `desktop-companion/companion_login_worker.py`, `desktop-companion/companion_state.py`, `backend/src/modules/accounts/accounts.service.ts`, `backend/src/modules/platforms/platforms.service.ts`, deployment docs | Added WeChat Video nickname guards so `sph...` platform IDs are treated as suspicious nicknames in scan-bind, account create/update, and metrics reporting. Bumped Pixingyun Mate to `3.2.37`, published the ZIP/installer, updated local `D:\Pixingyun`, and deployed backend guards. | A colleague's collected WeChat Video account showed the platform UID `sph7Dz1JYr6SPc6` as the website nickname, which looked like garbled data and polluted account display. | Companion Python compile passed; targeted backend Jest passed `33/33`; backend build/deploy passed; public ZIP/installer Range checks passed; local health was `3.2.37`; production diagnose passed. |
| 2026-08-05 12:09 Asia/Shanghai | Codex | `backend/src/modules/analytics/analytics.service.ts`, `backend/test/unit/analytics.service.spec.ts`, deployment docs | `getAccountDetailList` now overlays new-source WeChat Video `periodMetrics.videoData/followerData` onto day/week/month account detail totals, while keeping old metadata snapshot fallback disabled. Added a unit test for dashboard day/week/month period values and deployed the backend. | The website dashboard period toggle was still reading only `DailyStats`, so newly uploaded companion period metrics did not affect the visible “昨日 / 近7天 / 近30天” cards. | Analytics Jest passed `31/31`; backend build/deploy passed; production account-detail API now returns different day/week totals; production diagnose passed. |
| 2026-08-05 12:17 Asia/Shanghai | Codex | `desktop-companion/companion_metrics.py`, `desktop-companion/companion_state.py`, deployment docs | Changed WeChat Video video period scraping to use card dropdown fallback for any missing API-derived period, not only when fewer than two periods were captured. Bumped Pixingyun Mate to `3.2.36`, published ZIP/installer, and updated local `D:\Pixingyun`. | The backend could now display period metrics, but the companion sometimes uploaded day/week without `month_total`, leaving 近30天 unable to differ until the next full collection. | Python compile passed; PyInstaller/Inno builds passed; public manifest/download checks passed; local health is `3.2.36`; production diagnose passed. |
| 2026-08-05 11:52 Asia/Shanghai | Codex | `desktop-companion/companion_updater.py`, `desktop-companion/companion_app.py`, `desktop-companion/companion_state.py`, deployment docs | Added HTTP Range retry/resume to companion package downloads, used manifest size as the expected byte count, verifies package completeness before SHA256, and reports incomplete downloads clearly. Bumped Pixingyun Mate to `3.2.35`, rebuilt and published the ZIP plus installer, and manually updated local `D:\Pixingyun` to `3.2.35`. | The local `3.2.33` updater downloaded only about 31.9 MB of the 59.7 MB ZIP, then failed with `update package sha256 mismatch`; the team needs a more robust updater path for shared studio use. | Python compile passed; PyInstaller/Inno builds passed; ZIP/installer hashes, sizes, and magic checked locally and over public Range requests; production diagnose passed; local `/health` is `3.2.35`; local update check returned `available=false`. |
| 2026-08-05 11:26 Asia/Shanghai | Codex | `desktop-companion/companion_metrics.py`, `desktop-companion/companion_state.py`, `backend/src/modules/platforms/platforms.service.ts`, `backend/test/unit/platforms.service.spec.ts`, deployment docs | Added WeChat Video home-card avatar extraction with existing safe-avatar guards, scoped period dropdown handling to the intended metric card, added `关注者数据` period collection for `昨日数据` / `近7天` / `近30天`, kept `视频数据` period collection on the `关键指标` card, and saved backend `periodMetrics.followerData` without overwriting `videoData`. Bumped Pixingyun Mate to `3.2.34`, deployed backend, and published the ZIP plus installer. | Fix missing WeChat Video avatars and the missing extra-click data-center paths for follower/video 7-day and 30-day period metrics without affecting other collection modules. | Python compile passed; backend `platforms.service` Jest passed `4/4`; backend build/deploy passed; PyInstaller/Inno builds passed; public manifest is `3.2.34`; public ZIP/installer range checks returned correct magic and sizes; production diagnose passed. |
| 2026-08-05 10:38 Asia/Shanghai | Codex | `docs/project-memory.md`, `README.md`, `LATEST_DEPLOYMENT.md`, `docs/project-change-log.md` | Added durable project memory for the 2026-08-05 website and Pixingyun Mate readiness review, including Feishu login lessons, account binding/deletion safeguards, nickname/avatar capture guards, Win10/Win11 installer/WebView2 guidance, app-update progress behavior, public package links, local shortcut-path lesson, and future operating rules. Linked the memory from README and latest deployment truth, and refreshed the current Pixingyun Mate release marker to `3.2.33`. | Preserve the long troubleshooting session as durable context so future work does not repeat solved issues or rely on stale companion version/path assumptions. | Documentation-only change; `git diff --check` run on touched docs after editing. |
| 2026-07-30 12:05 Asia/Shanghai | Codex | `frontend/src/views/monetization/DoudianView.vue`, `backend/src/modules/scheduler/order-report.scheduler.ts` | Unified Doudian source ranking with the same effective-order set used by the top-level order KPI, and reused the shared Doudian revenue-order helper in daily report source summaries. | A Doudian source card could show one fewer order than the top-level effective count when an order was closed but not successfully refunded yet; operators expect it to count as valid until refund success. | `npm test -- src/__tests__/doudianStoreMetrics.test.ts` in `frontend` passed `3/3`; `npm run build` in `backend` passed; `npm run build` in `frontend` passed; `py -3 scripts\diagnose-production.py --remote --require-worker-route` passed before deployment. |
| 2026-07-30 11:00 Asia/Shanghai | Codex | `frontend/src/views/login/LoginView.vue`, `desktop-companion/companion_app.py`, `desktop-companion/companion_clean_ui.py`, `desktop-companion/companion_state.py`, `desktop-companion/update-release`, deployment docs | Added Feishu login to Pixingyun Mate. The companion creates a one-time local login session, opens the website Feishu OAuth entry, exposes a validated localhost completion callback, saves returned access/refresh tokens, and updates the UI through polling. The website login callback now recognizes companion redirects and posts the Feishu login session back only to the allowed local callback. Bumped version to `3.2.19`, rebuilt and uploaded the public companion package and manifest; deployed frontend bundle `assets/js/index-C2Ibt1gL.js`. | Website Feishu login already worked, but local companion needed a supported SSO path without requiring a separate Feishu redirect URI registered to localhost. | `py -3 -m py_compile desktop-companion\companion_app.py desktop-companion\companion_clean_ui.py desktop-companion\companion_state.py`; `npm --prefix frontend run typecheck`; `npm --prefix frontend run build`; PyInstaller build; remote ZIP SHA256 `63e98fe86e817ee0aedfc4b3e81238f811424fc69ef3d0da5838d675f3da15e8`; public manifest `3.2.19`; public ZIP range `206 application/zip`; `py -3 scripts\diagnose-production.py --remote --require-worker-route` passed. |
| 2026-07-30 10:47 Asia/Shanghai | Codex | `desktop-companion/companion_auth.py`, `desktop-companion/companion_app.py`, `desktop-companion/companion_clean_ui.py`, `desktop-companion/companion_state.py`, `desktop-companion/update-release`, deployment docs | Changed Pixingyun Mate login to accept either email or phone number by sending `identifier + password` to the cloud login API. Added `saved_identifier` while preserving legacy `saved_email` fallback, bumped version to `3.2.18`, rebuilt and uploaded the public companion package and manifest. | Website login already supports phone numbers; the desktop companion needed the same login identifier behavior for phone-only users. | `py -3 -m py_compile desktop-companion\companion_auth.py desktop-companion\companion_app.py desktop-companion\companion_clean_ui.py`; PyInstaller build; remote ZIP SHA256 `59314b0b9dbf3cc06aae341e5972abe9b2fd9f1376511b799f5fafab185e5737`; public manifest `3.2.18`; `py -3 scripts\diagnose-production.py --remote --require-worker-route` passed. |
| 2026-07-29 12:30 Asia/Shanghai | Codex | `cloudflare/matrixflow-origin-proxy/src/worker.js`, deployment docs | Added conservative Cloudflare Worker caching for production static assets. Only `GET/HEAD /assets/*` uses `cacheEverything` with a 30-day TTL and strips cookie/authorization before origin fetches; `/api/*`, `/ws/*`, and HTML remain dynamic/no-store or revalidated. Deployed Worker version `e73783a2-a32b-41bb-9c07-eb9fb3b0d6ed`. | Improve repeat page loads and large frontend asset delivery without changing the currently stable Cloudflare Tunnel protocol or caching authenticated business data. | Backed up Worker source to `C:\Users\EDY\jujuju\backups\worker-cache-20260729-122830`; `node --check cloudflare\matrixflow-origin-proxy\src\worker.js`; `py -3 scripts\deploy-cloudflare-worker.py --execute`; public header checks confirmed static assets use `static-assets-30d`, API uses `no-store`, and HTML uses `no-cache, must-revalidate`. |
| 2026-07-29 11:39 Asia/Shanghai | Codex | `frontend/src/views/admin/AdminView.vue`, `frontend/src/api/admin.ts`, `backend/src/modules/admin/admin.controller.ts`, `backend/src/modules/admin/admin.service.ts`, `backend/prisma/migrations/202607290001_allow_user_email_nullable/migration.sql`, deployment docs | Changed super-admin organization user creation so email is optional and either email or phone can be used as the login identifier. Added backend validation for phone-only users and a migration to make `User.email` nullable in existing PostgreSQL databases. Deployed backend and frontend as `assets/js/index-CsbMzH2u.js` with admin chunk `AdminView-BcEjdaU9.js`. | Login already supports phone numbers, so creating a user should not require an email address when a valid phone number is provided. | `npm run typecheck` in `frontend`; `npm run build` in `backend`; `DATABASE_URL=postgresql://user:pass@localhost:5432/matrixflow npx prisma validate` in `backend`; `py -3 scripts\deploy-backend-safe.py --execute --migrate`; `py -3 scripts\deploy-frontend-fast.py --allow-dirty-source`; `py -3 scripts\diagnose-production.py --remote --require-worker-route` passed. |
| 2026-07-29 11:26 Asia/Shanghai | Codex | `frontend/src/views/monetization/DoudianView.vue`, `frontend/src/views/monetization/DoudianSourceDetailView.vue`, `frontend/src/router/index.ts`, deployment docs | Added a hidden Doudian source-detail route and made Doudian source cards navigate to it. The detail page supports `近7天` and `近30天`, loads Doudian orders/aftersales for the selected range, and displays source KPIs, trend chart, and per-order details. Deployed the frontend as `assets/js/index-D_cj3Lxh.js` with `DoudianView-Dfc0rV7L.js` and `DoudianSourceDetailView-dQGbw9Ew.js`. | Match the WeChat Store source-detail UX for Doudian so operators can inspect each source account without inline complexity. | `npm run typecheck --workspace=frontend`; `npm run test:ci --workspace=frontend` `7/7` files and `38/38` tests; `py -3 scripts\deploy-frontend-fast.py --allow-dirty-source`; public ref matched `assets/js/index-D_cj3Lxh.js`; `py -3 scripts\diagnose-production.py --remote --require-worker-route` passed. |
| 2026-07-29 11:20 Asia/Shanghai | Codex | `frontend/src/views/monetization/WechatSourceDetailView.vue`, deployment docs | Added a range switch to the WeChat Store source-detail page so operators can browse either `近7天` or `近30天`. The selected range controls data loading, KPI labels, trend chart buckets, and empty-state text. Deployed the frontend as `assets/js/index-CQGUjqNB.js` with `WechatSourceDetailView-DEN5wv-i.js`. | Operators needed a 30-day source-sales view in addition to the initial 7-day detail page. | `npm run typecheck --workspace=frontend`; `npm run test:ci --workspace=frontend` `7/7` files and `38/38` tests; `py -3 scripts\deploy-frontend-fast.py --allow-dirty-source`; public ref matched `assets/js/index-CQGUjqNB.js`; `py -3 scripts\diagnose-production.py --remote --require-worker-route` passed. |
| 2026-07-29 11:15 Asia/Shanghai | Codex | `frontend/src/views/monetization/MonetizationView.vue`, `frontend/src/views/monetization/WechatSourceDetailView.vue`, `frontend/src/router/index.ts`, deployment docs | Made WeChat Store source cards navigate to a hidden source-detail route. Added a standalone detail page that queries the selected source over the last 7 days, displays KPIs, a sales/order trend chart, and shipped order details. Deployed the frontend as `assets/js/index-7ycf8JUe.js` with `MonetizationView-DiQHfhi8.js` and `WechatSourceDetailView-B7M1J3Pf.js`. | Keep the source cards simple and avoid complex inline expanded state while still letting operators inspect each source's recent sales. | `npm run typecheck --workspace=frontend`; `npm run test:ci --workspace=frontend` `7/7` files and `38/38` tests; `py -3 scripts\deploy-frontend-fast.py --allow-dirty-source`; public ref matched `assets/js/index-7ycf8JUe.js`; `py -3 scripts\diagnose-production.py --remote --require-worker-route` passed. |
| 2026-07-28 19:32 Asia/Shanghai | Codex | `frontend/src/views/monetization/MonetizationView.vue`, deployment docs | Added a separate source-attribution aftersale list for refund annotations. The source section now keeps selected-day shipped orders as the denominator, but marks refunds from the selected order cohort through the current time. Deployed the frontend as `assets/js/index-BeUIq1Dm.js` with `MonetizationView-Dti0aaP4.js`. | The official order export showed a 2026-07-27 `心理学博士卢慧` order refunded at 2026-07-28 01:25; using refund-created-date only made the source card show 1 refund instead of the current 2 refunded orders for that order cohort. | `npm run typecheck --workspace=frontend`; `npm run test:ci --workspace=frontend` `7/7` files and `38/38` tests; frontend build/deploy; public chunk marker check for the second aftersale range request and `Math.max(end, Date.now())`; production DB recomputation for 2026-07-27 `唐商披星` returned `心理学博士卢慧` 18 orders / 2 refunds and store-origin `-` 6 orders / 1 refund; `py -3 scripts\diagnose-production.py --remote --require-worker-route` passed. |
| 2026-07-28 18:20 Asia/Shanghai | Codex | `frontend/src/views/monetization/MonetizationView.vue`, deployment docs | Changed the WeChat Store source card fallback display so store-origin orders show `店铺出单` as the card name and retain `官网来源：-` in metadata. Deployed the frontend as `assets/js/index-DPjOC0Z3.js` with `MonetizationView-Bi_34HUR.js`. | A bare `-` matches the official export but is unclear to operators; the UI should state that these are shop-origin orders without changing the underlying totals. | `npm run typecheck --workspace=frontend`; `npm run test:ci --workspace=frontend` `7/7` files and `38/38` tests; frontend build/deploy; public chunk marker check for `店铺出单`, `sale_channel:"store"`, `ship_time`, `refundCount`, and no status fallback; production DB recomputation for 2026-07-27 `唐商披星` returned `心理学博士卢慧` 18 orders / 1 refund and store-origin `-` 6 orders / 1 refund; `py -3 scripts\diagnose-production.py --remote --require-worker-route` passed. |
| 2026-07-28 17:48 Asia/Shanghai | Codex | `frontend/src/views/monetization/MonetizationView.vue`, deployment docs | Changed WeChat Store source attribution to strict shipped-order counting using `ship_time > 0` and removed the previous terminal-status fallback. Deployed the frontend as `assets/js/index-DEPhP1U6.js` with `MonetizationView-D4eQyJjF.js`. | The official `唐商披星` check showed `心理学博士卢慧` has 18 shipped source orders on 2026-07-27; a cancelled/no-ship row with status `250` had incorrectly lifted it to 19. | `npm run typecheck --workspace=frontend`; `npm run test:ci --workspace=frontend` `7/7` files and `38/38` tests; frontend build/deploy; public chunk marker check; production DB recomputation for 2026-07-27 `唐商披星` returned `心理学博士卢慧` 18 orders / 1 refund and `-` 6 orders / 1 refund; `py -3 scripts\diagnose-production.py --remote --require-worker-route` passed. |
| 2026-07-28 17:37 Asia/Shanghai | Codex | `frontend/src/views/monetization/MonetizationView.vue`, deployment docs | Preserved the official `-` source label for store-origin WeChat Store orders, added the `店铺出单` label to explain it, and changed source ranking sort to order-count first. Deployed the frontend as `assets/js/index-BQJaPROU.js` with `MonetizationView-BmxOX7Rf.js`. | Match the official export screenshot where `-` means store-origin orders, and make the source section answer "who/where produced orders" before revenue ranking. | `npm run typecheck --workspace=frontend`; `npm run test:ci --workspace=frontend` `7/7` files and `38/38` tests; frontend build/deploy; public chunk marker check for the `-` fallback, `店铺出单`, `refundCount`, `source_infos`; production DB recomputation for 2026-07-27 `唐商披星` returned 44 total orders, `心理学博士卢慧` 19 orders / 1 refund, and `-` 6 orders / 1 refund; `py -3 scripts\diagnose-production.py --remote --require-worker-route` passed. |
| 2026-07-28 17:25 Asia/Shanghai | Codex | `frontend/src/views/monetization/MonetizationView.vue`, deployment docs | Changed WeChat Store source attribution so the selected 24-hour/source section counts every shipped-result order (`ship_time`, or status `30/100/200/250`), groups orders without account/daren source as `店铺出单`, and leaves refund count/amount as a separate successful-aftersale annotation. Deployed the frontend as `assets/js/index-4C-ZHZ69.js` with `MonetizationView-trJxx-_h.js`. | Match the user's corrected source-reporting goal: account/daren orders and pure shop orders both appear, while the section does not reuse top-level sales/refund KPI rules. | `npm run typecheck --workspace=frontend`; `npm run test:ci --workspace=frontend` `7/7` files and `38/38` tests; frontend build/deploy; public chunk marker check for `店铺出单`, `ship_time`, status `250`, `refundCount`, `source_infos`; production recomputation returned `心理学博士卢慧` 19 orders / 1 refund / `29900` cents and included the store-origin group; `py -3 scripts\diagnose-production.py --remote --require-worker-route` passed. |
| 2026-07-28 16:59 Asia/Shanghai | Codex | `frontend/src/views/monetization/MonetizationView.vue`, deployment docs | Changed WeChat Store source attribution so source account order counts use all selected-range orders with `source_infos`; refund count and refund amount remain separately mapped from selected-range successful aftersales. Deployed the frontend as `assets/js/index-DifOXBVi.js` with `MonetizationView-DKyhJVjb.js`. | Match the WeChat official export/account-source review口径: `心理学博士卢慧` on 2026-07-27 should show 19 source orders and 1 refunded order, without mixing this source view with top-level sales/refund KPI calculations. | `npm run typecheck --workspace=frontend`; `npm run test:ci --workspace=frontend` `7/7` files and `38/38` tests; frontend build/deploy; public chunk marker check; production API recomputation returned `心理学博士卢慧` 19 orders / 1 refund / `29900` cents; `py -3 scripts\diagnose-production.py --remote --require-worker-route` passed. |
| 2026-07-28 16:41 Asia/Shanghai | Codex | `frontend/src/views/monetization/MonetizationView.vue`, deployment docs | Changed WeChat Store source attribution to use closed-loop orders: shipped orders, completed orders, and successfully refunded orders. Added refund count and refund amount per source account. Deployed the frontend as `assets/js/index-es3XKHxL.js` with `MonetizationView-BCQ5yIbc.js`. | Match the operator's Excel-style review goal: see which accounts produced orders and which accounts produced refund cases, without counting unclosed pending orders as source performance. | `npm run typecheck --workspace=frontend`; `npm run test:ci --workspace=frontend` `7/7` files and `38/38` tests; frontend build; public chunk marker check for `refundCount`, `refundAmount`, `source-row`, and `source_infos`; `py -3 scripts\diagnose-production.py --remote --require-worker-route` passed. |
| 2026-07-28 16:27 Asia/Shanghai | Codex | `frontend/src/views/monetization/MonetizationView.vue`, deployment docs | Changed WeChat Store source-account ranking to use only shipped/completed orders (`status === 30 || status === 100`) while keeping per-order source labels visible. Deployed the frontend as `assets/js/index-BDWH0qLf.js` with `MonetizationView-C6xcNRq6.js`. | Keep unshipped orders out of source attribution totals so the ranking reflects confirmed fulfillment rather than pending orders. | `npm run typecheck --workspace=frontend`; `npm run test:ci --workspace=frontend` `7/7` files and `38/38` tests; frontend build; public chunk marker check for `status===30`, `status===100`, `source-row`, and `source_infos`; `py -3 scripts\diagnose-production.py --remote --require-worker-route` passed. |
| 2026-07-28 16:17 Asia/Shanghai | Codex | `backend/src/modules/wechat-store/wechat-store.service.ts`, `frontend/src/api/wechat-store.ts`, `frontend/src/views/monetization/MonetizationView.vue`, `backend/test/unit/wechat-store.service.spec.ts`, `scripts/deploy-backend-safe.py`, deployment docs | Extracted sanitized WeChat order `source_infos` from cached raw order detail into the backend order-list response; added frontend source types, source-account ranking, per-order source labels, and source-aware order search; added unit coverage; fixed backend deploy script stdout encoding on Windows. Deployed backend and frontend as `assets/js/index-C0WcPUjV.js` with `MonetizationView-CLCwbsK8.js`. | Let operators see which video account or influencer source produced WeChat Store orders, matching the WeChat Store console's "成交来源" visibility. | Backend build passed; backend WeChat Store service test passed `5/5`; frontend test passed `7/7` files and `38/38` tests; frontend build/typecheck passed; production API sample returned `api_orders=1986` and `api_orders_with_source_infos=1701`; public chunk marker check; `py -3 scripts\diagnose-production.py --remote --require-worker-route` passed. |
| 2026-07-28 15:42 Asia/Shanghai | Codex | `frontend/src/views/dashboard/MatrixDashboard.vue`, deployment docs | Added clickable KPI/stat cards with a read-only account contribution drawer, added keyboard focus support, and made multi-account table rows/account names navigate to existing account detail pages. Deployed the frontend as `assets/js/index-CSTf3aEf.js` with `MatrixDashboard-D_-7Tati.js`. | Let operators drill into aggregate matrix metrics and inspect account-level details instead of only viewing static summary numbers. | `npm run typecheck --workspace=frontend`; `npm run test:ci --workspace=frontend` `7/7` files and `38/38` tests; frontend deploy typecheck/build; public chunk marker check; `py -3 scripts\diagnose-production.py --remote --require-worker-route` passed. |
| 2026-07-28 15:32 Asia/Shanghai | Codex | `frontend/src/views/monetization/MonetizationView.vue`, deployment docs | Added a dedicated WeChat Store list loading state and stopped clearing current store data at the start of manual refresh. Deployed the frontend as `assets/js/index-UvIYTfUW.js` with `MonetizationView-CX7bcGpg.js`. | Prevent the WeChat Store page from briefly showing the wrong empty-store state during page refresh and keep visible data stable during refresh. | `npm run typecheck --workspace=frontend`; `npm run test:ci --workspace=frontend` `7/7` files and `38/38` tests; frontend deploy typecheck/build; public chunk marker check; companion ZIP range check; `py -3 scripts\diagnose-production.py --remote --require-worker-route` passed. |
| 2026-07-28 15:09 Asia/Shanghai | Codex | `scripts/deploy-frontend-fast.py`, deployment docs | Changed the official frontend deploy script to preserve production `downloads/` and `companion-updates/` folders while replacing the frontend build output. Recorded the frontend/backend deployment and static download repair. | A frontend deploy for the WeChat Store add flow replaced `/opt/matrixflow/frontend-dist` and temporarily removed the Pixingyun Mate public ZIP/manifest until they were restored. | `py -3 -m py_compile scripts\deploy-frontend-fast.py`; restored production ZIP SHA256 `67ad12b5f4e6d98590e0bba3463f17a9676e0e830286de3a2d4e4af01609eeef`; `py -3 scripts\diagnose-production.py --remote --require-worker-route` passed. |
| 2026-07-28 14:58 Asia/Shanghai | Codex | `frontend/src/views/monetization/MonetizationView.vue`, `backend/src/modules/wechat-store/wechat-store.service.ts`, `backend/test/unit/wechat-store.service.spec.ts` | Added a manual WeChat Store binding entry on the website with store name, AppID, and AppSecret fields; selecting the newly created store now reloads data. Added backend validation for required credentials and duplicate AppID handling. | Let operators add WeChat Stores themselves instead of relying on a developer to seed stores manually. | `npm run build --workspace=backend`; `npm run typecheck --workspace=frontend`; `npm run test --workspace=backend -- wechat-store.service.spec.ts --runInBand`; local `/monetization` returned HTTP 200. |
| 2026-07-28 14:41 Asia/Shanghai | Codex | `desktop-companion/companion_metrics.py`, `desktop-companion/companion_login_worker.py`, `desktop-companion/companion_collector.py`, `desktop-companion/local_db.py`, `desktop-companion/companion_state.py`, `backend/src/modules/platforms/platforms.service.ts`, `desktop-companion/update-release`, deployment docs, production DB/backend | Added legal-entity nickname rejection for WeChat Video scraping, scan-bind, local nickname writes, remote-name sync, and backend `reportMetrics`; corrected production account `cmqho4tiauh6t8zofg4edpmh0` from `娣卞湷甯傛姭鏄熸暀鑲叉枃鍖栨湁闄愬叕鍙竊 to `鍗㈡収楂樼淮鐮村眬`; hot-deployed compiled backend `platforms.service.js`; bumped Pixingyun Mate to `3.2.8`, rebuilt/restarted the local companion, and uploaded the new public ZIP/manifest. | A WeChat Video account collected from another PC showed the certified company subject as the account name instead of the actual account display name `鍗㈡収楂樼淮鐮村眬`. | `py -3 -m py_compile` for touched companion modules; `npm run build --workspace=backend`; local `/health` version `3.2.8`; remote DB account query returned nickname `鍗㈡収楂樼淮鐮村眬`; public ZIP SHA256 `67ad12b5f4e6d98590e0bba3463f17a9676e0e830286de3a2d4e4af01609eeef`; full public ZIP download hash matched manifest; `py -3 scripts\diagnose-production.py --remote --require-worker-route`. |
| 2026-07-28 11:29 Asia/Shanghai | Codex | `desktop-companion/doudian_store_collector.py`, `desktop-companion/companion_app.py`, `desktop-companion/companion_clean_ui.py`, `desktop-companion/companion_state.py`, `desktop-companion/update-release`, deployment docs | Added Doudian browser login-state detection, persisted store status fields, exposed `?probe=1` on the local Doudian store API, rendered `宸茬櫥褰昤/`鐧诲綍澶辨晥`/`鏈‘璁 badges in Doudian management, made refresh perform a status check, bumped Pixingyun Mate to `3.2.7`, rebuilt/restarted the local companion, and uploaded the new public ZIP/manifest. | The Doudian management page did not show whether a store profile was still logged in, so a dropped Doudian account could be missed until sync failed later. | `py -3 -m py_compile` for touched companion modules; PyInstaller build; local `/health` version `3.2.7`; local `/api/doudian/stores?probe=1` showed both stores `online`; Browser check confirmed status badges; public ZIP SHA256 `95fca2994881932087b20b516b16472baea966d0bb5a13805d4d0a4268d82800`; `py -3 scripts\diagnose-production.py --remote --require-worker-route`. |
| 2026-07-28 10:49 Asia/Shanghai | Codex | `desktop-companion/companion_metrics.py`, `desktop-companion/local_db.py`, `desktop-companion/companion_collector.py`, `desktop-companion/companion_state.py`, `desktop-companion/update-release`, deployment docs | Removed the fragile mojibaked title regex from the Douyin helper parser, mapped Douyin `posts_count` to local `video_count`, added a saved-content-count fallback when account metrics expose `0`, bumped Pixingyun Mate to `3.2.6`, rebuilt/restarted the local companion, and uploaded the new public ZIP/manifest. | Douyin collection itself succeeded, but helper parsing could throw a JS regex syntax error and older runs left account-list video counts at `0`, making the UI look as if no data was collected. | Douyin-only run succeeded for 4/4 accounts with 1682 reported posts; `py -3 -m py_compile` for touched companion modules; PyInstaller build; local `/health` version `3.2.6`; local `/api/local-accounts` showed Douyin counts `637`, `1470`, `701`, `680`; public ZIP SHA256 `91301fd7ee864da17dfe8e83257e743e0f6fc5ef385891d4bfa48524fb48e6af`; `py -3 scripts\diagnose-production.py --remote --require-worker-route`. |
| 2026-07-27 18:55 Asia/Shanghai | Codex | `desktop-companion/companion_clean_ui.py`, `desktop-companion/companion_app.py`, `desktop-companion/companion_collector.py`, `desktop-companion/update-release`, deployment docs | Added dedicated WeChat Video and Douyin collection buttons to the real clean companion UI, added backend platform filtering for collection triggers, made account-list auto-refresh silent, fixed toolbar button widths, rebuilt the portable package, and uploaded the new public ZIP/manifest. | Make account collection controls discoverable and remove visual flicker caused by background refresh toggling the toolbar's refresh button state. | Browser sampling confirmed stable toolbar widths across refreshes; `py -3 -m py_compile companion_clean_ui.py companion_app.py companion_collector.py`; PyInstaller build; public manifest/download SHA256 `edd503303ad8ece6497b143d2ba423226f5005792a71e6a9a711293007b38ea9`; public ZIP `206 application/zip`, `bytes 0-15/81635390`, magic `504b0304`; `py -3 scripts\diagnose-production.py --remote --require-worker-route`. |
| 2026-07-27 18:30 Asia/Shanghai | Codex | `desktop-companion/webview_window.py`, `desktop-companion/companion_state.py`, `desktop-companion/update-release`, deployment docs | Caught pywebview import/initialization exceptions and allowed the companion to fall back to the system browser, bumped the companion to `3.2.5`, rebuilt the portable package, and uploaded the new public update manifest. | Prevent startup crashes like `Failed to resolve Python.Runtime.Loader.Initialize` on users' PCs while keeping the local Flask companion service usable. | `py -3 -m py_compile companion_state.py webview_window.py companion_app.py`; PyInstaller build; public manifest `3.2.5`; public ZIP `206 application/zip` with SHA256 `011c353169f1d154ce09152d600a57b8e0988add6819ef6dc3a7124d5af276bc`; `py -3 scripts\diagnose-production.py --remote --require-worker-route`. |
| 2026-07-27 17:52 Asia/Shanghai | Codex | `desktop-companion/companion_state.py`, `desktop-companion/update-release`, `LATEST_DEPLOYMENT.md`, `docs/deployment-log.md` | Bumped Pixingyun Mate to `3.2.4`, rebuilt the packaged app into `desktop-companion/release-dist`, created `pixingyun-mate-portable-3.2.4.zip`, uploaded it to the public companion download path, and added a real JSON update manifest for auto-update checks. | Let studio users update to the latest companion from the website without overwriting local saved config, local DB files, browser profiles, or login state. | `py -3 -m py_compile companion_state.py companion_app.py companion_updater.py companion_clean_ui.py`; PyInstaller build; public manifest `200 application/json`; public ZIP `206 application/zip` with SHA256 `f5e8b8ad9383808678a80141571f5efab2f4781d28e9e758510ca24a95d09ff1`; `py -3 scripts\diagnose-production.py --remote --require-worker-route`. |
| 2026-07-27 16:40 Asia/Shanghai | Codex | `backend/src/modules/auth/auth.service.ts`, `desktop-companion/companion_app.py`, `.gitignore`, desktop companion source modules | Closed the backend public registration endpoint, expanded companion `/health` with local diagnostics, and fixed the desktop companion ignore rule so Python source modules such as `companion_updater.py` are visible to version control instead of hidden by the parent directory ignore. | Keep the project focused on whitelist-operated accounts and make the desktop companion easier to diagnose, update, and carry across machines without missing source modules. | `py -3 -m py_compile companion_app.py companion_updater.py companion_config.py companion_state.py local_db.py`; `npm run build` in `backend`. No production deployment executed. |
| 2026-07-22 14:02 Asia/Shanghai | Codex | `scripts/diagnose-production.py`, `LATEST_DEPLOYMENT.md`, `docs/deployment-log.md`, public companion package | Rebuilt the companion portable ZIP from the current `desktop-companion/dist/pixingyun-mate` release excluding logs, uploaded it to the production downloads path, and changed diagnostics to validate `PK\\x03\\x04` ZIP magic via a ranged GET. | The old check allowed `/downloads/pixingyun-mate-portable.zip` to pass with `200` even when Nginx returned the frontend HTML fallback, meaning the download feature was not actually working. | Public range GET returned `206`, `application/zip`, `bytes 0-15/81592552`, first bytes `504b0304`, Worker header `cloudflare-worker`; remote SHA256 `0abe8f8caf573be21f436237d7241202165d82b6962f3c5497327251bf823fa7`; `py -3 -m py_compile scripts\diagnose-production.py scripts\deploy-cloudflare-worker.py`; `py -3 scripts\diagnose-production.py --remote --require-worker-route`; Chrome smoke desktop/mobile `/` and `/login` passed. |
| 2026-07-22 12:45 Asia/Shanghai | Codex | `scripts/diagnose-production.py`, `scripts/deploy-cloudflare-worker.py`, `.env.example`, deployment docs | Added public Worker marker-header detection with optional blocking mode, added production data sanity SQL checks for core counts and obvious anomalies, introduced a standard Worker deploy/verify script, and renamed the example Cloudflare env section away from Pages-only wording. | Make the inactive Cloudflare Worker route and production data anomalies machine-detectable instead of relying on manual memory or one-off commands. | `py -3 -m py_compile scripts\diagnose-production.py scripts\deploy-cloudflare-worker.py`; `py -3 scripts\diagnose-production.py --remote`; `py -3 scripts\diagnose-production.py --require-worker-route` failed as expected; `py -3 scripts\deploy-cloudflare-worker.py`; `py -3 scripts\deploy-cloudflare-worker.py --verify-only` failed as expected; `py -3 scripts\deploy-cloudflare-worker.py --execute --skip-dry-run` failed with Cloudflare invalid token as expected. |
| 2026-07-22 12:49 Asia/Shanghai | Codex | `scripts/diagnose-production.py`, deployment docs | Added a read-only production API/DB consistency gate for analytics overview, accounts list, and Doudian stores. The check generates a short-lived in-memory JWT on the server and compares API output to DB aggregates without printing credentials. | Catch real-data regressions where pages load but dashboard/account/Doudian numbers diverge from the database or leak internal Doudian profile paths. | `py -3 -m py_compile scripts\diagnose-production.py`; `py -3 scripts\diagnose-production.py --remote`; API/DB values matched for account totals, active accounts, followers, likes, posts, published posts, account-list total, and Doudian store count. |
| 2026-07-22 12:35 Asia/Shanghai | Codex | `backend/package.json`, `scripts/diagnose-production.py`, `docker-compose.yml`, `docs/cloudflare-tunnel-runbook.md`, `scripts/install-tunnel-guard.py`, deployment docs, production ECS | Added backend `test`/`test:ci` scripts, made production diagnostics UTF-8 safe on Windows, fixed production Docker DB/Redis volume and port-binding declarations, removed shell-form tunnel healthchecks from recovery paths, repaired production DB/Redis/tunnel state, and documented the Cloudflare Worker route gap. | Make the project maintainable through standard commands, keep production data from moving to empty Docker volumes, close internal DB/Redis port exposure at the container layer, and avoid false unhealthy tunnel status. | `npm run test:ci --workspace=frontend`; `npm test --workspace=backend -- --runInBand`; `npm run build:backend`; `npm run build:frontend`; `py -3 scripts\diagnose-production.py --remote`; live Playwright smoke for `/` and `/login`; `npx wrangler deploy --dry-run --outdir dist-dry-run`; Cloudflare Observability showed `0` `matrixflow-origin-proxy` events. |
| 2026-07-19 00:51 Asia/Shanghai | Codex | `backend/src/modules/mcp/mcp.controller.ts`, `backend/test/unit/mcp-controller.spec.ts`, `frontend/src/views/mcp/MCPConnectionView.vue`, `frontend/src/router/index.ts`, deployment docs, production MCP keys | Added admin-role requirements to MCP key/connection management, fixed `createdBy` to use the JWT user `id`, masked MCP tokens in key rows and config previews while preserving copy actions, restricted the MCP route to admin roles, deployed backend/frontend, and rotated exposed production MCP keys. | Prevent demo screenshots/recordings from leaking MCP bearer tokens and prevent ordinary members from viewing or managing MCP keys. | MCP controller unit test `2/2`; full backend Jest `27/27` suites and `285/285` tests; backend build; frontend `npm run test:ci` `7/7` files and `38/38` tests; frontend build; backend/front deploys; rotated `1` DB MCP key and `1` env MCP key; production MCP page `leakedTokenCount=0`; `MEMBER` `GET /api/v1/mcp/keys` returned `403`; `py -3 scripts\diagnose-production.py --remote`. |
| 2026-07-19 00:29 Asia/Shanghai | Codex | `backend/src/modules/auth/auth.service.ts`, `backend/test/unit/auth.service.spec.ts`, `frontend/src/views/login/LoginView.vue`, `.env.example`, `backend/.env.template`, `backend/src/modules/analytics/analytics.controller.ts`, `backend/src/modules/analytics/analytics.service.ts`, `frontend/src/api/analytics.ts`, `frontend/src/composables/useMatrixDashboard.ts`, `backend/test/unit/analytics.service.spec.ts`, deployment docs | Disabled public registration by default in production unless explicitly enabled, hid/guarded the production register tab, added dashboard platform filter propagation across backend and frontend analytics calls, and updated deployment/change records with the latest production bundle. | Prevent unauthorized users from creating accounts before the demo and ensure filtered dashboard views show only real data from the selected platform instead of mixed-platform aggregates. | Backend auth unit tests `24/24`; backend analytics unit tests `30/30`; full backend Jest `27/27` suites and `284/284` tests; backend build; frontend `npm run test:ci` `7/7` files and `38/38` tests; frontend build; `py -3 scripts\diagnose-production.py --remote`; public register `403`; production frontend `assets/js/index-DT8VBwB8.js`; authenticated `platform=DOUYIN` API checks. |
| 2026-07-19 00:09 Asia/Shanghai | Codex | `backend/src/modules/teams/permissions.controller.ts`, `backend/test/unit/permissions.controller.spec.ts`, `frontend/src/views/team/PermissionView.vue`, `frontend/src/views/ai/AIAssistantView.vue`, deployment docs | Replaced static permission echo behavior with persisted team permission reads/writes and frontend-compatible permission IDs; made the permission page show a real empty state when there is no team; changed AI anomaly auto-fill to use real follower trend data only and relabeled unavailable AI as not connected. | Avoid showing old/default/fabricated data during the July 19 demo and prevent permission edits from appearing successful without persistence. | Backend Jest `27/27` suites and `280/280` tests; frontend `npm run test:ci` `7/7` files and `38/38` tests; frontend build; production frontend deploy; `py -3 scripts\diagnose-production.py --remote`; authenticated Playwright screenshots under `artifacts/qa-20260719-production`. |
| 2026-07-18 22:34 Asia/Shanghai | Codex | Frontend routes, production diagnostics, demo QA artifacts | Added `SUPER_ADMIN` to team and permission route role allowlists; extended `scripts/diagnose-production.py` to verify the companion download package, anonymous write/upload guards, and external TCP reachability for sensitive DB/Redis ports; produced production QA screenshots under `artifacts/qa-20260718-production`. | Ensure the July 19 demo can show real authenticated pages with a super-admin account, verify upload/download boundaries, and distinguish a Docker listen warning from actual external DB/Redis exposure. | `npm run test --workspace=frontend` (7/7 files, 38/38 tests); `npm run build --workspace=frontend`; `py -3 -m py_compile scripts/diagnose-production.py`; `py -3 scripts/diagnose-production.py --remote`; production Playwright QA with short-lived read-only token; account CSV export downloaded `璐﹀彿鍒楄〃_2026-07-18.csv`; frontend deployed as `assets/js/index-GZd5vnNY.js`. |
| 2026-07-18 22:00 Asia/Shanghai | Codex | Doudian backend, backend health, monetization frontend, desktop companion CORS, bridge service, Docker ports, repo hygiene, deployment records | Removed public access from Doudian browser cache routes, stopped selecting `profilePath` for store list responses, made Doudian/WeChat shop pages clear stale data before/after failed loads, escaped CSV exports, guarded companion download links, tightened companion scan CORS, made `bridge.py` local-first with optional token auth, expanded ignore rules for debug/cookie artifacts, stabilized health memory checks, added deploy health retry, and changed source compose port mappings for DB/Redis/bridge app ports to localhost bindings. | Prepare for the July 19 demo with real data only, close anonymous business-data exposure, reduce false health failures, and reduce the chance that local credentials/debug artifacts or rebuilt infrastructure leak through repo, demo packaging, or public ports. | `npm run test --workspace=frontend` (7/7 files, 38/38 tests); `npm run build --workspace=frontend`; `npx jest --config test/jest.config.ts --runInBand` (26/26 suites, 277/277 tests); `npm run build --workspace=backend`; `py -3 -m py_compile bridge.py scripts/diagnose-production.py scripts/deploy-backend-safe.py`; companion health `ok`; production diagnose passed and anonymous Doudian endpoints now return `401`; Docker config parsing skipped because Docker is not installed locally. |
| 2026-07-17 15:04 Asia/Shanghai | Codex | `backend/src/modules/health/health.service.ts`, `backend/test/unit/health.service.spec.ts`, Docker healthcheck config, tunnel runbook | Made backend health status reflect failed database or memory checks while still hiding internal check details in production; added HealthService regression tests; added container-level healthchecks for nginx/cloudflared paths and the tunnel recovery command. | Prevent production `/health` from reporting a misleading `ok` when internal checks fail, and keep future production container rebuilds from losing Docker health metadata. | `npm exec --workspace=backend -- jest --config test/jest.config.ts --runInBand test/unit/health.service.spec.ts`; `npm exec --workspace=backend -- jest --config test/jest.config.ts --runInBand` (26/26 suites, 275/275 tests); `npm run build:backend`; `npm run build:frontend`; `npm run test --workspace=frontend` (7/7 files, 38/38 tests); `py -3 -m py_compile` for deploy/diagnose/tunnel scripts; `git diff --check` on touched files. No production deployment executed to avoid shipping unrelated dirty backend changes. |
| 2026-07-17 11:39 Asia/Shanghai | Codex | `scripts/diagnose-production.py`, `docs/deployment-log.md`                                                                             | Updated the production diagnostic frontend-ref check to read the active `matrixflow-frontend` container instead of the obsolete `/opt/matrixflow/frontend/dist` path, and recorded the backend/frontend reliability deployment.                            | Prevent future deploy checks from reporting a stale frontend bundle after production is already serving the new Docker-mounted frontend.                                                               | Public HTML and Docker frontend both served `assets/js/index-CTElgN0V.js`; remote obsolete nginx path was identified as stale; deployment log added for the 2026-07-17 backend/frontend deploy.    |
| 2026-07-17 01:34 Asia/Shanghai | Codex | `frontend/src/api/request.ts`, `frontend/src/__tests__/requestTokenRefresh.test.ts`                                                     | Reworked frontend API token refresh handling to reuse one in-flight refresh across concurrent requests, proactively refresh an expiring access token before normal API calls, avoid showing 401 errors before a refresh retry, and only log out after refresh is unrecoverable. Added regression tests for retry, concurrent refresh, and failed-refresh logout behavior. | Reduce false "login expired" behavior and make the website session behave consistently with the 365-day refresh-token backend change.                                                                  | `npm test -- --run src/__tests__/requestTokenRefresh.test.ts`; `npm test -- --run` in `frontend` (33/33); `npm run build` in `frontend`; `git diff --check -- frontend\\src\\__tests__\\requestTokenRefresh.test.ts`. No production deployment executed.      |
| 2026-07-17 01:24 Asia/Shanghai | Codex | `scripts/deploy-backend-safe.py`                                                                                                       | Added backend deployment guards that back up remote `.env`, ensure `JWT_REFRESH_EXPIRES=365d`, add `JWT_ACCESS_EXPIRES=15m` only when missing, print non-secret JWT expiry env values, and assert deployed `dist` no longer contains the legacy 7-day auth defaults before PM2 restart. | Prevent production from staying on 7-day refresh tokens or old scan-bind JWT defaults after the fixed backend code is deployed.                                                                        | `py -m py_compile scripts\\deploy-backend-safe.py`; `py scripts\\deploy-backend-safe.py --plan`; local shell simulation preserved existing `JWT_ACCESS_EXPIRES=1h` and appended `JWT_REFRESH_EXPIRES=365d`. No production deployment executed. |
| 2026-07-17 01:19 Asia/Shanghai | Codex | backend auth/platform freshness, frontend account/dashboard UI, desktop companion scan-bind/collector, companion dist                  | Hardened Pixingyun companion and website collection flow: refresh-aware auth paths, 365-day refresh-token defaults in config, no scan-bind success before initial collection and cloud sync finish, no collection success for empty/transient payloads, login-page fast skip for WeChat Video, clearer stale labels, and rebuilt `desktop-companion/dist/pixingyun-mate/pixingyun-mate.exe`. | Fix recurring instability around expired login state, misleading "binding success", multiple WeChat Video accounts, and website "recently not collected" false negatives. | `npm run build` in `backend`; `npx jest --config test/jest.config.ts --runInBand --silent` in `backend` (246/246); `npm run build` in `frontend`; `py -3 -m py_compile desktop-companion\\companion_app.py desktop-companion\\companion_login_worker.py desktop-companion\\companion_collector.py desktop-companion\\local_db.py desktop-companion\\companion_metrics.py`; `py -3 -m PyInstaller --clean -y pixingyun-mate.spec`; `git diff --check`. No production deployment executed; backend deploy requires explicit `--execute` approval. |
| 2026-07-17 01:05 Asia/Shanghai | Codex | backend auth/accounts/content/teams tests, cookie crypto, CookieManager, frontend Doudian sync guard, `.gitignore`, official scripts | Stabilized Jest setup/env loading, repaired backend mocks and E2E provider lookup, aligned tests with current shared-mode/upsert behavior, unified cookie encryption/decryption with legacy CookieManager compatibility, fixed account cookie status checks, and made Doudian sync fail fast when the companion omits `job_id`. | Remove recurring small frontend/backend breakages caused by stale tests, split cookie encryption paths, misleading script ignore rules, and silent companion sync starts. | `npx jest --config test/jest.config.ts --runInBand --no-cache`; `npm run build:backend`; `npm run build:frontend`; `npm run test --workspace=frontend`; `desktop-companion\\.venv\\Scripts\\python.exe -m compileall -q desktop-companion`; `py -m py_compile scripts\\diagnose-production.py scripts\\deploy-frontend-fast.py scripts\\deploy-backend-safe.py scripts\\install-tunnel-guard.py`; `git diff --check`. No production deployment. |
| 2026-07-03 Asia/Shanghai       | Codex | `README.md`, `.github/workflows/*`, `scripts/*`, `docs/*`, `.gitignore`                                                                | Replaced stale README, added official diagnose/backend deploy scripts, made legacy ECS deploy workflows manual-confirm only, added project/deployment logs and legacy deployment index, added ignore rules for generated artifacts/profiles/screenshots. | Stop recurring deployment confusion and make future work traceable.                                                                                                                                    | Python scripts compiled; backend deploy plan printed without executing; production diagnose passed; public health returned `200`. No production deployment command executed.                     |
| 2026-07-03 01:22 Asia/Shanghai | Codex | `frontend/src/*`, `frontend/dist`, production frontend                                                                                 | Recorded dirty frontend source before redeploying the current Doudian-capable frontend. Public site had reverted to old bundle `assets/js/index-uL8uFGV0.js`; deployed `assets/js/index-DatxT-TX.js` with Doudian route/page.                            | Restore the production UI that includes the Doudian section and avoid another AI mistaking the old public bundle for latest.                                                                           | Public frontend served `assets/js/index-DatxT-TX.js`; Doudian route tokens and chunk `DoudianView-DkKp-eK0.js` were present.                                                                     |
| 2026-07-03 Asia/Shanghai       | Codex | legacy deploy scripts, legacy GitHub deploy workflows, `docs/legacy-deploy-index.md`, `LATEST_DEPLOYMENT.md`                           | Removed runnable legacy deployment and repair entry points that referenced old paths, hard resets, mixed deploy flows, PM2 delete/recreate, or nginx kill/rewrite behavior. Updated latest marker and legacy index.                                      | Old versions and old deployment scripts were influencing deployment decisions and causing repeated mistakes.                                                                                           | Official scripts `diagnose-production.py`, `deploy-frontend-fast.py`, and `deploy-backend-safe.py` still exist and compile; removed files no longer exist locally; public health returned `200`. |
| 2026-07-03 01:33 Asia/Shanghai | Codex | `scripts/deploy-backend-safe.py`, `LATEST_DEPLOYMENT.md`, `docs/deployment-log.md`                                                     | Fixed backend deploy script to remove the remote `prisma` directory before copying the new one; updated latest deployment marker and deployment log.                                                                                                     | Prevent production from generating Prisma Client with a stale schema, which caused Doudian endpoints to fail with `findMany` undefined.                                                                | Re-ran safe backend deploy; remote schema and Prisma Client expose Doudian models; public Doudian endpoints returned `200`.                                                                      |
| 2026-07-03 Asia/Shanghai       | Codex | `backend/dist`, `frontend/dist`, `desktop-companion/dist`, root screenshots                                                            | Removed generated artifacts and screenshots from Git tracking with `git rm --cached`; files remain on local disk and production server was not touched.                                                                                                  | Generated outputs were flooding `git status`, confusing version checks, and making deployment decisions harder.                                                                                        | Staged changes contain generated artifact deletions plus this log update only; public health returned `200`.                                                                                     |
| 2026-07-03 Asia/Shanghai       | Codex | `backend/public/assets`                                                                                                                | Removed stale frontend build assets from Git tracking with `git rm --cached`; local files remain on disk and production server was not touched.                                                                                                          | `backend/public/assets` contained hundreds of historical frontend bundles that could mislead version checks.                                                                                           | Public health returned `200`; local `backend/public/assets` still exists; `git ls-files backend/public/assets` returned `0`.                                                                     |
| 2026-07-03 Asia/Shanghai       | Codex | root screenshots, Vite logs, `wechat_*.png`, `token_output.txt`                                                                        | Deleted root-level local screenshots, transient Vite logs, WeChat verification screenshots, and an empty token output file after verifying all paths stayed inside the workspace root.                                                                   | These temporary files were cluttering the project root and making cleanup/deployment audits harder to read.                                                                                            | Deleted 43 files totaling about 6.24 MB; matching root temp files remaining `0`; production public health returned `200`.                                                                        |
| 2026-07-03 Asia/Shanghai       | Codex | `backend_new`, `matrixflow-desktop`, `pixing-local-companion`, `pixingyun-desktop`                                                     | Removed obsolete local legacy project copies from the workspace and Git tracking after checking deployment scripts and current deployment markers.                                                                                                       | Old project copies and legacy desktop/companion paths were confusing version selection and deployment cleanup.                                                                                         | `backend_new` had zero references in README/deploy docs/scripts and 964 local files were deleted; the other legacy paths were already absent locally; production public health returned `200`.   |
| 2026-07-03 Asia/Shanghai       | Codex | legacy root deploy scripts, `start.sh`, `HANDOVER.md`, `scripts/deploy_backend.py`, `scripts/diagnose-ecs.py`, `nginx-matrixflow.conf` | Removed remaining runnable legacy deployment/debug entry points, stale handoff material, and transient deployment tarballs after preserving the official diagnose/frontend/backend scripts.                                                              | These files referenced old paths, remote hard resets, PM2 delete/recreate, old port 3001, direct cloud credentials, or stale May 2026 production assumptions and could mislead future deployment work. | Official scripts `diagnose-production.py`, `deploy-frontend-fast.py`, and `deploy-backend-safe.py` still exist; production public health returned `200`.                                         |
| 2026-07-03 Asia/Shanghai       | Codex | `desktop-companion/build`, `scripts/__pycache__`                                                                                       | Deleted ignored local build/cache directories while preserving current desktop companion `dist`, backups, and browser/login profiles.                                                                                                                    | PyInstaller intermediate build output and Python cache files were local generated artifacts, not source or deployable release material.                                                                | Deleted about 82.85 MB of local generated files; current companion `dist` and backups still exist; production public health returned `200`.                                                      |

## Entry Template

| Time                | Actor | Files / Area | Change            | Reason                 | Verification                          |
| ------------------- | ----- | ------------ | ----------------- | ---------------------- | ------------------------------------- |
| YYYY-MM-DD HH:mm TZ | name  | path(s)      | short description | why this change exists | tests, diagnostics, or reason skipped |
