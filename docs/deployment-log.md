- 2026-09-01 11:55 Asia/Shanghai: 业绩天梯月份切换上线后的自查修复轮（backend + frontend，无新迁移；随后 12:00 的系统健康中心收尾部署基于同一工作树，两个修复都包含在内）。(1) 后端 `deploy-backend-safe.py --execute --skip-build` 成功（backup `/opt/matrixflow/releases/backend/20260901115359`）：`getMonthSnapshot` 首次创建历史月快照时捕获 P2002 唯一约束竞态（两个标签页同时首次打开同一历史月 → 另一个请求的 500），冲突后重读已创建快照返回；`captureCurrentMonthSnapshot` 的 upsert 遇 P2002 时回退为 update 重试。新增 3 项竞态单测（快照套件 12/12）。(2) 前端 `deploy-frontend-fast.py` 成功（public entry `assets/js/index-Bj62cDSq.js`，Cloudflare 已 purge）：① 修复快速切月的 loading 计数泄漏——被最新请求守卫丢弃的旧请求此前不配对 stop，引用计数的全局刷新动画会永久卡住，现在每次 start 必配对 stop；② 修复历史月跨月退款遗漏——微信/抖店售后查询原来按「月初~月末」过滤，8 月订单 9 月才退款的售后记录查不到导致历史月有效订单高估（量化：抖店 8 月订单 9 月退款 3 笔、7 月订单 8 月退款 10 笔），现改为只设 begin（月首至今），多余售后记录按本月订单 id 匹配无害。(3) 生产数据核查发现的历史快照缺口（非本次代码引入）：8 月快照缺王晶（8/24 被删，8 月实际抖店 51 + 微信 24 单）、7 月快照为空（卢慧配置 8/21 才创建被 createdAt 保护排除，7 月实际有大量卢慧订单）——历史月目标值与缺失老师需运营确认后人工修正，已提供 `scripts/fix-ladder-snapshot.py`（list/set/remove/reset）工具。详见 `docs/project-change-log.md` 2026-09-01 11:55 条目。
- 2026-09-01 12:00 Asia/Shanghai: 系统健康中心 Phase 1 收尾上线（backend + frontend，无新迁移）。(1) 后端两次部署：首次 `deploy-backend-safe.py --execute --migrate`（backup `/opt/matrixflow/releases/backend/20260901114712`，21 migrations up to date，health 200，PM2 online）上线本轮修复——故障恢复巡检 sweepIncidentRecovery（每分钟：OPEN 静默10分钟→RECOVERING，恢复窗口内无复发→RESOLVED；COMPANION 来源排除；阈值 INCIDENT_RECOVER_AFTER_MINUTES/INCIDENT_RESOLVE_AFTER_MINUTES 可配）、严重度只升不降、overview P0/P1/未解决改真实 count（不再从 Top8 截断列表统计）、总体状态基于当前未恢复故障（不再被 24h 粘性计数压住）、前端事件时间戳钳制（早于7天/晚于5分钟回落）、慢接口严重度对齐（≥1s WARNING）。部署后生产实测发现并发竞态 bug：前端刷新健康中心时 overview/incidents 并行触发 syncCompanionIncidents → findFirst→create 竞态 → 每个伴侣故障产生一对重复 SystemIncident（16 行含 8 行重复）。修复：syncCompanionIncidents 进程内串行化（syncChain 链式排队）+ 新增并发单测；第二次部署（backup `/opt/matrixflow/releases/backend/20260901115533`，health 200）后清理生产存量重复（按 dedupeKey 分组保留最早一条、合并 max occurrenceCount/ack 字段，7 组合并、删除 8 行），75 秒观察窗口 duplicate_keys=0。(2) 前端 `deploy-frontend-fast.py --allow-dirty-source` 成功：public entry `assets/js/index-BepOSBOX.js`（remote/origin/public 三处一致；hash 未变是因为 __APP_VERSION__ 常量折叠后产物与上一版一致，功能等价）、SystemHealthCenterView chunk `SystemHealthCenterView-CGAjyRPo.js` 公网 200，Cloudflare 已 purge，backup `/tmp/matrixflow-frontend-dist-backup-20260901114755`。(3) 生产实测（服务器端短时 super-admin JWT 签发验证，密码不出服务器）：GET /system-health/overview 200（overallStatus=INCIDENT，p0=0，p1=3，unresolved=7，六层卡片齐全）；incidents/events 200（events 含真实 BACKEND HTTP_4XX 事件且带 requestId——事件管线生产已流动）；companion-monitor/overview 200（3 设备 1 在线，在线设备心跳持续 201，重启后采集/同步不受影响）；SystemEvent=11 行、SystemIncident=8 行（去重后）；一条 FRONTEND JS_ERROR 故障已被巡检自动置为 RECOVERING——恢复巡检 cron 在生产真实生效。功能设计详见 `docs/system-health-center-architecture-report.md` 与 project-change-log 2026-09-02 条目。
- 2026-08-25 18:22 Asia/Shanghai: Follow-up on 组长+商业转化权限 — user reported "members still see the pages". Investigation: the deployed frontend was verified correct (all five 商业转化 routes carry `roles: [SUPER_ADMIN,OWNER,ADMIN,MANAGER,GROUP_LEADER]`, sidebar filter + router guard both gated). The DB has only TWO non-member accounts besides the super admin: `nana` (13530744337) and `1321427839@qq.com`, both `MANAGER` (管理者) — 管理者 is 组长及以上 by design, so they correctly still see the pages; the 1321427839@qq.com account logged in at 17:54, matching the testing window. All 17 MEMBER accounts are blocked. Fix shipped (frontend-only): `AppLayout` now calls `fetchUserInfo()` on boot (silent catch) so the persisted localStorage role self-corrects after an admin changes a role — a stale browser session can no longer keep old permissions across reloads (open tabs from before a deploy still need one refresh). Frontend redeployed: public entry `assets/js/index-D7VcOktl.js` (deploy script timed out locally while the remote side was copying the 7.8G preserve dirs back; the remote command completed on its own — verified no deploy processes left, target ref matches, downloads/companion-updates/uploads intact: latest.json serves, installer 200 with 451MB — then Cloudflare HTML cache purged manually). If 管理者 accounts should be demoted, use 超管后台.
- 2026-08-26 14:45 Asia/Shanghai: 伴侣监控中心上线（backend + frontend + 伴侣 3.2.102 一起打包发布）。(1) 后端：新增 companion-monitor 模块（POST /companion-monitor/heartbeat 心跳接收 + GET devices/overview/alerts/acknowledge/history 查询，租户隔离：SUPER_ADMIN 全量、OWNER/ADMIN 按组织，JWT 鉴权）；Prisma 三表 CompanionDevice/CompanionHeartbeat/CompanionAlert + 迁移 202608260001_add_companion_monitor（服务端 prisma migrate deploy 已应用）；心跳 upsert 设备 + 节流写历史（5 分钟内同状态不重复写、任务/同步/错误变化即写）；每分钟健康评估（0-2min 在线 / 2-5min 不稳定 / >5min 离线；连续3次同步失败、超24h离线、心跳超5分钟、任务超时、自动更新失败、版本过旧、403·404·500异常增多、大量登录态失效 8 类告警去重）；凌晨 3:30 清理 30 天前心跳与已确认告警；COMPANION_LATEST_VERSION=3.2.102 写入服务端 .env（pm2 restart --update-env）。单元测试 16/16，全后端单测 27 套 239 项绿。deploy-backend-safe.py --execute --migrate 成功（backup /opt/matrixflow/releases/backend/20260826143115，health 200）。(2) 前端：新页 伴侣监控中心（/companion-monitor，系统分区，SUPER_ADMIN/OWNER/ADMIN 可见）：健康总览（正常/异常/离线/版本过旧/今日采集成功率/今日同步成功率/最新版本）、设备列表（设备/使用人/版本/在线状态/当前任务/平台状态/最后采集/最后同步/最近异常/资源/最后心跳 + 全部/在线/离线/异常/版本过旧/正在采集/正在同步 筛选）、未处理告警面板（确认按钮）、单设备 7/30 天心跳+告警历史弹窗、30 秒自动刷新。deploy-frontend-fast.py 上传+替换成功（public entry assets/js/index-CDf0_2wc.js、monitor chunk CompanionMonitorView-B37EH4sr.js 公网 200），脚本在远程验证读取时超时（已知 paramiko 模式），已手工核对容器文件与公网引用并 purge Cloudflare。(3) 伴侣 3.2.102：新增 companion_heartbeat.py（45 秒心跳，隐私铁律：只上报自身进程/任务状态，不采集用户其他软件/浏览历史/网页/屏幕，无远程控制接口），字段：deviceId/deviceName/loginUser/companionVersion/startedAt/taskStatus(空闲/采集中/同步中/排队中/更新中)+taskDetail+taskStartedAt/平台状态(抖音/小红书/快手/视频号/抖店 登录正常·登录失效·未绑定)/最近采集(success/accountCount/错误码)/最近同步(success/uploadCount/错误码)/最近错误/自动更新状态/近期403·404·500计数/自身进程CPU·内存·运行时长(只读自身PID)；采集收尾/抖店同步收尾打点（record_collection/record_sync），install_companion_request_headers 响应计数，companion_app 启动线程。发布：installer pixingyun-mate-setup-3.2.102.exe 451977204 B sha256 9c7facdbc5378fe37c97c8b75d03829d1bf590f8753d46833c439fb776c5c19a；portable zip 348091492 B sha256 47ff8b58699e3ac2e4e9f86989db6ef28c6f7a2cbc8d8cf83c1ad6b472e8d207；manifest 3.2.102 + Cloudflare purge。本机 D:\Pixingyun 已替换为 3.2.102（备份 D:\Pixingyun.bak-3302），/health 3.2.102，生产端心跳 201 已确认（device a31f9d1c5ecd819e，devices/overview/alerts/history 全部 200；期间 PM2 重启窗口出现的瞬时 502 已自行恢复，设备状态 unstable→online 阈值行为符合设计）。发布前门禁：全源码 py_compile 0 失败、编码回归 13/13、windows-compat ok、后端 tsc、前端 typecheck/build。上线后修复一处生产发现：lastSync.success 为 null（尚无同步）时被误计为同步失败（consecutiveSyncFailures 每 45 秒 +1 → 设备被误标异常）——仅显式 false 才计失败（新增回归测试，16/16 → 17/17），后端再部署（backup /opt/matrixflow/releases/backend/20260826144401），并清除了该 bug 膨胀的计数；同时补发 lite 包 pixingyun-mate-lite-3.2.102.zip 155852996 B sha256 e1581b783f10b913410cbc1e83ef29be364e971df0366ecac34ce84ffe75a1f9（manifest 含 lite_url/lite_sha256/lite_size 与 installer_url/installer_sha256，kind=portable）。最终验证：overview normal=1/abnormal=0、设备 online、公网 chunk 含 伴侣监控中心/连接不稳定/版本过旧/未处理告警 全部命中。
- 2026-08-26 15:55 Asia/Shanghai: 伴侣监控中心 Phase 2 闭环上线（后端迁移 + 后端部署 + 伴侣 3.2.103 三件套发布，无前端改动）。(1) 后端：迁移 `202608260002_add_monitor_phase2` 已应用（CompanionDevice 增 bootId/bootSeq/bootCount/exitState/uiMode/startupDiagnostic/lastProgressAt；CompanionHeartbeat 增 bootId/seq；新表 CompanionEvent/CompanionIncident），deploy-backend-safe.py --execute --migrate 成功（backup /opt/matrixflow/releases/backend/20260826153957，health 200）；COMPANION_LATEST_VERSION 更新为 3.2.103（scripts/set-companion-latest-version.py 现支持命令行参数/自动读源码版本）。(2) 伴侣 3.2.103：bootId 每进程一次 + seq 单调递增 + atexit 优雅退出信标（exitState=clean）；webview_window 上报 uiMode 与浏览器降级原因 + WebView2 运行时版本；任务进展快照检测 → lastProgressAt；record_sync 失败路径打点（抖店同步包装器 + post-stats 上传）；TASK_TIMEOUT 替换为 TASK_STUCK（无进展判定）。发布前门禁：全源码编译 0 失败、tests/test_heartbeat.py 10/10、编码回归 13/13、windows-compat ok；后端 tsc + 单测 30/30（全量 27 套 253 项）。三件套：installer 451,975,468 B sha256 b681099aca0f961ed66eae7030daa95b214a0b4658cfee1037f175823c0bbc3b；portable 348,097,993 B sha256 f3df50b90f08d55ce01c9f653e18803e176f5c8cc19a7089a2a6c39fe50ff482；lite 155,860,897 B sha256 93698377d2613aa5650bd9b62a09573774492d048d53b54c6805b10047d58632；manifest 3.2.103 kind=portable（含 lite/installer 元数据），三个下载 200，Cloudflare 已清缓存。(3) 本机端到端实测：D:\Pixingyun 已换 3.2.103（备份 D:\Pixingyun.bak-32102），心跳携带 bootId/seq（bootSeq 持续递增）、uiMode=webview；硬杀进程再重启 → 生产自动产生 CRASH_SUSPECTED 故障 + BOOT_STARTED 事件 + bootCount 递增，2 分钟后自动 recovering→resolved（duration=190s）；3.2.102→3.2.103 升级前产生的 VERSION_OUTDATED 故障在升级后自动 resolved（duration=180s）且同类型告警自动关闭——Event/Incident 闭环在真实环境全部验证。GET /companion-monitor/incidents 与 /events 公网 200。注意：新告警类型（CRASH_SUSPECTED/TASK_STUCK）在现有页面的告警面板显示原始英文类型名（前端未加中文映射，按运营要求本轮不动前端）。
 - 2026-08-26 12:05 Asia/Shanghai: Pixingyun Mate 3.2.101 — 真机回归发现并修复注册表死记录累积。(1) 真机全场景回归（3.2.100 发布后执行）：扫码绑定 douyin（8 个 scan_login_browser 登记→取消→全部退出，保护日志完整）；抖店同步披星教育（8 个 doudian_browser 登记→取消→KILL_PREPARE×12 全走注册表五重校验→用户 chrome 基线 20 全程不变）；profile 占用场景（系统 Chrome 占用唐商披星 profile→同步失败提示「抖店浏览器启动失败，请关闭残留浏览器窗口后重试」→占用实例存活→保护日志 PROFILE_NO_MANAGED_PROCESS 拒绝清理→释放锁后重新同步恢复正常）；关闭/重启伴侣全程 chrome 20/20、Weixin 6+12、Feishu 14、explorer 1 零损失。(2) 发现缺陷：清扫后已退出的登记记录不从 managed_processes.json 剪除（重启后 21 条死记录残留、无限累积）。修复：terminate_managed 的 already_gone 路径同步注销记录；terminate_stale_records 清扫后剪除已死 PID；新增回归测试 test_11（套件 11/11 PASS）。(3) 3.2.101 发布：portable/lite/installer 三件套 + latest.json + Cloudflare 清缓存，公网 magic bytes 504b0304/4d5a5000 验证通过；本机 D:\Pixingyun 已替换为 3.2.101（备份 D:\Pixingyun.bak-3301=3.2.99、D:\Pixingyun.bak-3300=3.2.100），启动清扫把 21 条死记录剪除为 0，health 3.2.101。
 - 2026-08-26 11:36 Asia/Shanghai: Pixingyun Mate 3.2.100 — P0 进程安全 + 更新器 WSH 修复双发布。(1) 进程安全：新增 process_registry.py 托管进程注册表（pid/create_time/type/taskId/accountId/storeId/profilePath/executable，持久化 %LOCALAPPDATA%/MatrixFlow/managed_processes.json）；所有 terminate/kill 只走 terminate_managed 五重校验（PID 存在/已登记/create_time 一致防复用/可执行文件一致/命令行含 profile），任何一项无法确认 → 拒绝并写 SKIP_UNMANAGED_PROCESS 保护日志（process_guard.log）；browser_manager 两个 cleanup 不再全系统扫描猜测；companion_app 抖店清理删除 PowerShell CommandLine -like 全表 Stop-Process（旧逻辑会误杀命令行含 profile 路径的任何软件）；weixin_channels_collector/collect_and_upload 的 connect_over_cdp 前校验 /json/version Browser-Pid 归属、结束后不再 Browser.close()（旧逻辑会整关用户浏览器）；chrome_cdp taskkill 前过注册表；全部 launch 点（metrics/login_worker/doudian/pixing_worker/webview）launch 前后 diff 登记新进程。新增 tests/test_process_safety.py 10 项回归（10/10 PASS，覆盖未登记拒绝/create_time 篡改拒绝/可执行文件不一致拒绝/按 profile 只关 A 不关 B/已死 PID/CDP 归属/launch diff/保护日志）。(2) 更新器：删除 run_update.vbs —— 旧实现用 write_text_file(bom=True) 把 .vbs 写成 UTF-8-SIG，Windows Script Host 不认 UTF-8 BOM，首字节 EF BB BF 被判非法字符报 800A0408（本机 cscript 复现一致）；改为 CREATE_NO_WINDOW + STARTF_USESHOWWINDOW/SW_HIDE 直接隐藏启动 powershell.exe；新增 _validate_generated_script 生成后自动校验（可读/非空/UTF-8 解码/无替换符/首字符合法）；更新失败统一提示「自动更新失败，请重新尝试。」不再反复弹 WSH 窗口（apply_update 两处异常路径接入 _friendly_update_failure）。(3) 打包：pixingyun-mate.spec 与 pixingyun-mate-onedir.spec 的 datas 增加 process_registry.py（函数内局部 import，静态分析抓不到，缺了会打包丢失）；APP_VERSION 3.2.99→3.2.100。发布前全量检验：源码 137 文件 py_compile 0 失败；进程安全回归 10/10；更新器端到端验证（坏脚本被拒/隐藏启动成功/不再产生 vbs）。发布：portable zip 348074580 B sha256 71692168e9a21802ffa340c47405ea15aa48f20d38182bfd90ecd782076f9189；lite zip 155837280 B sha256 9f99980fc7c7033ec90f572841ee2023639905eb78451c0ebde41bc67a168325；installer 451954913 B sha256 d2e574d70cd438adc6b14ea226774d46b1659f64049170053ac8c549af833aa0。公网 latest.json kind=portable（含 lite/installer 元数据）、三个下载 200、magic bytes 504b0304/4d5a5000 验证通过、Cloudflare 已清缓存。待办：本机 D:\Pixingyun 仍跑 3.2.99，替换需停掉当前实例；真实环境回归清单见 desktop-companion/PROCESS_SAFETY.md。
- 2026-08-25 18:50 Asia/Shanghai: Pixingyun Mate 3.2.99 — silent duplicate-install cleanup (per operator: no prompts). On startup the companion now silently recycles ALL other Pixingyun Mate installs found on the machine (running install wins; all installs share one AppData, so deleting install dirs loses no data; if a newer install is removed the app self-updates to latest anyway). Removal goes to the Recycle Bin (recoverable), logged locally + on the server, with NO toast/banner (removed the earlier prompt UI, open-folder route, and config exposure). Fixed a startup-flow truncation bug introduced in 3.2.98 (a column-0 `def` inside main closed the `if __name__` block early, making the app exit right after Flask was ready — the 3.2.98 build was never published). Real-machine E2E test passed: installed 3.2.99 to D:\Pixingyun, started → `D:\Pixingyun Mate` (a real second install) was silently recycled (vunknown), current install intact, app stays running (health 3.2.99), log shows 已静默清理其他伴侣安装. Also verified the 3.2.97 startup browser sweep on real machine earlier: 10 fake leaked companion processes killed, 8 innocent chrome processes untouched. Published installer + full/lite zips 3.2.99.
- 2026-08-25 18:20 Asia/Shanghai: Pixingyun Mate 3.2.97 — multi-colleague "杀后台应用" fix (startup browser sweep). Diagnosis: with many colleagues affected, the most likely mechanism is leaked companion browser processes (persistent-context Chromium + CDP browser + doudian system-Chrome) accumulating across sessions — they consume RAM until Windows itself starts closing background apps (which users report as 伴侣杀后台); the earlier single-machine dual-install case remains a separate contributor. Fix: `browser_manager.cleanup_stale_companion_browsers()` sweeps ALL companion-owned browser processes at startup — matching only command lines containing companion-owned profile dirs (`%LOCALAPPDATA%/MatrixFlow/browser-profiles` and the exe-dir `chrome_profile`) — never touching the user's own browsers/software; wired into companion_app startup before any collection/scheduler starts. Verified compile + local install; the sweep is passive when nothing is stale. Also note: colleagues still on pre-3.2.88 versions carry the old blind port-9222 kill; the 30-min strong update reminder (3.2.93+) will prompt them to update. Built/published 3.2.97 (installer + full/lite zips), local install replaced (backup D:\Pixingyun.bak-3296).
- 2026-08-25 17:33 Asia/Shanghai: Added the 组长 (GROUP_LEADER) user role and restricted the 商业转化 pages to 组长及以上 (backend + frontend; nothing else changed). (1) New `UserRole.GROUP_LEADER` enum value: prisma schema + migration `20260824200000_add_group_leader_role` (`ALTER TYPE "UserRole" ADD VALUE IF NOT EXISTS 'GROUP_LEADER'`), `prisma-enums.ts` Role mirror updated, and the teams roleHierarchy now ranks GROUP_LEADER 2.5 (VIEWER 1 < MEMBER 2 < 组长 2.5 < MANAGER 3 < ADMIN 4 < OWNER 5) so role-change checks stay sound. (2) 微信小店/抖店/业绩天梯 routes now carry `roles: ['SUPER_ADMIN','OWNER','ADMIN','MANAGER','GROUP_LEADER']` — sidebar hides the entries and the router guard redirects MEMBER/VIEWER to 仪表盘; the two hidden 出单来源详情 sub-routes carry the same roles so direct URLs are blocked too. No backend endpoint guards were added, GROUP_LEADER keeps the member permission tier (no manage_accounts etc.), and 组长 does NOT gain 团队管理/权限管理/MCP/超管 access. (3) 超管后台 user create/edit role selects + role tag now include 组长, and 个人设置 shows the 组长 label. Backend deployed via `deploy-backend-safe.py --execute --skip-build --migrate` (migration applied, all migrations up to date, pm2 online, health 200, backup `/opt/matrixflow/releases/backend/20260825173232`); frontend deployed, public entry `assets/js/index-CcKAkRK8.js` with ladder chunk `PerformanceLadderView-B1KEMAPN.js` (deploy script timed out reading remote output AFTER the remote replace completed — the known pattern — so verification was redone manually: public ref matches, both public bundles 200, entry bundle contains the full roles array + the three page titles, Cloudflare HTML cache purged manually, production `UserRole` enum now `{ADMIN,MEMBER,VIEWER,OWNER,MANAGER,SUPER_ADMIN,GROUP_LEADER}`). Post-deploy `diagnose-production.py --remote` DIAGNOSE OK (first run hit one transient public-check failure in the deploy window; re-run all green).
- 2026-08-25 17:10 Asia/Shanghai: Pixingyun Mate 3.2.96 — update-progress-in-modal + kill-paths audit. (1) The strong-reminder modal now turns into the download progress dialog after clicking 立即更新: percent bar, 已下载/总量, 暂停/继续 buttons, error message with 关闭, restart notice — no more hidden progress. (2) Full kill-path audit: all 7 terminate/kill sites verified gated (browser_manager profile-marker, doudian profile cleanup, chrome_cdp ownership-gated + own-handle, updater own-process, ffmpeg own-handle) — the 3.2.88 fix is intact in all later builds. (3) Investigation of the colleague's "打开伴侣杀其他后台": the colleague's device (9eb68261, v3.2.93) was installed under `C:\Users\EDY\AppData\Local\Programs\Pixingyun Mate` — the SAME Windows user on the SAME machine as the operator's `D:\Pixingyun` install; both share `%LOCALAPPDATA%\MatrixFlow` (config/db/doudian profiles), so one instance's doudian browser cleanup can kill the other's in-flight sync, and two bundled Chromium collectors on one machine cause memory pressure (Windows closes background apps). That install has since been removed. Recommendation: one companion per Windows user; colleague should use their own Windows account/machine and stay on >=3.2.88. Built/published 3.2.96 (installer + full/lite zips), local install replaced (backup D:\Pixingyun.bak-3295). Adaptive-download field test on the degraded link: probe measured 161KB/s -> auto-switched to 8-thread -> parallel hit a read timeout -> gracefully fell back to sequential and completed with sha256 verified (network was the bottleneck, logic behaved as designed).
- 2026-08-25 13:00 Asia/Shanghai: Pixingyun Mate 3.2.94 — update download speed overhaul + pause/resume. (1) No-proxy channel: update manifest fetches and package downloads now bypass the Windows system proxy (ProxyHandler({})) — colleagues behind Clash/VPN no longer route the 300MB package through proxy nodes. (2) 8-thread parallel chunked download: the updater splits the package into 8 byte-range segments fetched concurrently (falls back to sequential if the server lacks Range support; sequential path keeps its Range resume). (3) Lite update package: published `pixingyun-mate-lite-3.2.94.zip` (149.8MB vs 332MB full — excludes the bundled Playwright Chromium which rarely changes); manifest now carries lite_url/lite_sha256/lite_size, and the updater chooses the lite package when the local install already has `_internal/ms-playwright/chromium-*` (falls back to the full zip otherwise). (4) Pause/resume: new `POST /api/update/pause` + `/api/update/resume` endpoints; downloads check a pause event per chunk, keep partial part-files, and resume from exact byte offsets (8-thread parts resume per-segment; the work dir persists across pause). UI shows 暂停下载/继续下载 buttons and a 已暂停下载 phase. `publish-companion-download.py` gained `--lite-artifact` (uploads + cleans old lite zips, keeping current + previous). Verified: manifest 3.2.94 kind=portable with lite fields, lite zip HEAD 200, local install replaced (backup D:\Pixingyun.bak-3293).
- 2026-08-25 11:45 Asia/Shanghai: Pixingyun Mate 3.2.93 — update strong-reminder. The companion now runs a 30-minute update-check daemon (first check 15s after startup); when a newer version is detected it sets a pending-update state, flashes the taskbar, and the UI shows a prominent modal (发现新版本 + notes + 立即更新 / 稍后提醒30分钟) driven by a new cheap `/api/update/available` endpoint polled every 15s. Same-version re-prompt window shortened from 24h to 30 min (strong reminder repeats until updated); snooze now supports minutes (default 30). Verified end-to-end with a temporary real test manifest (9.9.9) served from the site: daemon detected within 15s, endpoint returned available=true, log shows 触发强提醒; config then reverted to the real manifest and the test file removed. Built/published (installer + portable ZIP 3.2.93), local install replaced.
- 2026-08-25 11:25 Asia/Shanghai: Companion log upload (device-level telemetry). Backend: `POST /api/v1/platforms/report-logs` (platforms controller + service) saves each device's latest `companion.log` + `webview2-diagnostics.log` under `/opt/matrixflow/backend/companion-logs/<deviceId>/` and maintains a `devices.json` registry (version/userId/lastSeenAt, capped at 60 devices); sizes capped (600KB log / 200KB diag), deployed via deploy-backend-safe (tests 33/303 green). Companion 3.2.91→3.2.92: new `companion_telemetry.py` uploads logs 60s after startup then every 30 minutes (daemon thread wired at startup), redacts tokens/passwords/service_token before upload, and reads the frozen exe-dir companion.log (3.2.92 fixed the path bug where 3.2.91 read the wrong location and uploaded an empty main log). Verified live: `latest.log` 13KB + `latest-diag.log` 70KB on the server, registry shows device `a31f9d1c5ecd819e` version 3.2.92 lastSeen 11:24. Combined with 3.2.90's version/device request headers, all devices' version + activity + full logs are now remotely observable.
- 2026-08-25 11:05 Asia/Shanghai: Companion upload telemetry. Backend: `logging.interceptor.ts` now also logs `clientIp` (first hop of X-Forwarded-For, fixing the constant 127.0.0.1 behind the proxy), `companionVersion` (X-Companion-Version) and `companionDevice` (X-Companion-Device-Id). Companion 3.2.90: `companion_config.py` adds a stable per-machine `device_id` (persisted in companion_config.json, generated from hostname/MAC/arch hash) and `install_companion_request_headers()` monkeypatches `requests.Session.request` to inject those two headers on all outbound requests to dddddkiii.com; installed at app startup in `companion_app.py`. Backend deployed (tests 33/303 green); companion 3.2.90 built/published/installed locally. Verified live: doudian upload logs now show `companionVersion=3.2.90`, `companionDevice=a31f9d1c5ecd819e`, `clientIp=116.30.102.129`. This lets the operator distinguish every device/version in the server logs going forward (older companions report empty version/device).
- 2026-08-24 20:00 Asia/Shanghai: Shipped Pixingyun Mate 3.2.89 fixing the Douyin avatar + collection defects (visual-verified). (1) Avatars: 4 of 6 DOUYIN accounts (刘仪伟唠叨唠叨/刘仪伟来了/心理学博士卢慧/晶哥来了-电影文化) showed video covers as avatars — root cause: the scan-time avatar fallback picks the largest image on the Douyin profile page, which is a 9:16 video cover (subtitles visible in the captured images); the 2 correct accounts (卢慧家庭教育/殷老师的国学感悟) were refreshed through the collection profile-API path. Fix: fallback scoring now penalizes portrait images (h > w*1.35 -> 0.2x) and prefers square avatars (companion_login_worker.py). (2) Collection: `_collect_douyin_api_once` never passed `override_sec_user_id`, so `collect_douyin_data` auto-detected the currently-logged-in account — a stale/mis-switched session would attribute A's data to account B; and identity MISMATCH was only logged, data was still accepted. Fix: mismatch now DISCARDS the collection result and marks it failed (companion_metrics.py guard). Built (dist-release-3289), installer + portable ZIP 3.2.89 published, local install replaced. The 4 wrong avatars will auto-correct on the next successful collection after the accounts are re-scanned (profile-API avatars overwrite; same path that already fixed 卢慧家庭教育/殷老师的国学感悟). WebView2 note: colleagues getting the browser-mode UI are on machines without the WebView2 Runtime (portable ZIP doesn't bundle it; the installer does) — pending operator decision on auto-installing WebView2 in a future release.
- 2026-08-24 17:05 Asia/Shanghai: Fixed the colleague's 视频号扫码绑定 403. The colleague's phone-only account (id `cmt6kyd1yc4lp971uh8j0efki`, created 08-23 17:51, no email) had role MEMBER, and `POST /api/v1/accounts` requires the `manage_accounts` permission, which members don't have by default (PermissionService: members only pass when a TeamPermission row enables it, but production has zero teams, so no override path existed). Fixed by upgrading the colleague to MANAGER via direct DB update (same result as the admin user-management UI; MANAGER maps to admin-level permissions incl. manage_accounts + manage_browser, matching the other colleague 1321427839@qq.com). PermissionService reads the role from the DB on every request, so the colleague's existing companion login works immediately — they can retry the scan-bind without re-login. Note for future colleagues: newly added users default to MEMBER and will hit the same 403; either set them as MANAGER in user management, or request an org-level member-permission override (a small backend enhancement — currently the permissions UI only works per team and the org has no teams). No code deploy, no business-logic change.
- 2026-08-24 17:10 Asia/Shanghai: Made account binding work for ALL members by default (operator request after the one-by-one MANAGER role fix was too cumbersome). Flipped the member-level default permissions `manage_accounts` and `manage_browser` from false to true in three places (`permission.service.ts` defaults, `permissions.controller.ts` UI defaults, `PermissionView.vue` member list) — the companion's scan-bind flow needs both (POST /accounts + POST /accounts/:id/cookies). Teams with explicit overrides still win (saved enabled=false blocks), so per-team restrictions remain possible. The previously upgraded colleague was reverted to MEMBER. Deployed backend (backup `/opt/matrixflow/releases/backend/20260824170638`, tests 33/303 green) + frontend (entry `assets/js/index-jdLuCj01.js`, PermissionView chunk `PermissionView-C0rG_K3G.js`); verified end-to-end with a MEMBER-identity probe: create account 201 (was 403), delete 200, no residue. Side effect note: members can now also edit performance-ladder targets (the ladder reuses the manage_accounts permission).
- 2026-08-24 17:25 Asia/Shanghai: Website caching round 1. nginx: HTML (`location /`) changed from `no-cache, no-store, must-revalidate` to `no-cache` + `etag on` so browsers revalidate via ETag/304 instead of refetching the full document every visit (applied to the live container via in-place base64 write on the bind mount + `nginx -s reload`, validated with nginx -t; repo `docker/nginx/default.conf` re-synced to the real production file — it had drifted to a `proxy_pass http://backend:3000` form that fails validation against the production upstream, recovered from the 11:38 `.bak-20260824` which uses the host-IP `172.30.41.188:3000`; API health 200 after reload). Cloudflare Worker (`matrixflow-origin-proxy/src/worker.js`): SPA HTML routes now edge-cached 5 minutes (cacheEverything, cacheKey=URL, 404/5xx not cached; auth/cookie stripped), `/uploads/*` edge-cached 24h; deploy script now resolves CF tokens from `.env.local` before `secrets.env` (the secrets.env `cfat_` token is IP-allowlisted and rejected the current IP with error 9109) and `deploy-frontend-fast.py` purges the site HTML cache after every successful deploy. **RESOLVED 17:35**: found wrangler's cached OAuth login in `C:\Users\EDY\.wrangler\config\default.toml` (owner account li2889514244@gmail.com, auto-refreshed) — OAuth tokens are not IP-restricted, so the Worker was deployed via `npm exec -- wrangler deploy` with OAuth (version 5834967d); `deploy-cloudflare-worker.py` now has a `--oauth` mode for future deploys. Verified live: HTML `CF-Cache-Status: REVALIDATED` (edge-served, origin returns 304), assets `HIT` (30d immutable), API correctly uncached, `x-matrixflow-entry: cloudflare-worker` intact.
- 2026-08-24 18:35 Asia/Shanghai: Diagnosed the "抖音账号保存不住" issue (no code deploy; local config change). Findings: (1) the companion's background collector was PAUSED (`auto_collect_on_start: false` → `collector.paused=true`, next run null) — Douyin sessions are kept alive ONLY by the periodic 30-min collection runs (the session_keepalive daemon covers WECHAT_VIDEO only), so with collection off for days the Douyin logins naturally expired (local douyin cookies_age 71h); (2) the local accounts DB (`%LOCALAPPDATA%\MatrixFlow\browser-profiles\accounts.db`) contains 0 accounts (accounts/accounts_history/account_sessions/current_sessions all empty; contents history shows 19562 posts ever collected; last collection run 08-21 11:15 with startup-stale errors) — nothing to collect, so nothing refreshes sessions; (3) cloud side: 5 DOUYIN accounts, 殷老师的国学感悟/心理学博士卢慧 marked FAILED at 08-23 22:11 (登录态失效/没有拿到有效数据 — attempts from a machine without valid Douyin profiles, likely the colleague's companion), 卢慧家庭教育 SUCCESS 08-21, 刘仪伟来了/晶哥来了-电影文化 never collected. Fix applied: re-enabled auto collection via `POST /api/config {auto_collect_on_start:true}` (persisted, next run 19:00, 30-min interval). Remaining user action: re-scan the Douyin accounts in the companion (local DB has none) — after scanning, the collection loop will refresh sessions every 30 min like before and the cloud statuses recover. If accounts disappear from the local DB again after re-scanning, investigate a wipe bug (current-session log shows no scan/delete events; log truncates each app start). Optional future enhancement: extend session_keepalive to DOUYIN so sessions stay warm even without collection.
- 2026-08-24 19:00 Asia/Shanghai: Root-caused "别人使用披星云伴侣会关闭其他网页和软件". The culprit is `chrome_cdp.py`: `ChromeCDP.start()` blindly `taskkill /F`-ed whatever process listened on CDP port 9222 (plus a netstat fallback killing any LISTENING pid on that port) with NO ownership check — if the colleague's own Chrome/Edge (launched with --remote-debugging-port=9222 by any tool) or any other software held that port, the companion force-killed it, closing their browser tabs/apps. `stop()` had the same blind fallback kill. Fixed in source (3.2.88): kills are now ownership-gated — the target process's command line must contain the companion's own profile path (`--user-data-dir=...chrome_profile`), verified via psutil/PowerShell cmdline lookup; when the port is held by a FOREIGN process the companion no longer kills it but shifts to the next port (9222→9231 max), and `stop()` leaves foreign listeners untouched. Done: 3.2.88 built (`dist-release-3288`), installer (451,906,5xx B) + portable ZIP (348,084,821 B) published with Cloudflare purge; public manifest 3.2.88; local `D:\Pixingyun` replaced (backup `D:\Pixingyun.bak-3287`) and runs 3.2.88. The colleague's machine will prompt the 3.2.88 update via the auto-updater (or install the new installer once).
- 2026-08-24 15:53 Asia/Shanghai: Doudian full-wipe + re-collection per operator decision, plus the companion sync-timeout fix. (1) Root cause of the recurring "Page.wait_for_timeout: Connection closed while reading from the driver": 唐商披星's full pagination (order list + aftersale list, up to 60 pages each) takes ~7.5-8 minutes, right at the companion's 480s `asyncio.wait_for` collect timeout — the timeout cancels the browser mid-wait and surfaces as the driver-disconnect error. Fixes: collector per-page waits tightened (PAGE_SETTLE_MS 1500→900, NETWORK_SETTLE_TIMEOUT_MS 2000→1200, source-capture retries 6→3 with 600ms backoff), collect timeout raised 480→660s, and `_friendly_doudian_error` now maps 'Connection closed'/'wait_for_timeout' to the friendly timeout message; version bumped to 3.2.87 (PyInstaller rebuild + republish pending verification). (2) Data reset executed: server-side full-dump backup taken first (`/opt/matrixflow/backups/matrixflow-full-20260824151946.sql.gz`), then all 880 `DoudianStoreOrder` rows deleted (aftersales/products kept), both stores re-collected through the companion's local sync API. Re-collection captured the newest 600 orders only (60-page cap), losing the 06-12~06-20 band (133 orders) — restored by merging the 08-20 full-DB backup (`matrixflow-pre-account-operators-20260820193941.sql.gz`) via a scratch-database COPY with `ON CONFLICT (storeId,orderId) DO NOTHING`. Final state verified: 唐商披星 733 orders (06-12~08-24, identical to pre-wipe), 披星教育 147 orders (06-18~08-24, identical to pre-wipe), aftersales 667 intact, orphan refunds back to 447 (436 pre-window + 11 boundary — unchanged). The wipe+resync did NOT remove the historical orphans (the platform's order list doesn't serve pre-window pages), so that ⚠️/ℹ️ split stays as designed. Companion hot-patched locally (collector) and restarted; verification syncs of both stores succeeded (465s for 唐商, ~3.5min for 披星教育) under the tightened waits; full 3.2.87 rebuild with the 660s timeout is the durable fix. Follow-up verified: 3.2.87 built (PyInstaller onedir `dist-release-3287`), installer `pixingyun-mate-setup-3.2.87.exe` (451,906,532 B) and portable ZIP `pixingyun-mate-portable-3.2.87.zip` (348,082,956 B) published via `publish-companion-download.py --execute` with Cloudflare purge; public manifest reports 3.2.87 kind=portable, both downloads HEAD 200; local `D:\Pixingyun` replaced with the new build (backup `D:\Pixingyun.bak-3287`) and runs 3.2.87.
- 2026-08-24 19:06 Asia/Shanghai: Resized the global 刷新中 overlay image to a fixed 200px circle (frontend-only). `GlobalLoadingOverlay.vue` `.global-loading__img` is now `width/height: min(200px, 35vw)` (small screens shrink to 35vw to avoid overflow), keeping `border-radius: 50%` + `object-fit: cover`; spin slowed to 2.4s/圈; image-text gap 14px → 20px. Refresh logic untouched (show/hide, random carousel, fade-out all unchanged). Deployed via `deploy-frontend-fast.py --skip-typecheck --allow-dirty-source`; public entry `assets/js/index-BPo7jbfN.js` (four refs matched), overlay styles ship in `assets/css/AppLayout-BNdCrgcO.css` (public-verified live: min(200px,35vw) / gap:20px / 2.4s present, old 72px gone), Cloudflare cache purged, remote backup `/tmp/matrixflow-frontend-dist-backup-20260824190619`.
- 2026-08-24 15:07 Asia/Shanghai: Fixed the 业绩天梯 three reported issues and deployed backend + frontend. (1) 页面弹两次 'Teacher not found'：生产日志定位到 `DELETE /api/v1/performance-ladder/teachers/pltea_1787553581513_r9omvntc`（王晶）在 06:40:54.968Z 删除成功后，06:40:56.631Z 同一 id 又被 DELETE 一次 → 404（失效引用：另一个标签页/重复点击仍持有已删老师）；同一 404 被全局 axios 拦截器和组件 catch 各弹一次，所以用户看到两条英文错误。修复：后端老师相关消息全部中文化（老师不存在或已被删除/无组织访问权限/老师姓名不能为空/权限不足：xxx），前端 teacher 增改删接口改 silent（组件统一中文提示），删除改为先本地移除+404 视为已删（防重复提交），保存时非 pltea_ id 直接新建、PATCH 404 自动按配置重建（失效引用自愈），添加老师加防重锁。 (2) 订单磁贴绑定：有效订单磁贴误绑 totalRefundedOrders（显示 255）→ 改绑 totalCurrentOrders（1059，副文案改 去退款）；总订单磁贴副文案由乱码占位 `?? + ??` 改为 `有效 X + 退款 Y`（1314 = 1059 + 255）；出单账号详情抽屉里重复的 退款订单 标签第一处改为 有效订单。 (3) 乱码排查：`?? + ??` 是前端源码里的字面量占位（模板插值被替换后未填回），非编码/后端问题；整页扫描 �/锟斤拷/Ã/Â/æ 均无其它乱码。后端 `deploy-backend-safe.py --execute --skip-build`（backup `/opt/matrixflow/releases/backend/20260824150509`，与 15:06 对账消息拆分并存，服务器 dist 已验证两处改动都在），前端 `deploy-frontend-fast.py --skip-typecheck --allow-dirty-source`，public entry `assets/js/index-DwNm7xmO.js`（四 ref 一致）ladder chunk `assets/js/PerformanceLadderView-s1uXaUtr.js`（remote backup `/tmp/matrixflow-frontend-dist-backup-20260824150554`）。Post-deploy `diagnose-production.py --remote` DIAGNOSE OK（data sanity 全 0、api_consistency ok）；公网 chunk 与服务器后端 dist 均逐项验证通过。新增只读取证脚本 `scripts/diag-ladder-teachers.py`。
- 2026-08-24 15:06 Asia/Shanghai: Refined the reconciliation orphan-refund warning after investigating the "447 笔成功退款找不到对应订单" message. Findings: all 447 belong to 唐商披星; the order table's earliest synced order is 2026-06-12 while aftersale records go back to ~2026-03, so 436 are refunds of pre-window orders that were never synced (historical caliber gap, total ¥96,078.98 not counted anywhere — pages/daily report exclude them because order-date attribution requires the order row); the other 11 were initiated 2026-06-12~07-04 at the sync-window boundary; zero in the last 30 days, so current-day numbers are complete. The daily message now splits the count: ⚠️ N 笔退款（订单同步开始后发起）找不到对应订单 → needs checking; ℹ️ M 笔为订单同步开始前发起的老退款 → normal. Backend redeployed via `deploy-backend-safe.py --execute --skip-build` (backup `/opt/matrixflow/releases/backend/20260824150500`), tests 33/303 green, live retrigger pushed the updated ✅ message (11 ⚠️ / 436 ℹ️). No counting logic changed.
- 2026-08-24 14:58 Asia/Shanghai: Shipped the daily Doudian data reconciliation (对账) that runs every day at 09:10 Beijing (`DailyReconciliationScheduler`). It double-checks yesterday's per-store numbers through two independent paths — (A) the production caliber (`buildDoudianSummary`, same as the page API) and (B) a pure-SQL recount that shares no code with the metrics module — and compares six fields (all-orders count, total, valid, refunded, gross, refund) plus the 总订单=有效+退款 invariant; any difference pushes a 🔴 anomaly notification, otherwise a ✅ pass summary with per-store totals for eyeballing against the Doudian platform (plus a ⚠️ warning line for orphan refunds whose order is missing from the DB). Reuses the existing notification/Feishu pipeline (`NotificationType.REPORT`, `reportType=daily_reconciliation`, `pushFeishu`), manual trigger endpoint `POST /api/v1/scheduler/trigger/reconciliation`, and a new ops helper `scripts/trigger-reconciliation-once.py` (SSH + short-lived admin JWT) for one-off runs. Comparison logic extracted to `reconcile-compare.ts` with unit tests (4/4). Also repaired four pre-existing failing test suites (test-only changes: added missing `PermissionService`/`accountOperator` mocks, fixed `findOne` signatures and shared-mode org mocks in `accounts.service/integration` specs, updated the `platforms.service` period-metrics test to read the business update instead of the collect-attempt update, added store mocks to `wechat-store.service` spec) — full backend suite now 33 suites / 303 tests green (was 29/260). Backend deployed via `deploy-backend-safe.py --execute --skip-build` (no migration; backup `/opt/matrixflow/releases/backend/20260824145543`); PM2 online, health 200. Live verification: manual trigger 06:58Z returned 200, logs show both stores 一致, notification "✅ 每日对账通过（2026-08-23）" pushed and Feishu token refreshed. WeChat counting logic untouched.
- 2026-08-24 14:33 Asia/Shanghai: Prevention layer for the Doudian counting caliber (no business-logic change; WeChat counting untouched). (1) New single-source-of-truth `docs/订单口径规范.md`: 总订单 = 有效订单 + 退款订单, refunds归订单日 (never refund update_time), implementation locations per page, regression-test requirements, and the 8/24 history lesson. (2) `scripts/diagnose-production.py`: data-sanity SQL now enforces zero duplicates in Doudian/WeChat order+aftersale tables, zero orphan orders/aftersales, zero negative aftersale amounts, zero createTime<=0 orders (day attribution would break), and reports an informational `cross_day_doudian_refunds` count (cross-day refunds are normal — the caliber assigns them to the order day); the api-consistency node now fetches every Doudian store's 30-day `shop/summary` via the authenticated API and fails on any store where totalOrderCount != validOrderCount + refundedOrderCount. (3) DoudianView 总订单 KPI sub now shows 退款按订单日归集 so operators can see which caliber applies. Frontend redeployed via `deploy-frontend-fast.py --skip-typecheck --allow-dirty-source`, public entry `assets/js/index-Y-HPc1-x.js` (all four refs matched), remote backup `/tmp/matrixflow-frontend-dist-backup-20260824143339`. Diagnose script needs no server deploy (it runs checks from the local repo over SSH). Post-change `diagnose-production.py --remote` DIAGNOSE OK with the new invariants live: all 20 zero-required checks 0, informational `cross_day_doudian_refunds=117` (proving order-date attribution matters), api-consistency `doudian_invariant_violations=0` for both stores.
- 2026-08-24 14:25 Asia/Shanghai: Fixed Doudian order counting to attribute refunds by ORDER date and stop double-counting refunded orders (backend + frontend; user-reported data mismatch on 8/21-8/22). Root cause: the site attributed refunds by refund update_time (cross-day refunds landed on the wrong day) and `总订单 = 有效 + 退款` counted refunded orders twice (有效 already included them), so for 披星教育 8/21 the page showed 总13/有效13/退款0 instead of 总13/有效10/退款3, and for 唐商披星 8/22 the per-source numbers were similarly inflated/shifted. Fix: `filterDoudianRefunds` (backend `doudian-store-metrics.ts` + frontend `utils/doudianStoreMetrics.ts`) now only counts refunds whose ORDER is inside the selected range (order-date attribution, cross-day refunds归订单日); `buildDoudianSummary` and the DoudianView fallback now compute 有效 = 营收 − 退款 with `net = gross − refund`; DoudianView/DoudianSourceDetailView fetch ALL aftersales (no refund-time range filter). Unit tests updated (backend 4/4, frontend 42/42). Verified via public API: 披星教育 8/21 → 总14/有效11/退款3; 唐商披星 8/22 → 总49/有效35/退款14 (store-level includes all sources; per-source 卢慧-高维破局 = 总26/有效17/退款9, matching the operator's 出单26; the 9th refund completed 8/24 13:43). Deployed backend (`deploy-backend-safe.py --execute --skip-build`, backup `/opt/matrixflow/releases/backend/20260824142053`) and frontend (`index-DRThBHRj.js`, four refs matched); post-deploy diagnose DIAGNOSE OK.
- 2026-08-24 12:08 Asia/Shanghai: Fixed the remaining 刷新动画图片 browser-upload failure (frontend-only; business logic untouched). Backend log showed the browser's `PUT /organization-settings/loading` arriving with zero parsed files (400 `请选择要上传的图片`): axios 1.16 does not strip the instance default header `Content-Type: application/json` for FormData requests, so the browser serialized the multipart body without a boundary and multer saw no files (server-side python requests and a real headless-Chromium `fetch`+FormData test both returned 200 through the same public chain, isolating the defect to the axios layer). Fix: `organizationSettingsApi.uploadLoadingImages` now uses native `fetch` with FormData (browser sets the multipart boundary itself) plus Bearer auth, with friendly error handling; delete/prefs stay on axios (JSON bodies are unaffected). Deployed via `deploy-frontend-fast.py --skip-typecheck --allow-dirty-source`; public entry `assets/js/index-Y9FRoSJH.js` (all four refs matched); uploads dir preserved (existing image still serves 200 image/jpeg); typecheck + 42/42 tests passed.
- 2026-08-24 12:00 Asia/Shanghai: Repaired the 刷新动画图片 upload/delete 404 + missing-file chain (infra + frontend; business logic untouched). Root causes: (1) the docker nginx static-asset regex `location ~* \.(png|jpg|webp|...)$` intercepted any `/api/...` path ending in an image extension (`try_files $uri =404`), so `DELETE /api/v1/organization-settings/loading/{fileName}.jpg` returned nginx HTML 404 without ever reaching the backend — fixed by changing `location /api/` and `location /ws/` to `location ^~ /api/` / `^~ /ws/` (prefix-with-priority beats regex) in the container nginx; the first `sed -i` attempt broke the single-file bind mount inode so the container kept serving the old config until `docker restart matrixflow-frontend` re-resolved the mount. (2) `scripts/deploy-frontend-fast.py` had `uploads` in the preserve loop but NOT in the restore loop, so every frontend deploy silently deleted the uploads directory — the Aug 21 image file vanished while its DB record remained (preview 404); restore loop fixed, and the remote backup step now excludes the large downloads/companion-updates/uploads dirs (each deploy was also copying ~2.7G into /tmp twice, filling the 79G disk to 100% — cleaned 24 stale deploy/backup dirs, disk back to 32%; failed-deploy tmp dirs are now removed on success). Frontend: settings API calls no longer set a manual multipart Content-Type (browser sets the boundary), request interceptor supports a `silent` config flag, and the settings page shows friendly errors only (图片上传失败/删除失败，请稍后重试) with technical details in console. The dead Aug 21 DB record and the test upload were removed through the public DELETE API (now returning 200 JSON — verified end-to-end); the library now contains exactly one valid image whose URL serves 200 image/jpeg. Frontend redeployed (`assets/js/index-CfURANG-.js`, all four refs matched); post-deploy diagnose DIAGNOSE OK; repo `docker/nginx/default.conf` synced with the `^~` fix.
- 2026-08-24 11:16 Asia/Shanghai: Upgraded the 刷新动画图片 single-image setting into a multi-image library with random playback (backend + frontend; business logic untouched). Backend `organization-settings` module: library stored under the existing `OrganizationSetting` table with new key `loading_images` (JSON: images[], randomEnabled, defaultFileName); legacy `loading_image` rows are auto-migrated into the library on read; endpoints now `GET /loading` (library config), `PUT /loading` (multi-file upload via `FilesInterceptor('files', 10)`, PNG/JPG/JPEG/WEBP ≤3MB each, max 30 images), `DELETE /loading/:fileName` (delete single, filename regex-validated), `DELETE /loading` (clear all + restore default), `PATCH /loading/prefs` (randomEnabled / defaultFileName); images continue to live in `/opt/matrixflow/frontend-dist/uploads` served by nginx. Deployed with `deploy-backend-safe.py --execute --skip-build` (backup `/opt/matrixflow/releases/backend/20260824111404`, no migration needed — same table; PM2 restart; health 200). Frontend: `loading` store now holds the image library and `pickRandomImage()` (avoids repeating the last index); `GlobalLoadingOverlay` picks one random image per refresh and, when random playback is on and more than one image exists, rotates every 2.5s with a 220ms crossfade (fast refreshes just show one image; image keeps its 360° spin); fixed mode uses the user-designated default image or the first one; 设置页 rebuilt: multi-upload button, circular thumbnail gallery with per-image preview (el-image viewer) / delete / 设为默认, 随机播放 el-switch, 清空全部. Deployed via `deploy-frontend-fast.py --skip-typecheck --allow-dirty-source`; public entry `assets/js/index-DEiltA6q.js` (all four refs matched); unauth settings GET returns `401`; post-deploy `diagnose-production.py --remote` DIAGNOSE OK.
- 2026-08-21 19:14 Asia/Shanghai: Deployed frontend-only motion polish (no business logic changes). Unified motion tokens `--motion-fast 150ms / --motion-normal 220ms / --motion-slow 320ms` + `--ease-standard` (cubic-bezier(0.4,0,0.2,1)) in `_variables.scss`/`global.scss`; added `prefers-reduced-motion` global fallback. Element Plus motion overrides in `_element-overrides.scss`: dialog/msgbox fade + scale 0.97→1 (mask fades), popover/select/dropdown `el-zoom-in-top` replaced with fast fade + translateY(-4px), message/notification slide-in + fade, button `:active` scale(0.97), el-card hover translateY(-1px) standard, table/pagination/tabs/segmented transitions tokenized. Sidebar collapse now smooth: `Sidebar.vue` width tokenized + overflow clip, `SidebarInner.vue` logo/group-title/item-label/status text switch from `v-if` to opacity fade classes (no more text flash). Refresh numbers now animate: `AnimatedNumber` default 800ms→280ms and wired into dashboard stat bar, WeChat/Doudian KPI cards, ladder hero numbers, account-card fan counts (ends exactly on the true value — no accuracy impact). Global refresh breathing: `GlobalLoadingOverlay` toggles `body.global-loading-active` so the main content dims gently during refresh and fades back (its own fade uses motion tokens). Deleting an account card now fades out and collapses height before list refresh (`removing-item` utility + AccountListView handleDelete). Deployed via `deploy-frontend-fast.py --skip-typecheck --allow-dirty-source` (script's output-read timed out again after the remote replace had completed; re-verified manually: public ref `assets/js/index-BLagOYb1.js` == local dist; manifest `3.2.83` intact, downloads HEAD `200`, health `ok`).
- 2026-08-21 19:02 Asia/Shanghai: Deployed the site-wide unified loading experience + organization-level refresh-image setting (backend + frontend). Backend: new `OrganizationSetting` org-scoped key-value table (migration `20260821190000_add_organization_settings`, applied via `deploy-backend-safe.py --execute --skip-build --migrate`, backup `/opt/matrixflow/releases/backend/20260821185643`), new `organization-settings` module: `GET /api/v1/organization-settings/loading` (current config), `PUT /api/v1/organization-settings/loading` (multipart upload, PNG/JPG/JPEG/WEBP ≤3MB, per-upload unique filename, old file cleanup), `DELETE /api/v1/organization-settings/loading` (restore default); image files live in `/opt/matrixflow/frontend-dist/uploads` served directly by nginx (`deploy-frontend-fast.py` now preserves the `uploads` dir on redeploys). Frontend: new Pinia `loading` store (`start`/`stop`/`stopNow`, 200ms show delay, 400ms minimum visible, concurrent ref-count), new `GlobalLoadingOverlay.vue` (teleported fixed overlay measured to the main content area, 72px circular image spinning 360°/1.5s linear infinite + 刷新中..., semi-transparent backdrop, auto-fades out; falls back to the Pixingyun logo when no custom image or on image error), mounted in `AppLayout` which also hydrates the org-level image config once per session. 设置页（ProfileView）新增【刷新动画图片】卡片：上传/圆形预览/恢复默认/当前使用状态。All data pages wired to the store: 矩阵数据、账号接入(store)、微信小店、抖店、抖店/微信来源详情、业绩天梯、内容洞察、团队管理(store)、平台配置、账号详情 — business logic untouched. Frontend deployed via `deploy-frontend-fast.py --skip-typecheck --allow-dirty-source`, public entry `assets/js/index-BKeHq4bB.js`, all four refs matched. Post-deploy verify: unauth settings GET `401`, prod `OrganizationSetting` table present, `diagnose-production.py --remote` DIAGNOSE OK (15 migrations up to date, health 200, data sanity 0 anomalies).
- 2026-08-21 18:28 Asia/Shanghai: Deployed frontend-only 出单来源 display-unification + ladder ranking responsive layout. WeChat Store / Doudian 出单来源 and both source-detail pages now use the Performance Ladder field semantics: main number = 有效订单(去退款), sub line `总订单 X · 退款 Y`, with `totalOrderCount = validOrderCount + refundedOrderCount` (refund counts deduplicated per order). Ladder counting was not modified. The ladder 出单来源排行 section now uses container queries based on the main-content width (`container-type: inline-size` on `.source-board`): Top3 podium 3/2/1 columns at 940/560px content breakpoints, normal ranking 2/1 columns, podium cards became flex-column with in-flow number block (no more absolute-position overlap), long names/operators ellipsize with hover titles, and the main number uses `clamp(26px, 5.5cqw, 38px)`. Deployed with `deploy-frontend-fast.py --skip-typecheck --allow-dirty-source`; the local script timed out reading remote output AFTER the remote replace had completed, so verification was redone manually: public ref `assets/js/index-2OpEzRA0.js` matches local dist, manifest `3.2.83` intact, downloads HEAD `200`, public health `ok`. Post-deploy `diagnose-production.py --remote` passed (see below).
- 2026-08-21 17:58 Asia/Shanghai: Fixed and shipped the Doudian stale-binding failure chain (backend + companion 3.2.83). Root cause: the companion's local `cloud_store_id` pointed at cloud stores that were re-created on 08-20 (new ids `cmsqxh0m…`/`cmsqxio…`), so every upload hit `POST /doudian-browser/stores/{old_id}/upload` and got `400 Doudian store not found` from `assertStoreAccess`; auto-rebind never fired because (a) the global `HttpExceptionFilter` rewrote structured exception bodies to numeric `code`, so the companion's `STALE_COMPANION_BINDING` detection never matched, and (b) the dead-store-id path had no recovery at all. Changes: backend `doudian-browser.service.ts` now throws structured `NotFoundException({code:'DOUDIAN_STORE_NOT_FOUND'})` for missing stores (and the upload path uses the same), and `http-exception.filter.ts` now passes a string business code through as `errorCode` (additive, numeric `code` kept for compatibility); added `backend/test/unit/http-exception-filter.spec.ts` (2/2 pass). Companion 3.2.83: `doudian_store_collector.py` parses `errorCode` first, adds `relink_companion_store()` (finds the unique same-name cloud store, never binds ambiguous matches), 404 upload/rebinb errors carry `DOUDIAN_STORE_NOT_FOUND`; `companion_app.py` upload recovery now: STALE 409 → rebind saved id as before; DOUDIAN_STORE_NOT_FOUND → rebind dead id (404) → relink by unique name → rebind → persist new `cloud_store_id` → retry upload; `_friendly_doudian_error` maps the new codes; GET `/api/doudian/stores` sanitizes legacy raw `login_message` (no more `400 Client Error`/URL/traceback anywhere). Backend deployed 17:47 via `deploy-backend-safe.py --execute --skip-build` (backup `/opt/matrixflow/releases/backend/20260821174715`, migrations clean, PM2 restart, health 200). Companion published 3.2.83: installer `451904776` bytes SHA256 `9be70d77…`, portable ZIP `348015869` bytes SHA256 `7fb8f256…`, manifest kind `portable`, both downloads HEAD `200`, Cloudflare cache purged; local `D:\Pixingyun` replaced (backup `D:\Pixingyun-backup-3282`), `/health` returns `3.2.83`; companion UI now shows the friendly message instead of the historical raw 400. No store order/product/aftersale data was touched; the next sync auto-relinks 唐商披星/披星教育 to the current cloud stores by unique name match.
- 2026-08-21 17:25 Asia/Shanghai: Deployed the post-audit defensive fix round (backend + frontend, no companion release, no schema change). Backend via `py -3 scripts\deploy-backend-safe.py --execute --skip-build`: backup `/opt/matrixflow/releases/backend/20260821172049`, Prisma generate ok, `migrate status` clean (`Database schema is up to date!`, 14 migrations), PM2 `matrixflow` restarted (pid 1462287), local and public health `200`. Backend content: analytics `getAccountDetailList` median×20 outlier zeroing removed (viral days no longer silently zeroed), WeChat Video period-metric `net_fans`/`new_fans` allow negative values, platforms reportMetrics delta fill allows negative followers increment, plus an `: string[]` annotation on the avatar-cache readdir. Frontend via `py -3 scripts\deploy-frontend-fast.py --skip-typecheck --allow-dirty-source`: public entry `assets/js/index-B2l8OQ1y.js` (DoudianView-DNnMrEBJ / DoudianSourceDetailView-BaEWSzzl / AccountListView-JozOfmeE / PerformanceLadderView-ClEX_uMp / MonetizationView-Cx1NYfz0 / WechatSourceDetailView-DtPtJG-c / MatrixDashboard-Bel0iOH1), remote backup `/tmp/matrixflow-frontend-dist-backup-20260821172134`, local/remote/origin/public refs all matched. Frontend content: DoudianView latest-request-wins requestId guard (prevents stale responses overwriting the newly selected store), removed duplicate `@change="loadData"` on store selects, sync failure alert no longer hidden when cached data exists; DoudianSourceDetailView same requestId guard; AccountListView batch delete now removes deleted ids from `selectedIds`; this deploy also ships the earlier local-only frontend fixes (duplicate data-status tag removal, ladder operator unification). Doudian/WeChat store counting semantics were deliberately left unchanged per operator instruction. Post-deploy `scripts/diagnose-production.py --remote` passed: DIAGNOSE OK (public health `200`, frontend ref matches, migrations up to date, PM2 online, data sanity 0 anomalies, api consistency ok).
- 2026-08-21 16:22 Asia/Shanghai: Published Pixingyun Mate 3.2.81 to repair the companion blank-window regression introduced during the login-template hotfix. Root cause was malformed Vue template closure in `companion_clean_ui.py`: the large outer `<template v-else>` still enclosed the startup `<script>`, so Vue never mounted and `v-cloak` kept the page hidden. Fixed template closure order, rebuilt PyInstaller onedir, installer, and portable ZIP, updated local `D:\Pixingyun`, and republished the public manifest. Public manifest `/companion-updates/latest.json` reports version `3.2.81`, kind `portable`, ZIP size `155770467`, ZIP SHA256 `48c139bbee7de53bb34abbb6bf538a880d27d8aa9ba5c91f41ab4ee9481c8358`, installer size `316163937`, and installer SHA256 `d5ea9470d1c3f06cdc48c8f67beb23254b84a4c4a9ae564983ae2c80f762021a`. Verification passed: Python compile, PyInstaller build, local `/health` version `3.2.81`, Playwright render shows the dashboard with `v-cloak` removed and `#app` displayed as grid, public manifest `200`, portable HEAD `200`, and installer HEAD `200`.
- 2026-08-21 16:06 Asia/Shanghai: Replaced the Pixingyun Mate companion hotfix with clean version 3.2.80 after local install validation found the 3.2.79 package could fail startup because `companion_state.py` had mojibake-corrupted platform-name string literals. Repaired platform names using Unicode escapes, rebuilt PyInstaller onedir, installer, and portable ZIP, republished public manifest, and updated local `D:\Pixingyun`. Public manifest `/companion-updates/latest.json` reports version `3.2.80`, kind `portable`, ZIP size `155768297`, ZIP SHA256 `e3231e88cc03c0b8e72ca1bf303cc1b72b8cd8bd72a5f521c449adb164af9af6`, installer size `316167178`, and installer SHA256 `5f5ff03d7af5828b3164447a512b84dcb6a648c0e80b074f967daefb07e0a1c8`. Verification passed: Python compile, PyInstaller build, local `/health` version `3.2.80`, local login page contains `v-cloak` and no raw loading ternary, public manifest `200`, ZIP HEAD `200`, and installer HEAD `200`.
- 2026-08-21 15:52 Asia/Shanghai: Published Pixingyun Mate 3.2.79 companion hotfix. Fixed the login page showing raw Vue template expressions on loading buttons by adding `v-cloak` and replacing ternary mustache button text with `v-if`/`v-else` spans. Built PyInstaller onedir full package, installer, and portable ZIP; public manifest `/companion-updates/latest.json` reports version `3.2.79`, kind `portable`, ZIP size `155768079`, ZIP SHA256 `dcea2103b24114b221285a78c2fa3f952ee6c9124eafad6b9b02b6d8c95752a5`, installer size `316166516`, and installer SHA256 `91747caece1e0e3738090b8fae3d5c730826797b4d30b8c26f35f197de303990`. Verification passed: Python compile, PyInstaller build, public manifest `200`, ZIP HEAD `200`, and installer HEAD `200`.
- 2026-08-21 15:26 Asia/Shanghai: Deployed frontend-only Performance Ladder order semantics fix. Ladder target/progress/ranking/store/source views now use fixed `totalOrderCount`, `validOrderCount`, and `refundedOrderCount` semantics, with goal metrics based on valid orders. Published frontend ref `assets/js/index-C31qaCg9.js`; public health, page ref, Worker route, migrations, PM2, and data sanity checks passed.
- 2026-08-21 15:08 Asia/Shanghai: Deployed Matrix Data period semantics fix. Backend safe deploy succeeded with Prisma migrate check (no pending migrations) and PM2 restart; frontend fast deploy published `assets/js/index-BsNnq0vR.js`. Production health, public page, Worker route, migrations, PM2, and API/data sanity checks passed via `scripts/diagnose-production.py --remote`.
# Deployment Log

Every production deploy, failed deploy, outage, rollback, or production repair must be recorded here.

- 2026-08-21 19:58 Asia/Shanghai: Deployed the 矩阵数据 period-data presentation and status-judgment fix (backend + frontend). Backend deployed with `py -3 scripts\deploy-backend-safe.py --execute`: Prisma migrations up to date, PM2 `matrixflow` restarted online, public health `200`, backup `/opt/matrixflow/releases/backend/20260821195815`. The backend change marks periods sourced from companion `periodMetrics` as `complete` with full covered days instead of mislabeling them 数据不完整 when DailyStats rows are sparse. Frontend deployed with `py -3 scripts\deploy-frontend-fast.py --allow-dirty-source`: entry bundle `assets/js/index-oCtutWHu.js`, dashboard chunk `assets/js/MatrixDashboard-k22JvzeF.js`; the script's verification step hit a paramiko socket timeout after the upload/replace had already completed, and manual verification passed (public HTML references `index-oCtutWHu.js`, public health `200`, remote backup `/tmp/matrixflow-frontend-dist-backup-20260821195858`, `matrixflow-frontend` container up). Frontend changes: historical rows visually weakened with an explicit 历史数据 label while partial rows display values normally, and the top-card drilldown drawer now separates 参与汇总 accounts from a collapsed 历史参考账号 group (not in the current-period sum). Top aggregation rule unchanged (complete + partial only).

- 2026-08-21 19:25 Asia/Shanghai: Published Pixingyun Mate `3.2.84`

- 2026-08-21 19:25 Asia/Shanghai: Published Pixingyun Mate `3.2.84` (Doudian auto-relink response-envelope fix). Release gates passed (compile 13 modules, encoding regression `13/13`, Windows compat `ok=True`); built with PyInstaller onedir `release-dist-onedir-3284-full` (bundled Chromium/WebView2/app icon), Inno installer, and portable ZIP (built sequentially after ISCC to avoid the concurrent file-lock issue). Local smoke: `D:\Pixingyun` replaced (old backed up to `D:\Pixingyun-backup-3283`), `/health` returns `3.2.84`. Publish incident: the installer publish script's remote cleanup bash exited 1 and left the server missing `pixingyun-mate-setup-3.2.84.exe` while `latest.json` already pointed at 3.2.84 (temporarily broken manifest); repaired manually via SFTP upload + rerunning the alias/cleanup steps, then published the portable ZIP normally (Cloudflare purge success). Final verification: public manifest `3.2.84` kind `portable`, ZIP `348016353` SHA256 `c79c269b`, installer `451901007` SHA256 `596aa2ab`, both downloads `206` with magic `504b0304` / `4d5a5000`, public health `200`, local companion `3.2.84` ok.

- 2026-08-21 19:05 Asia/Shanghai: Repaired the 唐商披星 / 披星教育 Doudian bindings (data repair, no code deploy). Root cause: the local companion held stale `cloud_store_id` values pointing to DoudianStore records deleted in a 2026-08-13 direct-DB rebuild (no audit rows), and the companion's by-name relink could not see the new stores because it parsed the wrapped API response as a bare array. Repair: re-bound both new cloud stores to the local profiles via `POST /doudian-browser/stores/:id/rebind` (REBIND_OK, storeId unchanged so history preserved), updated the local companion config cloud ids, cleared the stale website error display, and verified with real syncs: 唐商披星 `success` (600 orders / 10 products / 600 aftersales / 565 sources), 披星教育 `success` (132 orders / 2 products / 24 aftersales / 105 sources). No user re-login required.

- 2026-08-21 17:05 Asia/Shanghai: Published Pixingyun Mate `3.2.82` encoding release. Release gates passed: Python compile (12 modules), encoding regression `13/13`, Windows compatibility check `ok=True`. Built with PyInstaller onedir full output `release-dist-onedir-3282-full` (bundled Chromium, WebView2 DLLs, app icon), Inno Setup installer via `scripts\build-companion-installer.py`, portable ZIP via `Compress-Archive` (retried once after a concurrent-build file lock produced a 0-byte ZIP; final ZIP has 2101 entries and no sensitive local files). Local smoke: `D:\Pixingyun` replaced (old backed up to `D:\Pixingyun-backup-3281`), `/health` returns `3.2.82`, update check `available=false`. Published installer then portable with `scripts\publish-companion-download.py --execute`; Cloudflare cache purged after each. Public verification: manifest `3.2.82` kind `portable`, ZIP `348012126` bytes SHA256 `09858a7b`, installer `451894387` bytes SHA256 `d746e17d`, both downloads return `206` with magic bytes `504b0304` / `4d5a5000`, public health `200`.

- 2026-08-21 16:46 Asia/Shanghai: Deployed frontend-only release containing the JWT payload UTF-8 decoding fix (`utils/jwt.ts` now decodes base64url payloads through TextDecoder UTF-8 instead of atob latin1). Deployed with `py -3 scripts\deploy-frontend-fast.py --allow-dirty-source`; entry bundle `assets/js/index-CDX2ZDRS.js`; local/remote/origin/public refs matched; public health `200`. Remote backup: `/tmp/matrixflow-frontend-dist-backup-20260821164604`.

- 2026-08-21 16:16 Asia/Shanghai: Deployed frontend-only release containing the Performance Ladder source-operator unification and the Matrix Data duplicate data-status label removal. Ladder 出单来源排行 operators now resolve from account 主负责人 data (stable source ID match on `Account.platformUserId` first, unique normalized nickname second, legacy `sourceOperators` fallback last) with a read-only three-state display, and the account nickname cell no longer repeats the 数据状态 tag. Built and deployed with `py -3 scripts\deploy-frontend-fast.py --allow-dirty-source`; entry bundle `assets/js/index-BjSM6npV.js`, ladder chunk `assets/js/PerformanceLadderView-7Ca32XXo.js`, dashboard chunk `assets/js/MatrixDashboard-BKzS7d4d.js`. Verification passed: pre-deploy `diagnose-production.py` OK, local/remote/origin/public refs all matched `index-BjSM6npV.js`, public home `200`, public health `200`. Remote backup: `/tmp/matrixflow-frontend-dist-backup-20260821161615`.

Latest deployment note: at 2026-08-21 14:38 Asia/Shanghai, deployed the WeChat Store/Doudian total-order display update, Doudian companion stale-binding repair API, and Pixingyun Mate 3.2.78. The first backend deploy attempt failed during `prisma migrate deploy` because the production root disk was 100% full and Postgres was still in recovery (`database system is not yet accepting connections`); old `/tmp/matrixflow-*` deploy backups/temp directories were removed, freeing the root filesystem from 0 free space to about 55G free, then backend deploy succeeded with `py -3 scripts\deploy-backend-safe.py --execute --migrate --skip-build`. Prisma reported no pending migrations, PM2 `matrixflow` restarted, and `/api/v1/health` returned `200`. Frontend deployed with `py -3 scripts\deploy-frontend-fast.py --allow-dirty-source`; local/remote/origin/public bundle all matched `assets/js/index-CCWJSjbZ.js`, with remote frontend backup `/tmp/matrixflow-frontend-dist-backup-20260821142001`. Pixingyun Mate was bumped to `3.2.78`, built with PyInstaller onedir full output `desktop-companion/release-dist-onedir-3278-full/pixingyun-mate`, packaged as portable ZIP and Inno Setup installer, then published via `scripts\publish-companion-download.py`. Public manifest `/companion-updates/latest.json` now reports version `3.2.78`, kind `portable`, ZIP size `348069366`, ZIP SHA256 `5a5fcdf7b16fba4d6cb362213cf2f57ac6095bab6fbfe971a5a815098918b699`, installer size `451888143`, and installer SHA256 `08287539c9dcdddb2fabf638ab98bc3b893f653082934534ab5c10f5eeb76aa5`. Verification passed: public home `200`, public health `200`, manifest `200`, ZIP/installer HEAD `200`, PM2 online, and remote root disk about 50G free after deployment.

Latest deployment note: at 2026-08-21 12:14 Asia/Shanghai, deployed the account collection-status and organization-scoped Performance Ladder configuration update. Deployment order was database migration, backend, then frontend. Backend was deployed with `py -3 scripts\deploy-backend-safe.py --execute --migrate`, applying migration `202608210001_collect_status_and_ladder_config`, backing up backend to `/opt/matrixflow/releases/backend/20260821121009`, and restarting PM2 `matrixflow`. Frontend was deployed with `py -3 scripts\deploy-frontend-fast.py --allow-dirty-source`, preserving `downloads` and `companion-updates`, with remote/origin/public bundle `assets/js/index-BgEhD1XE.js` and frontend backup `/tmp/matrixflow-frontend-dist-backup-20260821121108`. Companion binaries/update manifest were not published. Verification passed: public health `200`, Cloudflare Worker route active, PM2 online, Prisma migrations up to date, data sanity/API consistency ok, and new Account collection fields are nullable on production.

Latest deployment note: at 2026-08-20 19:08 Asia/Shanghai, deployed a frontend-only Performance Ladder fix and removed the obsolete project `.npmrc` `frozen-lockfile=false` setting that npm 11 warned about. The release makes the ladder query the current month through today's end-of-day on each refresh and keeps refund counters independent from effective-order filtering, so top cards/source rows display the main order count plus the `go refund` count instead of only the refund-deducted quantity. No WeChat Store or Doudian page counting logic was changed. Verification passed: `npm config list --location=project` no longer warns about `frozen-lockfile`, frontend typecheck passed, frontend production build passed, frontend deploy passed, public home returned `200`, public HTML references `assets/js/index-YMYSWV9m.js`, and public `/api/v1/health` returned `200`. Remote frontend backup: `/tmp/matrixflow-frontend-dist-backup-20260820190545`.

Latest deployment note: at 2026-08-18 16:08 Asia/Shanghai, deployed dashboard readability fixes, Douyin badge color alignment, trend chart scaling, and backend collected-avatar caching. Frontend deployed with `py -3 scripts\deploy-frontend-fast.py --allow-dirty-source`; upload/origin replacement succeeded with entry bundle `assets/js/index-phpyqjAz.js`, dashboard chunk `assets/js/MatrixDashboard-BLhZ3wzn.js`, and platform badge chunk `assets/js/PlatformBadge-DGF35_5z.js`. The deploy script's first local public HTTPS verification timed out after the remote and origin refs already matched; manual verification then passed with public home `200`, public HTML referencing `assets/js/index-phpyqjAz.js`, public `/api/v1/health` `200`, and missing-avatar route `404`. Backend was minimally hot-patched in compiled production dist for avatar caching, with backups `/opt/matrixflow/backend/dist/modules/platforms/platforms.service.js.bak-202608181530` and `/opt/matrixflow/backend/dist/modules/platforms/platforms.controller.js.bak-202608181530`; `node --check` passed and PM2 `matrixflow` restarted successfully. Remote frontend backup: `/tmp/matrixflow-frontend-dist-backup-20260818160335`.

Latest production repair note: at 2026-08-18 15:10 Asia/Shanghai, repaired a WeChat Store/Video Shop order sync outage where date ranges after 2026-08-14 could show `0` orders. Production logs showed the 2026-08-14 WeChat sync failed with Postgres `No space left on device`, then the backend stayed in a stuck `syncRunning` state and scheduler runs skipped with `sync already running`. Repair: confirmed disk free space was healthy again, restarted PM2 backend to clear the in-memory sync lock, allowed startup sync to backfill WeChat orders, then hot-patched the compiled production WeChat store service with 30s WeChat API fetch timeouts, a 4-minute per-store sync timeout, and timeout failure-status marking. Backups before hotpatches: `/opt/matrixflow/backend/dist/modules/wechat-store/wechat-store.service.js.bak-202608181445` and `/opt/matrixflow/backend/dist/modules/wechat-store/wechat-store.service.js.bak-202608181505`. Verification: `node --check` passed on the patched compiled file, PM2 restart succeeded, public `/api/v1/health` returned `200`, all 3 WeChat stores ended `syncStatus=ok`, and recovered counts were `Tangshang Pixing` today `11` total / `9` effective and yesterday `33` / `25`, `Pixing Education` today `8` / `5` and yesterday `19` / `15`, `Pixing Culture` today `0` / `0` and yesterday `9` / `7`. No WeChat Store or Doudian page counting logic was changed for this repair.

Latest deployment note: at 2026-08-14 19:06 Asia/Shanghai, deployed a frontend-only `业绩天梯` 有效订单 口径 correction with `py -3 scripts\deploy-frontend-fast.py --allow-dirty-source`. The release makes the ladder default count conform to the store pages: 微信小店 excludes statuses `10`, `12`, `200`, and `250` without a shipped-time requirement; 抖店 uses the shared Doudian revenue/effective-order helper. The `去退款` switch is off by default and applies only an extra successful-refund deduction when selected. Build output deployed entry bundle `assets/js/index-0aA07nNK.js` with ladder chunk `assets/js/PerformanceLadderView-fDYB8eEx.js`. Verification passed: frontend typecheck, frontend production build, remote ref, origin ref, and public ref all matched `assets/js/index-0aA07nNK.js`. Remote backup: `/tmp/matrixflow-frontend-dist-backup-20260814190621`.

Latest deployment note: at 2026-08-14 18:52 Asia/Shanghai, deployed a frontend-only `业绩天梯` data口径 fix with `py -3 scripts\deploy-frontend-fast.py --allow-dirty-source`. The release corrects two mismatches found by comparing the ladder with existing source detail/ranking pages: WeChat source orders now require `ship_time > 0` so the ladder excludes待发货 orders, and Doudian source orders now reuse `getDoudianRevenueOrders` with `getDoudianSuccessfulRefundOrderIds` to match Doudian revenue/refund statistics. The UI chips now disclose `微信：已发货` and `抖店：营收订单`. Build output deployed entry bundle `assets/js/index-BcxfOKkl.js` with ladder chunk `assets/js/PerformanceLadderView-Bqr1RDqh.js`. Verification passed: frontend typecheck, frontend production build, remote ref, origin ref, and public ref all matched `assets/js/index-BcxfOKkl.js`. Remote backup: `/tmp/matrixflow-frontend-dist-backup-20260814185215`.

Latest deployment note: at 2026-08-14 18:44 Asia/Shanghai, deployed a frontend-only `业绩天梯` source-account detail drawer upgrade with `py -3 scripts\deploy-frontend-fast.py --allow-dirty-source`. The release replaces the previous repetitive store attribution rows in the source-account drawer with order-flow rows matching the existing source-detail pages: product image, product title, order time, status chip, shipped chip where available, order number, transaction source, and amount. WeChat Store rows use `product_price || pay_amount`, status labels, shipped state, and source type labels; Doudian rows use `pay_amount`, `doudianOrderStatus`, product image/title, and author source. Build output deployed entry bundle `assets/js/index-DDmI3lgo.js` with ladder chunk `assets/js/PerformanceLadderView-jtNIapHh.js`. Verification passed: frontend typecheck, frontend production build, remote ref, origin ref, and public ref all matched `assets/js/index-DDmI3lgo.js`. Remote backup: `/tmp/matrixflow-frontend-dist-backup-20260814184411`.

Latest deployment note: at 2026-08-14 18:33 Asia/Shanghai, deployed a frontend-only `业绩天梯` source-account attribution update with `py -3 scripts\deploy-frontend-fast.py --allow-dirty-source`. The release groups source ranking by normalized source account name instead of `platform:store:sourceKey`, so identical source account names are counted together across stores/platforms after teacher/platform/refund filters are applied. Top podium cards and source rows are now clickable and open a detail drawer. The drawer shows source-account order attribution and lets users edit the source account operator; mappings are persisted in browser localStorage as `performance-ladder-source-operators` and are also shown on cards/search. Build output deployed entry bundle `assets/js/index-C8p0v9R-.js` with ladder chunk `assets/js/PerformanceLadderView-CBRHnWtP.js`. Verification passed: frontend typecheck, frontend production build, remote ref, origin ref, and public ref all matched `assets/js/index-C8p0v9R-.js`. Remote backup: `/tmp/matrixflow-frontend-dist-backup-20260814183342`.

Latest deployment note: at 2026-08-14 18:21 Asia/Shanghai, deployed a frontend-only platform badge polish with `py -3 scripts\deploy-frontend-fast.py --allow-dirty-source`. The release changes Douyin from near-black to visible brand red on dark UI, uses TikTok cyan for the same shared color map, and updates `PlatformBadge` so labels and borders inherit the platform color instead of looking muted. The first deploy attempt failed before replacing production files because remote `/tmp` was full while creating the frontend backup/preserve scratch directory; old `/tmp/matrixflow-frontend-dist-backup-*`, `/tmp/matrixflow-release-fast-*`, and `/tmp/matrixflow-frontend-fast-*.tar.gz` scratch artifacts were removed, reducing root usage from 100% to 28%, then redeploy succeeded. Build output deployed entry bundle `assets/js/index-BosPr3E3.js` with account-list chunk `assets/js/AccountListView-tKN6KOZr.js` and platform badge chunk `assets/js/PlatformBadge-DfCfHhjk.js`. Verification passed: frontend typecheck, frontend production build, remote ref, origin ref, and public ref all matched `assets/js/index-BosPr3E3.js`. Remote backup: `/tmp/matrixflow-frontend-dist-backup-20260814182157`.

Latest deployment note: at 2026-08-14 18:09 Asia/Shanghai, deployed the frontend-only `业绩天梯` UI upgrade with `py -3 scripts\deploy-frontend-fast.py --allow-dirty-source` after one local pre-upload build failure. The first attempt failed during Vite SCSS compilation because `PerformanceLadderView.vue` referenced undefined `$bg-card`; no production files were uploaded. Fixed the variable to `$bg-elevated` and redeployed successfully. The release adds a sales-report hero, teacher goal cards, attribution-rule drawer, Top 3 podium ranking, compact source list, and clearer filter chips. Build output deployed entry bundle `assets/js/index-9tuJWhvN.js` with ladder chunk `assets/js/PerformanceLadderView-n8IqOQr-.js`. Verification passed: frontend typecheck, frontend production build, remote ref, origin ref, and public ref all matched `assets/js/index-9tuJWhvN.js`. Remote backup: `/tmp/matrixflow-frontend-dist-backup-20260814180954`.

Latest deployment note: at 2026-08-14 17:57 Asia/Shanghai, deployed a frontend-only `业绩天梯` refinement with `py -3 scripts\deploy-frontend-fast.py --allow-dirty-source`. The release moves the ladder to a standalone `商业转化` sidebar page, aggregates all WeChat Store/Video Account and Doudian stores for current-month effective orders, keeps the `去退款` toggle, filters mixed source data through editable teacher recognition keywords, defaults 卢慧/王晶 teacher profiles, hides unmatched sources by default, and gives the source-ranking Top 3 medal icons plus `销冠`/`Top2`/`Top3` tags. Build output deployed entry bundle `assets/js/index-C6hAelM7.js` with ladder chunk `assets/js/PerformanceLadderView-CMyH4dod.js`. Verification passed: frontend typecheck, frontend production build, remote ref, origin ref, and public ref all matched `assets/js/index-C6hAelM7.js`. Remote backup: `/tmp/matrixflow-frontend-dist-backup-20260814175723`.

Latest deployment note: at 2026-08-14 17:46 Asia/Shanghai, deployed the revised standalone `业绩天梯` frontend page with `py -3 scripts\deploy-frontend-fast.py --allow-dirty-source`. The release places `业绩天梯` under `商业转化` after `抖店`, aggregates all WeChat Store/Video Account and Doudian stores for current-month effective orders, adds a `去退款` toggle, defaults teacher targets for `卢慧` and `王晶` with add/remove support, and shows `出单来源排行` for source/teacher order comparisons. Build output deployed entry bundle `assets/js/index-NQcUHkDE.js` and ladder chunk `assets/js/PerformanceLadderView-D7C7i56P.js`. Verification passed: frontend typecheck, production build, remote ref, origin ref, and public ref all matched `assets/js/index-NQcUHkDE.js`. Remote backup: `/tmp/matrixflow-frontend-dist-backup-20260814174625`.

Latest deployment note: at 2026-08-14 17:31 Asia/Shanghai, deployed a frontend-only monetization dashboard update with `py -3 scripts\deploy-frontend-fast.py --allow-dirty-source`. The release adds `当月` and `自定义` filters plus the `业绩天梯` monthly order-target card to Doudian and WeChat Store/Video Account monetization pages. Build output deployed entry bundle `assets/js/index-sU2QtndJ.js` with Doudian chunk `assets/js/DoudianView-D2GvuWxy.js` and WeChat store chunk `assets/js/MonetizationView-wrrfFqPE.js`. Verification passed: frontend typecheck, frontend production build, remote ref, origin ref, and public ref all matched `assets/js/index-sU2QtndJ.js`. Remote backup: `/tmp/matrixflow-frontend-dist-backup-20260814173106`.

Latest deployment note: at `2026-07-27 18:30 Asia/Shanghai`, Pixingyun Mate `3.2.5` was rebuilt, packaged, and uploaded to the public download path to fix pywebview/pythonnet startup crashes on other PCs. The update manifest serves JSON at `/companion-updates/latest.json`, and `py -3 scripts\diagnose-production.py --remote --require-worker-route` passes.

| Time                           | Type                  | Actor | Scope                             | Commands / Actions                                                                                                                                                                                                                                                                                                                                                                                           | Verification                                                                                                                                                                                                                                                                   | Result                                                   | Follow-up                                                                                                                                     |
| ------------------------------ | --------------------- | ----- | --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-07-27 18:30 Asia/Shanghai | Companion hotfix | Codex | Pixingyun Mate startup compatibility | Fixed `webview_window.py` so pywebview/pythonnet initialization failures are caught and the companion falls back to the system browser instead of crashing before Flask starts. Bumped version to `3.2.5`, rebuilt with PyInstaller into `desktop-companion/release-dist-325`, created a clean portable ZIP excluding local user data, backed up the previous server ZIP, and uploaded the new public ZIP plus update manifest. | `py -3 -m py_compile companion_state.py webview_window.py companion_app.py`; PyInstaller build passed; public manifest returned version `3.2.5` and SHA256 `011c353169f1d154ce09152d600a57b8e0988add6819ef6dc3a7124d5af276bc`; public ZIP range request returned `206 application/zip`, `bytes 0-15/81276579`, magic `504b0304`; `py -3 scripts\diagnose-production.py --remote --require-worker-route` passed. | Success. | Ask affected users to download again from `/downloads/pixingyun-mate-portable.zip`; if their old companion cannot start, manual download is required. |
| 2026-07-27 17:52 Asia/Shanghai | Companion release | Codex | Pixingyun Mate public download + update manifest | Bumped Pixingyun Mate to `3.2.4`, rebuilt with PyInstaller into `desktop-companion/release-dist`, created a clean portable ZIP excluding logs, configs, local DB files, and browser profile data, backed up the old server ZIP, uploaded the new ZIP to `/opt/matrixflow/frontend-dist/downloads/pixingyun-mate-portable.zip`, and uploaded `/opt/matrixflow/frontend-dist/companion-updates/latest.json`. | Public manifest returned `200 application/json` with version `3.2.4`; public ZIP range request returned `206 application/zip`, `bytes 0-15/81277387`, magic `504b0304`; SHA256 `f5e8b8ad9383808678a80141571f5efab2f4781d28e9e758510ca24a95d09ff1`; `py -3 scripts\diagnose-production.py --remote --require-worker-route` passed with DB/API consistency ok. | Success. | Users on older companion versions will see the update when their companion checks the manifest; existing local saved data is excluded from overwrite by the updater. |
| 2026-07-27 12:00 Asia/Shanghai | Incident repair + reliability hardening | Codex | Production Cloudflare Tunnel + Worker | Public site returned Cloudflare `530/1033` and intermittent `502` while ECS origin, PM2, DB, Redis, and Nginx were healthy. Recreated `matrixflow-tunnel` through production compose; set `net.core.rmem_max=7500000` and `net.core.wmem_max=7500000`; pinned cloudflared to `--region us --edge-ip-version 4 --protocol quic` with Aliyun DNS resolvers; later restored `matrixflow-origin-proxy` Worker routes after a valid Cloudflare API token was provided. Worker uses `ORIGIN_HOST=ddddkiii.com` and `ORIGIN_RESOLVE_HOST=origin.ddddkiii.com`; direct ECS IP origin returned Cloudflare `1003` and was not kept. | `py -3 scripts\diagnose-production.py --remote --require-worker-route` passed; `ddddkiii.com` and `www.ddddkiii.com` homepage and health returned `200` with `x-matrixflow-entry=cloudflare-worker`; anonymous business endpoints returned `401`; companion ZIP range check returned `206`; `matrixflow-tunnel` healthy. | Success. | Tunnel guard remains installed but automatic tunnel recreation is disabled by default to avoid false-positive restarts from ECS-to-Cloudflare egress timeouts. |
| 2026-07-22 14:02 Asia/Shanghai | Production repair | Codex | Companion public download + diagnostics | Detected that `https://ddddkiii.com/downloads/pixingyun-mate-portable.zip` returned a `200` HTML fallback instead of a ZIP; rebuilt a clean portable package from `desktop-companion/dist/pixingyun-mate` excluding logs; uploaded it to `/opt/matrixflow/frontend-dist/downloads/pixingyun-mate-portable.zip`; hardened `scripts/diagnose-production.py` to request a byte range and require ZIP magic `PK\\x03\\x04`. | Remote file size `81592552`, SHA256 `0abe8f8caf573be21f436237d7241202165d82b6962f3c5497327251bf823fa7`, magic `504b0304`; public range request returned `206`, `application/zip`, `bytes 0-15/81592552`, Worker header `cloudflare-worker`; `py -3 -m py_compile scripts\diagnose-production.py scripts\deploy-cloudflare-worker.py`; `py -3 scripts\diagnose-production.py --remote --require-worker-route`; Chrome smoke for desktop/mobile `/` and `/login` passed. | Success. | Keep using ZIP magic/range validation for companion downloads; status-code-only checks are not sufficient. |
| 2026-07-22 13:36 Asia/Shanghai | Cloudflare route activation | Codex | Worker routes + Tunnel origin DNS | Created/updated `origin.ddddkiii.com` as a proxied CNAME to `750c57ed-6785-4c69-90c9-772c9043d96e.cfargotunnel.com`; deployed Worker `matrixflow-origin-proxy` with routes `ddddkiii.com/*` and `www.ddddkiii.com/*`; confirmed direct ECS IP HTTP path returns an ICP filing block and must not be used as Worker origin. | `npm exec -- wrangler deploy` succeeded and listed both routes; `py -3 scripts\deploy-cloudflare-worker.py --verify-only` passed; `py -3 scripts\diagnose-production.py --remote --require-worker-route` passed; public health `200` with `x-matrixflow-entry='cloudflare-worker'`; homepage/download `200`; anonymous business endpoints `401`; API/DB consistency `ok`. | Success. | Keep `origin.ddddkiii.com` pointed at the Tunnel target, not the ECS public IP. Future Cloudflare token should include Workers Scripts Edit, Workers Routes Edit, Zone Read, and DNS Read/Edit only when DNS automation is needed. |
| 2026-07-22 12:45 Asia/Shanghai | Diagnostics hardening | Codex | Production diagnostics + Cloudflare Worker deploy path | Added Worker marker-header checking to `scripts/diagnose-production.py`, including `--require-worker-route`; added production data sanity SQL to remote diagnostics; added `scripts/deploy-cloudflare-worker.py` for Wrangler dry-run/deploy/header verification; confirmed `--execute` is blocked by the current invalid Cloudflare token. | `py -3 -m py_compile scripts\diagnose-production.py scripts\deploy-cloudflare-worker.py`; `py -3 scripts\diagnose-production.py --remote` passed with data sanity counts (`13` accounts, `83` daily stats, `687` Doudian orders, all anomaly counters `0`); `py -3 scripts\diagnose-production.py --require-worker-route` failed as expected; Worker dry-run passed; `py -3 scripts\deploy-cloudflare-worker.py --verify-only` failed as expected. | Success for diagnostics and deploy path; Worker route still inactive. | Replace/fix `CLOUDFLARE_API_TOKEN` and rerun `py -3 scripts\deploy-cloudflare-worker.py --execute`, then require `x-matrixflow-entry: cloudflare-worker` in diagnostics. |
| 2026-07-22 12:49 Asia/Shanghai | Diagnostics hardening | Codex | Production API/DB consistency | Added read-only API/DB consistency checks to `scripts/diagnose-production.py`. The remote check creates an in-memory short-lived JWT from the server's current JWT secret, calls local production API endpoints, compares key dashboard/account/Doudian values with DB aggregates, and never prints the token. | `py -3 -m py_compile scripts\diagnose-production.py`; `py -3 scripts\diagnose-production.py --remote` passed. API values matched DB: overview accounts `13`, active accounts `5`, followers `5181340`, likes `22451828`, posts `5325`, published posts `5325`, accounts total `13`, Doudian store count `2`; Doudian API did not expose `profilePath`. | Success. | Keep expanding this check for new user-facing aggregate pages before relying on them in production. |
| 2026-07-22 12:35 Asia/Shanghai | Production infrastructure repair | Codex | Production DB/Redis, Cloudflare Tunnel, Worker diagnostics | Created PostgreSQL backup `/opt/matrixflow/backups/matrixflow-pre-portbind-20260722122520.sql.gz`; synced `/opt/matrixflow/docker-compose.yml`; copied Redis data from the old anonymous volume to stable `redisdata`; recreated `matrixflow-db` and `matrixflow-redis` with `127.0.0.1` port bindings; recreated `matrixflow-tunnel` with the same token/host network and no shell-form healthcheck; added backend npm test scripts; made production diagnostics UTF-8 safe on Windows; dry-ran the Worker bundle and checked Cloudflare Observability. | Frontend Vitest passed `7/7` files and `38/38` tests; backend Jest passed `27/27` suites and `285/285` tests; backend build passed; frontend build passed; `py -3 scripts\diagnose-production.py --remote` passed; DB/Redis were healthy on `127.0.0.1` only; Prisma migrations up to date; live `/` and `/login` Playwright smoke returned `200` with `pageErrors=0` and `requestFailed=0`; Wrangler dry-run passed; Worker Observability events for `matrixflow-origin-proxy` remained `0`. | Success for production site, DB/Redis binding, tunnel status, and diagnostics. | Cloudflare Worker route is still not active for `ddddkiii.com`; use a valid `CLOUDFLARE_API_TOKEN`/`CLOUDFLARE_ACCOUNT_ID` or Dashboard to attach `ddddkiii.com/*` and `www.ddddkiii.com/*`, then verify the `x-matrixflow-entry` header and non-zero Worker events. |
| 2026-07-19 00:51 Asia/Shanghai | Security hotfix + demo QA | Codex | Production backend + frontend + MCP keys | Masked MCP tokens in the page and config previews while preserving real copy actions; restricted MCP key/connection management to `SUPER_ADMIN`, `OWNER`, and `ADMIN`; deployed backend with `py -3 scripts\deploy-backend-safe.py --execute --skip-build`; deployed frontend with `py -3 scripts\deploy-frontend-fast.py --skip-typecheck --allow-dirty-source`; rotated `1` DB MCP key and `1` env MCP key exposed by the pre-fix screenshot. | MCP controller unit test passed `2/2`; full backend Jest passed `27` suites / `285` tests; backend build passed; frontend `npm run test:ci` passed `7` files / `38` tests; frontend build passed; production frontend ref matched `assets/js/index-CyJTUD2Q.js`; production MCP page returned `200`, `leakedTokenCount=0`, console/page errors `0`; `MEMBER` `GET /api/v1/mcp/keys` returned `403`; `py -3 scripts\diagnose-production.py --remote` passed. | Success. | Existing external MCP clients must use freshly copied keys because old keys were intentionally rotated. Avoid showing full copied config in chat/recording. |
| 2026-07-19 00:29 Asia/Shanghai | Demo readiness deploy | Codex | Production backend + frontend + auth/platform QA | Disabled public registration by default in production, hid the production register tab, added backend and frontend platform query propagation for dashboard overview/platform stats/comparison, added regression tests, then deployed backend and frontend. AI assistant remains outside the July 19 demo path. | Backend auth unit tests passed `24/24`; backend analytics unit tests passed `30/30`; full backend Jest passed `27` suites / `284` tests; backend build passed; frontend `npm run test:ci` passed `7` files / `38` tests; frontend build passed; production diagnose passed; public frontend ref matched `assets/js/index-DT8VBwB8.js`; public register returned `403`; login screenshot `login-registration-disabled.png`; authenticated `platform=DOUYIN` APIs returned only `DOUYIN`. | Success. | Demo only real data pages. Avoid production write/destructive buttons unless intentionally demonstrating a reversible flow. Keep DB/Redis container recreate and companion packaged-exe rebuild as post-demo hardening. |
| 2026-07-19 00:09 Asia/Shanghai | Demo readiness deploy | Codex | Production frontend + read-only QA | Removed the AI assistant's derived anomaly sample fill so the button only uses real follower trend data; kept backend AI unchanged because it is not part of the demo path. Deployed frontend with `PYTHONIOENCODING=utf-8 py scripts\deploy-frontend-fast.py --skip-typecheck --allow-dirty-source`. | Frontend `npm run test:ci` passed `7` files / `38` tests; frontend build passed; production frontend ref matched `assets/js/index-BZ3S_Tjz.js`; `py -3 scripts\diagnose-production.py --remote` passed; authenticated Playwright QA loaded dashboard, accounts, WeChat store, Doudian, team, permissions, profile, and admin without login redirects or page errors. | Success. | Do not click production write/destructive buttons during the demo. DB/Redis Docker containers still show internal `0.0.0.0` listens, but external probes to `5432/6379` fail; recreate containers after the demo to apply source `127.0.0.1` bindings. |
| 2026-07-18 22:34 Asia/Shanghai | Demo readiness hotfix | Codex | Production frontend + real-data QA | Allowed `SUPER_ADMIN` users to access team and permission routes, built and deployed the frontend with `PYTHONIOENCODING=utf-8 py scripts\deploy-frontend-fast.py --skip-typecheck --allow-dirty-source`. Added production diagnostic checks for the companion download package plus anonymous POST guards on Doudian upload/store creation and account cookie upload. | Frontend deployed and public ref matched `assets/js/index-GZd5vnNY.js`; frontend Vitest passed `7` files / `38` tests; production diagnose passed; authenticated read-only QA returned `13` accounts, `2` Doudian stores, public companion package `HEAD 200`; Playwright screenshots saved under `artifacts/qa-20260718-production`; account CSV export downloaded `账号列表_2026-07-18.csv`. | Success. | Use a real user login for the final human walkthrough; DB/Redis still show Docker public listen lines but external TCP probes to `5432/6379` fail and source compose now binds future recreates to `127.0.0.1`. |
| 2026-07-18 22:08 Asia/Shanghai | Reliability hotfix    | Codex | Production backend                | Stabilized backend health memory check to use process RSS versus system memory instead of volatile V8 heap ratio, added regression tests, fixed `scripts/deploy-backend-safe.py` to retry health after PM2 restart, then deployed with `PYTHONIOENCODING=utf-8 py scripts\deploy-backend-safe.py --execute --skip-build`. | Deploy script completed successfully and printed public health `data.status=ok`; `PYTHONIOENCODING=utf-8 py scripts\diagnose-production.py` passed; anonymous Doudian business-data endpoints returned `401`; backend Jest passed `26` suites / `277` tests; frontend Vitest passed `7` files / `38` tests. | Success. | Continue authenticated real-data UI validation with a fresh login; production Postgres/Redis port exposure remains an infrastructure hardening item. |
| 2026-07-18 22:00 Asia/Shanghai | Security hotfix       | Codex | Production backend                | Deployed backend with `PYTHONIOENCODING=utf-8 py scripts\deploy-backend-safe.py --execute` to remove anonymous Doudian browser cache access and stop returning companion profile paths. The script uploaded the archive, backed up remote backend files, ran Prisma generate and migrate status, and restarted PM2 `matrixflow`; it exited non-zero because the immediate local health curl hit startup timing (`exit 7`). | Follow-up remote diagnose showed PM2 `matrixflow` online on port `3000`, migrations up to date, local/origin/public health HTTP `200`; public health body returned `data.status=ok`; anonymous `GET /api/v1/doudian-browser/stores`, `shop/products`, and `shop/summary` returned `401` instead of the pre-deploy `200`. | Success after follow-up verification; deploy script reported a false-negative health timing failure. | Consider increasing the post-restart wait/retry loop in `scripts/deploy-backend-safe.py`; remaining infra risk: production Postgres/Redis Docker ports are bound on `0.0.0.0`. |
| 2026-07-17 11:50 Asia/Shanghai | Reliability deploy    | Codex | Production backend + frontend     | Ran deployment gate checks first: `git diff --check`, `npx tsc --noEmit --pretty false -p backend\tsconfig.json`, full backend Jest, backend build, frontend Vitest, frontend typecheck, and frontend build. Deployed backend with `PYTHONIOENCODING=utf-8 py scripts\deploy-backend-safe.py --execute`, then frontend with `PYTHONIOENCODING=utf-8 py scripts\deploy-frontend-fast.py --allow-dirty-source`. No DB migration or production dependency install was required. | Backend Jest passed `25` suites / `271` tests; frontend Vitest passed `7` files / `38` tests. Backend deploy verified PM2 `matrixflow` online and public health `HTTP 200` with version `1.0.0`. Frontend deploy matched local/remote/origin/public ref `assets/js/index-CTElgN0V.js`. Independent smoke checks returned public health `200` and homepage `HTTP 200`. | Success. | Monitor normal user flows; no immediate follow-up. |
| 2026-07-17 11:37 Asia/Shanghai | Reliability deploy    | Codex | Production backend + frontend + companion token | Deployed backend with `PYTHONIOENCODING=utf-8 py scripts\deploy-backend-safe.py --execute`, then deployed frontend with `PYTHONIOENCODING=utf-8 py scripts\deploy-frontend-fast.py --allow-dirty-source`. Refreshed the local Pixingyun companion saved token after backend deployment and started the rebuilt local companion exe. | Backend deploy printed `jwt_env=JWT_ACCESS_EXPIRES=1h JWT_REFRESH_EXPIRES=365d` and `jwt_dist=365d defaults verified`; remote read-only check showed `ENV_REFRESH=365d`, `DIST_7D_MATCHES=0`, `DIST_365D_MATCHES=2`, PM2 `online`; public health returned `200`; public frontend served `assets/js/index-CTElgN0V.js`; local companion refresh token now expires `2027-07-17 11:37:32 +08:00` (`365.0` days); local companion health returned `status=ok`, version `3.2.1`. | Success. | Run the next scan-bind from the currently started rebuilt companion and confirm the target account shows a fresh collection time after completion. |
| 2026-07-03 Asia/Shanghai       | Incident repair       | Codex | Production backend                | Rebuilt broken server dependencies, ran Prisma generate, restarted PM2 app `matrixflow`. Did not reset git, did not touch DB data, did not deploy frontend.                                                                                                                                                                                                                                                  | `https://ddddkiii.com/api/v1/health` returned `200`; PM2 app was online; nginx origin health returned `200`.                                                                                                                                                                   | Site recovered from API 502.                             | Add safe backend deploy script, disable automatic legacy deploy workflows, document the node_modules corruption failure mode.                 |
| 2026-07-03 Asia/Shanghai       | Governance change     | Codex | Local repository only             | Added `scripts/diagnose-production.py`; added `scripts/deploy-backend-safe.py`; converted legacy ECS deploy workflows to manual confirmation; tightened frontend deploy target.                                                                                                                                                                                                                              | `python -m py_compile scripts\diagnose-production.py scripts\deploy-backend-safe.py scripts\deploy-frontend-fast.py`; `python scripts\deploy-backend-safe.py --plan`; `python scripts\diagnose-production.py`; public health `200`. No production deployment command executed. | Deployment workflow now has safer official entry points. | Continue repo cleanup in small batches; do not delete files without explicit approval.                                                        |
| 2026-07-03 01:22 Asia/Shanghai | Incident repair       | Codex | Production frontend               | Public site was serving stale frontend bundle `assets/js/index-uL8uFGV0.js` and Doudian section was missing. Redeployed current frontend via `python scripts\deploy-frontend-fast.py --skip-typecheck --allow-dirty-source`.                                                                                                                                                                                 | Public `/?verify=front`, `/doudian?verify=front`, and `/monetization?verify=front` all served `assets/js/index-DatxT-TX.js`; entry bundle contained Doudian route and `DoudianView-DkKp-eK0.js`.                                                                               | Success.                                                 | Keep `LATEST_DEPLOYMENT.md` current before and after future deploys.                                                                          |
| 2026-07-03 01:33 Asia/Shanghai | Incident repair       | Codex | Production backend                | Doudian endpoints returned `Cannot read properties of undefined (reading 'findMany')` because the safe backend deploy script copied new `prisma` into the existing remote directory instead of replacing it, leaving the old schema active. Fixed `scripts/deploy-backend-safe.py` to delete remote `prisma` before copy, then ran `PYTHONIOENCODING=utf-8 python scripts\deploy-backend-safe.py --execute`. | Remote schema contains `DoudianStore*` models; remote Prisma Client exposes `doudianStore`, `doudianStoreOrder`, `doudianStoreProduct`, `doudianStoreAftersale`; public Doudian stores/products/orders/aftersale endpoints all returned `200`.                                 | Success.                                                 | Do not manually copy backend `prisma` with `cp -a` into an existing directory; use the fixed safe deploy script.                              |
| 2026-07-03 01:37 Asia/Shanghai | Data cleanup          | Codex | Production Doudian store registry | Deleted stale server-managed Doudian store `cmr0et26q07xect7mtmg9g1wh` directly with Prisma after public DELETE returned `401`. Guard required `profilePath` under `/opt/matrixflow/backend/doudian_profiles/`, `lastSyncedAt=null`, and zero orders/products/aftersales.                                                                                                                                    | Store counts before deletion were all `0`; after cleanup `GET /api/v1/doudian-browser/stores` returned only companion stores `cmr0qai7f1188xz13dm8ywxvk` and `cmr3fkxxc635cr5jhwi1q4rls`, both `syncStatus=ok`.                                                                | Success.                                                 | The deleted store had no business data; keep companion stores with `profilePath=companion:*`.                                                 |
| 2026-07-03 Asia/Shanghai       | Version cleanup check | Codex | Production frontend               | Checked retired public frontend bundles without changing production.                                                                                                                                                                                                                                                                                                                                         | Old refs `index-DkOeEzzm.js`, `index-CQ1-QhGH.js`, `index-vojAo1n-.js`, `index-uL8uFGV0.js`, and `index-BO3yyIfS.js` returned `404`; current `index-DatxT-TX.js` returned `200`.                                                                                               | Old public frontend versions are no longer served.       | Finish repository cleanup separately: commit/remove stale tracked `frontend/dist` entries and keep legacy deploy scripts blocked or archived. |
| 2026-07-03 Asia/Shanghai       | Repo cleanup          | Codex | Local repository only             | Removed legacy deployment and repair scripts plus legacy GitHub production deploy workflows from runnable repo paths. Did not SSH to production, did not deploy, did not restart services, and did not delete server files.                                                                                                                                                                                  | Official scripts still compile; removed representative files returned `False` in `Test-Path`; public health returned `200`.                                                                                                                                                    | Success.                                                 | Commit these deletions with the current deployment docs so the old entry points do not return.                                                |
| 2026-07-03 11:28 Asia/Shanghai | Safety backup         | Codex | Production backup                 | Created manual server backup at `/opt/matrixflow/backups/manual/20260703112845`. Included `frontend-dist.tar.gz`, `backend-runtime.tar.gz`, `postgres-pg_dumpall.sql.gz`, PM2/Docker/port snapshots, health snapshots, manifest, and SHA256 checksums.                                                                                                                                                       | `sha256sum -c SHA256SUMS.txt` passed; backup size about `3.0M`; frontend entry was `assets/js/index-DatxT-TX.js`; DB container was `matrixflow-db`; public health returned `200`.                                                                                              | Success.                                                 | Keep this backup until the next successful cleanup/deploy cycle has been verified.                                                            |
| 2026-07-16 19:02 Asia/Shanghai | Incident repair       | Codex | Production backend                | Deployed backend rate-limit repair with `py scripts\deploy-backend-safe.py --execute`. The script completed the remote build upload, backup, PM2 restart, and health check, then hit a local Windows GBK output-encoding error while printing remote output. No frontend deploy or DB migration was performed.                                                                                               | `npm run build --workspace=backend` passed; `https://ddddkiii.com/api/v1/health` returned `200`; PM2 app `matrixflow` was online after restart; local/origin health returned `200`; response headers showed `X-RateLimit-Limit-short: 100`; Prisma migrate status was up to date. | Success.                                                 | Consider making deploy script stdout encoding-safe on Windows so successful remote deploys do not report a local `UnicodeEncodeError`.          |
| 2026-07-16 19:13 Asia/Shanghai | Feature repair        | Codex | Production backend + frontend     | Added super-admin password reset for users, normalized auth/admin emails with `trim().toLowerCase()`, and deployed with `PYTHONIOENCODING=utf-8 py scripts\deploy-backend-safe.py --execute` plus `PYTHONIOENCODING=utf-8 py scripts\deploy-frontend-fast.py --allow-dirty-source`. No DB migration was required.                                                                                              | Backend and frontend builds passed; PM2 app `matrixflow` restarted online; backend health returned `200`; frontend public ref matched `assets/js/index-DlEMPpGR.js`; local/origin/public refs all matched.                                                                        | Success.                                                 | Use the new user-table `重置密码` action when a created user cannot log in or forgets their password.                                         |
| 2026-07-16 19:18 Asia/Shanghai | Feature repair        | Codex | Production backend + frontend     | Relaxed super-admin create/reset password policy to require only 8+ characters, fixed newly added admin password-reset messages, added frontend placeholder/client check, and redeployed backend plus frontend. No DB migration was required.                                                                                                                                                                  | Production logs showed the failed reset was a `400` password-policy rejection; backend and frontend builds passed; PM2 app `matrixflow` restarted online; backend health returned `200`; frontend public ref matched `assets/js/index-Casje4AU.js`.                            | Success.                                                 | Refresh the browser before retrying reset password so the latest admin bundle is loaded.                                                       |
| 2026-07-16 19:49 Asia/Shanghai | Feature deploy + incident | Codex | Production backend + frontend + tunnel | Added all-user dashboard issue alerts, readable frontend error-message fallback, super-admin login diagnosis and next-step prompts, then deployed backend and frontend. Backend deploy reached PM2/origin health but public verification failed; frontend deploy reached remote/origin bundle `assets/js/index-WuBRnYTC.js` but public verification failed. Replaced the tunnel container with the existing config/HTTP2 launch path while diagnosing. | `npm run build --workspace=backend` passed; `npm run build --workspace=frontend` passed; mojibake scan and `git diff --check` passed; PM2 app `matrixflow` was online; origin health returned `200`; origin frontend served `assets/js/index-WuBRnYTC.js`; public `https://ddddkiii.com` and `/api/v1/health` returned `530`; tunnel logs show `Unauthorized: Invalid tunnel secret`. | Origin deploy succeeded; public site blocked by Cloudflare Tunnel credential failure. | Regenerate or restore the valid Cloudflare Tunnel credential for tunnel `750c57ed-6785-4c69-90c9-772c9043d96e`, update `/root/.cloudflared`, restart `matrixflow-tunnel`, then recheck public health and frontend bundle. |
| 2026-07-16 22:34 Asia/Shanghai | Incident repair       | Codex | Production Cloudflare Tunnel      | Re-ran Cloudflare login with a writable container mount, installed `/root/.cloudflared/cert.pem`, fetched a fresh token for tunnel `750c57ed-6785-4c69-90c9-772c9043d96e`, backed up old token files, updated `/root/.cloudflared/tunnel-token.env`, `/root/.cloudflared/tunnel-token.txt`, `/opt/matrixflow/.env`, and `/opt/matrixflow/backend/.env`, then recreated `matrixflow-tunnel` with `--protocol http2`. Removed temporary login and quick tunnel containers. | Tunnel registered four HTTP/2 connections; recent tunnel logs no longer show `Invalid tunnel secret`; `https://ddddkiii.com/api/v1/health` returned `200`; `https://ddddkiii.com/` returned `200` and served `assets/js/index-WuBRnYTC.js`; origin health still returned `200`. | Success; public site recovered.                          | `www.ddddkiii.com/api/v1/health` returned `403`; main domain is healthy, but www should be checked separately if users rely on it.              |
| 2026-07-16 22:41 Asia/Shanghai | Reliability hardening | Codex | Production Cloudflare Tunnel      | Installed `/opt/matrixflow/scripts/tunnel-health-check.sh`, `/opt/matrixflow/scripts/tunnel-recover.sh`, and `/etc/cron.d/matrixflow-tunnel-guard` with `py scripts\install-tunnel-guard.py --execute`. Disabled legacy Cloudflare config/credentials files under `backups/legacy-disabled-*`, added `/root/.cloudflared/README.matrixflow`, and kept `/root/.cloudflared/tunnel-token.env` as the canonical tunnel credential source. | `py -m py_compile scripts\install-tunnel-guard.py` passed; manual health guard run logged `OK public=200`; cron file runs every minute; `/root/.cloudflared` contains only `cert.pem`, `README.matrixflow`, `tunnel-token.env`, and `tunnel-token.txt`; public health returned `200`; public frontend served `assets/js/index-WuBRnYTC.js`. | Success.                                                 | If public health fails 3 times and origin is healthy, check `/opt/matrixflow/logs/tunnel-health.log`; for `Invalid tunnel secret`, run `/opt/matrixflow/scripts/tunnel-recover.sh --login` then `--apply`. |
| 2026-07-16 22:56 Asia/Shanghai | Deep stability check  | Codex | Production backend + tunnel + www | Ran layered checks across public main domain, public `www`, origin, tunnel, PM2 backend, Docker containers, PostgreSQL, Redis, Nginx, cron guard, disk, and memory. Fixed backend asset packaging by changing `backend/nest-cli.json` asset paths so `sensitive-words.json` is included in `dist`, redeployed backend with `PYTHONIOENCODING=utf-8 py scripts\deploy-backend-safe.py --execute`, routed `www.ddddkiii.com` to tunnel `750c57ed-6785-4c69-90c9-772c9043d96e`, and upgraded the guard to log `secondary=200` for `www`. | Main and `www` health returned `200`; main and `www` frontends served `assets/js/index-WuBRnYTC.js`; origin main and origin `www` returned `200`; PM2 app `matrixflow` online; `matrixflow-tunnel` running; PostgreSQL `pg_isready` passed; Redis authenticated `PING` returned `PONG`; Nginx config test passed; guard log showed `OK public=200 secondary=200`; remote `sensitive-words.json` exists and loads. | Success.                                                 | The recurring cloudflared DNS resolver warning is non-blocking while public/origin health stay `200`; keep watching the guard log for actual public failures. |

## Entry Template

| Time                | Type                                  | Actor | Scope                           | Commands / Actions    | Verification                             | Result                     | Follow-up   |
| ------------------- | ------------------------------------- | ----- | ------------------------------- | --------------------- | ---------------------------------------- | -------------------------- | ----------- |
| YYYY-MM-DD HH:mm TZ | deploy / rollback / incident / repair | name  | frontend / backend / DB / infra | exact command summary | health checks, hashes, PM2/docker status | success / failed / partial | next action |
- 2026-08-26 18:00 Asia/Shanghai: Douyin identity hotfix shipped (backend + Pixingyun Mate 3.2.104; no frontend change). Backend deployed with `py -3 scripts/deploy-backend-safe.py --execute --migrate --skip-build`, no pending migrations, PM2 online, health 200, backup `/opt/matrixflow/releases/backend/20260826174146`. Companion fixes: creator-center visible identity is preferred for Douyin avatar/name, API avatar is fallback only, non-`MS4wLjAB...` ids are no longer used as `sec_user_id`, existing `platform_uid` is passed into collection, and Douyin UI card labels such as `作品发布`/`AI分身`/`发布图文` are rejected locally and by backend account/report-metrics guards. Release gates: Python `py_compile` passed for touched companion modules, backend `npm run build` passed, PyInstaller onedir build passed. Published 3.2.104: installer `452014528` B sha256 `e6fdaca21d9adb2f6650ecbad4bff4a665b80a543200b3aed9f8ae60ccbe550b`; portable ZIP `348104867` B sha256 `7ea91204d8fd6ae12e9e53d404c77e10f167efc43cbe67f562a664647d8126f9`; lite ZIP `157129000` B sha256 `1f7246f42b47e1b0ebf98e9d89cd403ea5a6eda2b61c338da0524d02b68d3684`; manifest version `3.2.104` kind `portable`. `COMPANION_LATEST_VERSION=3.2.104` set on production. Public verification: health 200, manifest 200, all versioned/stable download Range checks returned correct magic bytes (`PK` for ZIP, `MZ` for EXE). Also hardened `scripts/set-companion-latest-version.py` so `--help` no longer writes an invalid version to production.
- 2026-08-31 11:19 Asia/Shanghai: Companion in-app update download-speed repair for `3.2.107` manifest. Diagnosis: software update was slow because old companions read manifest `url`, which still pointed to the 348MB full portable ZIP; Python updater-style probes showed the full ZIP path at about `0.36MB/s`, while the already-published lite ZIP path reached about `2.8MB/s`. Direct ECS domain `dl.ddddkiii.com` resolves to the Aliyun instance but external HTTP/HTTPS is intercepted by Aliyun Beaver (`403` / TLS reset), and direct IP high-port testing was slower, so it was not used. Fix: uploaded `latest.json` and `latest-3.2.107.json` with `url`/`sha256`/`size` defaulting to `pixingyun-mate-lite-3.2.107.zip` (`157266112` B) while preserving the 348MB full package as `full_url`/`full_sha256`/`full_size`; Cloudflare cache was purged. Future publish script now emits lite-as-default manifests when a lite artifact is provided, and the companion updater understands `full_*` fallback metadata. The temporary `18080` direct-download Nginx config/UFW/security-group rule created for diagnosis was removed after testing.
- 2026-08-31 10:40 Asia/Shanghai: Published Pixingyun Mate `3.2.107` AI editor test release. Scope is companion-only: packaged the V2 `video_editor` module into both full and onedir builds, fixed FFmpeg pipe draining to avoid export hangs, lowered small-video disk-space floor, added cached Whisper CLI fallback, allowed MP4 export without ASR by returning a clear warning, and clarified AI editor readiness/errors in the clean UI. No account/store/collection/website business logic changed. Release artifacts: installer `pixingyun-mate-setup-3.2.107.exe` 452050302 B sha256 `259c37ada4dc19a53460c9ed16573a5b29cbc112087c8d3b6637ffc6d36480b7`; portable `pixingyun-mate-portable-3.2.107.zip` 348241979 B sha256 `6101e9ca28672de00e715224c0750318f6153097024e695c11a9186c0ecd5047`; lite `pixingyun-mate-lite-3.2.107.zip` 157266112 B sha256 `08a208fdfaeaf22ce7ae3b7014ed5825e279ecbecc3a201cbffe2e109697acca`. Published manifest `https://ddddkiii.com/companion-updates/latest.json` version `3.2.107`, stable installer alias `https://ddddkiii.com/downloads/pixingyun-mate-setup.exe`, and updated backend env `COMPANION_LATEST_VERSION=3.2.107` via PM2 restart. Verification passed: Python compile, video editor core tests, real MP4 smoke with cached Whisper and no-ASR fallback, ZIP/EXE magic bytes, public downloads, manifest version, and public health `200`.
