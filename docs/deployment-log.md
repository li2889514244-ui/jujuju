# Deployment Log

Every production deploy, failed deploy, outage, rollback, or production repair must be recorded here.

Latest deployment note: at 2026-08-20 19:08 Asia/Shanghai, deployed a frontend-only Performance Ladder fix and removed the obsolete project `.npmrc` `frozen-lockfile=false` setting that npm 11 warned about. The release makes the ladder query the current month through today's end-of-day on each refresh and keeps refund counters independent from effective-order filtering, so top cards/source rows display the main order count plus the `go refund` count instead of only the refund-deducted quantity. No WeChat Store or Doudian page counting logic was changed. Verification passed: `npm config list --location=project` no longer warns about `frozen-lockfile`, frontend typecheck passed, frontend production build passed, frontend deploy passed, public home returned `200`, public HTML references `assets/js/index-YMYSWV9m.js`, and public `/api/v1/health` returned `200`. Remote frontend backup: `/tmp/matrixflow-frontend-dist-backup-20260820190545`.

Latest deployment note: at 2026-08-18 16:08 Asia/Shanghai, deployed dashboard readability fixes, Douyin badge color alignment, trend chart scaling, and backend collected-avatar caching. Frontend deployed with `py -3 scripts\deploy-frontend-fast.py --allow-dirty-source`; upload/origin replacement succeeded with entry bundle `assets/js/index-phpyqjAz.js`, dashboard chunk `assets/js/MatrixDashboard-BLhZ3wzn.js`, and platform badge chunk `assets/js/PlatformBadge-DGF35_5z.js`. The deploy script's first local public HTTPS verification timed out after the remote and origin refs already matched; manual verification then passed with public home `200`, public HTML referencing `assets/js/index-phpyqjAz.js`, public `/api/v1/health` `200`, and missing-avatar route `404`. Backend was minimally hot-patched in compiled production dist for avatar caching, with backups `/opt/matrixflow/backend/dist/modules/platforms/platforms.service.js.bak-202608181530` and `/opt/matrixflow/backend/dist/modules/platforms/platforms.controller.js.bak-202608181530`; `node --check` passed and PM2 `matrixflow` restarted successfully. Remote frontend backup: `/tmp/matrixflow-frontend-dist-backup-20260818160335`.

Latest production repair note: at 2026-08-18 15:10 Asia/Shanghai, repaired a WeChat Store/Video Shop order sync outage where date ranges after 2026-08-14 could show `0` orders. Production logs showed the 2026-08-14 WeChat sync failed with Postgres `No space left on device`, then the backend stayed in a stuck `syncRunning` state and scheduler runs skipped with `sync already running`. Repair: confirmed disk free space was healthy again, restarted PM2 backend to clear the in-memory sync lock, allowed startup sync to backfill WeChat orders, then hot-patched the compiled production WeChat store service with 30s WeChat API fetch timeouts, a 4-minute per-store sync timeout, and timeout failure-status marking. Backups before hotpatches: `/opt/matrixflow/backend/dist/modules/wechat-store/wechat-store.service.js.bak-202608181445` and `/opt/matrixflow/backend/dist/modules/wechat-store/wechat-store.service.js.bak-202608181505`. Verification: `node --check` passed on the patched compiled file, PM2 restart succeeded, public `/api/v1/health` returned `200`, all 3 WeChat stores ended `syncStatus=ok`, and recovered counts were `Tangshang Pixing` today `11` total / `9` effective and yesterday `33` / `25`, `Pixing Education` today `8` / `5` and yesterday `19` / `15`, `Pixing Culture` today `0` / `0` and yesterday `9` / `7`. No WeChat Store or Doudian page counting logic was changed for this repair.

Latest deployment note: at 2026-08-14 19:06 Asia/Shanghai, deployed a frontend-only `业绩天梯` 有效订单 口径 correction with `py -3 scripts\deploy-frontend-fast.py --allow-dirty-source`. The release makes the ladder default count conform to the store pages: 微信小店 excludes statuses `10`, `12`, `200`, and `250` without a shipped-time requirement; 抖店 uses the shared Doudian revenue/effective-order helper. The `去退款` switch is off by default and applies only an extra successful-refund deduction when selected. Build output deployed entry bundle `assets/js/index-0aA07nNK.js` with ladder chunk `assets/js/PerformanceLadderView-fDYB8eEx.js`. Verification passed: frontend typecheck, frontend production build, remote ref, origin ref, and public ref all matched `assets/js/index-0aA07nNK.js`. Remote backup: `/tmp/matrixflow-frontend-dist-backup-20260814190621`.

Latest deployment note: at 2026-08-14 18:52 Asia/Shanghai, deployed a frontend-only `业绩天梯` data口径 fix with `py -3 scripts\deploy-frontend-fast.py --allow-dirty-source`. The release corrects two mismatches found by comparing the ladder with existing source detail/ranking pages: WeChat source orders now require `ship_time > 0` so the ladder excludes待发货 orders, and Doudian source orders now reuse `getDoudianRevenueOrders` with `getDoudianSuccessfulRefundOrderIds` to match Doudian revenue/refund statistics. The UI chips now disclose `微信：已发货` and `抖店：营收订单`. Build output deployed entry bundle `assets/js/index-BcxfOKkl.js` with ladder chunk `assets/js/PerformanceLadderView-Bqr1RDqh.js`. Verification passed: frontend typecheck, frontend production build, remote ref, origin ref, and public ref all matched `assets/js/index-BcxfOKkl.js`. Remote backup: `/tmp/matrixflow-frontend-dist-backup-20260814185215`.

Latest deployment note: at 2026-08-14 18:44 Asia/Shanghai, deployed a frontend-only `业绩天梯` source-account detail drawer upgrade with `py -3 scripts\deploy-frontend-fast.py --allow-dirty-source`. The release replaces the previous repetitive store attribution rows in the source-account drawer with order-flow rows matching the existing source-detail pages: product image, product title, order time, status chip, shipped chip where available, order number, transaction source, and amount. WeChat Store rows use `product_price || pay_amount`, status labels, shipped state, and source type labels; Doudian rows use `pay_amount`, `doudianOrderStatus`, product image/title, and author source. Build output deployed entry bundle `assets/js/index-DDmI3lgo.js` with ladder chunk `assets/js/PerformanceLadderView-jtNIapHh.js`. Verification passed: frontend typecheck, frontend production build, remote ref, origin ref, and public ref all matched `assets/js/index-DDmI3lgo.js`. Remote backup: `/tmp/matrixflow-frontend-dist-backup-20260814184411`.

Latest deployment note: at 2026-08-14 18:33 Asia/Shanghai, deployed a frontend-only `业绩天梯` source-account attribution update with `py -3 scripts\deploy-frontend-fast.py --allow-dirty-source`. The release groups source ranking by normalized source account name instead of `platform:store:sourceKey`, so identical source account names are counted together across stores/platforms after teacher/platform/refund filters are applied. Top podium cards and source rows are now clickable and open a detail drawer. The drawer shows source-account order attribution and lets users edit the source account operator; mappings are persisted in browser localStorage as `performance-ladder-source-operators` and are also shown on cards/search. Build output deployed entry bundle `assets/js/index-C8p0v9R-.js` with ladder chunk `assets/js/PerformanceLadderView-CBRHnWtP.js`. Verification passed: frontend typecheck, frontend production build, remote ref, origin ref, and public ref all matched `assets/js/index-C8p0v9R-.js`. Remote backup: `/tmp/matrixflow-frontend-dist-backup-20260814183342`.

Latest deployment note: at 2026-08-14 18:21 Asia/Shanghai, deployed a frontend-only platform badge polish with `py -3 scripts\deploy-frontend-fast.py --allow-dirty-source`. The release changes Douyin from near-black to visible brand red on dark UI, uses TikTok cyan for the same shared color map, and updates `PlatformBadge` so labels and borders inherit the platform color instead of looking muted. The first deploy attempt failed before replacing production files because remote `/tmp` was full while creating the frontend backup/preserve scratch directory; old `/tmp/matrixflow-frontend-dist-backup-*`, `/tmp/matrixflow-release-fast-*`, and `/tmp/matrixflow-frontend-fast-*.tar.gz` scratch artifacts were removed, reducing root usage from 100% to 28%, then redeploy succeeded. Build output deployed entry bundle `assets/js/index-BosPr3E3.js` with account-list chunk `assets/js/AccountListView-tKN6KOZr.js` and platform badge chunk `assets/js/PlatformBadge-DfCfHhjk.js`. Verification passed: frontend typecheck, frontend production build, remote ref, origin ref, and public ref all matched `assets/js/index-BosPr3E3.js`. Remote backup: `/tmp/matrixflow-frontend-dist-backup-20260814182157`.

Latest deployment note: at 2026-08-14 18:09 Asia/Shanghai, deployed the frontend-only `业绩天梯` UI upgrade with `py -3 scripts\deploy-frontend-fast.py --allow-dirty-source` after one local pre-upload build failure. The first attempt failed during Vite SCSS compilation because `PerformanceLadderView.vue` referenced undefined `$bg-card`; no production files were uploaded. Fixed the variable to `$bg-elevated` and redeployed successfully. The release adds a sales-report hero, teacher goal cards, attribution-rule drawer, Top 3 podium ranking, compact source list, and clearer filter chips. Build output deployed entry bundle `assets/js/index-9tuJWhvN.js` with ladder chunk `assets/js/PerformanceLadderView-n8IqOQr-.js`. Verification passed: frontend typecheck, frontend production build, remote ref, origin ref, and public ref all matched `assets/js/index-9tuJWhvN.js`. Remote backup: `/tmp/matrixflow-frontend-dist-backup-20260814180954`.

Latest deployment note: at 2026-08-14 17:57 Asia/Shanghai, deployed a frontend-only `业绩天梯` refinement with `py -3 scripts\deploy-frontend-fast.py --allow-dirty-source`. The release moves the ladder to a standalone `商业转化` sidebar page, aggregates all WeChat Store/Video Account and Doudian stores for current-month effective orders, keeps the `去退款` toggle, filters mixed source data through editable teacher recognition keywords, defaults 卢慧/王晶 teacher profiles, hides unmatched sources by default, and gives the source-ranking Top 3 medal icons plus `销冠`/`Top2`/`Top3` tags. Build output deployed entry bundle `assets/js/index-C6hAelM7.js` with ladder chunk `assets/js/PerformanceLadderView-CMyH4dod.js`. Verification passed: frontend typecheck, frontend production build, remote ref, origin ref, and public ref all matched `assets/js/index-C6hAelM7.js`. Remote backup: `/tmp/matrixflow-frontend-dist-backup-20260814175723`.

Latest deployment note: at 2026-08-14 17:46 Asia/Shanghai, deployed the revised standalone `业绩天梯` frontend page with `py -3 scripts\deploy-frontend-fast.py --allow-dirty-source`. The release places `业绩天梯` under `商业转化` after `抖店`, aggregates all WeChat Store/Video Account and Doudian stores for current-month effective orders, adds a `去退款` toggle, defaults teacher targets for `卢慧` and `王晶` with add/remove support, and shows `出单来源排行` for source/teacher order comparisons. Build output deployed entry bundle `assets/js/index-NQcUHkDE.js` and ladder chunk `assets/js/PerformanceLadderView-D7C7i56P.js`. Verification passed: frontend typecheck, production build, remote ref, origin ref, and public ref all matched `assets/js/index-NQcUHkDE.js`. Remote backup: `/tmp/matrixflow-frontend-dist-backup-20260814174625`.

Latest deployment note: at 2026-08-14 17:31 Asia/Shanghai, deployed a frontend-only monetization dashboard update with `py -3 scripts\deploy-frontend-fast.py --allow-dirty-source`. The release adds `当月` and `自定义` filters plus the `业绩天梯` monthly order-target card to Doudian and WeChat Store/Video Account monetization pages. Build output deployed entry bundle `assets/js/index-sU2QtndJ.js` with Doudian chunk `assets/js/DoudianView-D2GvuWxy.js` and WeChat store chunk `assets/js/MonetizationView-wrrfFqPE.js`. Verification passed: frontend typecheck, frontend production build, remote ref, origin ref, and public ref all matched `assets/js/index-sU2QtndJ.js`. Remote backup: `/tmp/matrixflow-frontend-dist-backup-20260814173106`.

Latest deployment note: at `2026-07-27 18:30 Asia/Shanghai`, Pixingyun Mate `3.2.5` was rebuilt, packaged, and uploaded to the public download path to fix pywebview/pythonnet startup crashes on other PCs. The update manifest serves JSON at `/companion-updates/latest.json`, and `py -3 scripts\diagnose-production.py --remote --require-worker-route` passes.

| Time                           | Type                  | Actor | Scope                             | Commands / Actions                                                                                                                                                                                                                                                                                                                                                                                           | Verification                                                                                                                                                                                                                                                                   | Result                                                   | Follow-up                                                                                                                                     |
| ------------------------------ | --------------------- | ----- | --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-07-27 18:30 Asia/Shanghai | Companion hotfix | Codex | Pixingyun Mate startup compatibility | Fixed `webview_window.py` so pywebview/pythonnet initialization failures are caught and the companion falls back to the system browser instead of crashing before Flask starts. Bumped version to `3.2.5`, rebuilt with PyInstaller into `desktop-companion/release-dist-325`, created a clean portable ZIP excluding local user data, backed up the previous server ZIP, and uploaded the new public ZIP plus update manifest. | `py -3 -m py_compile companion_state.py webview_window.py companion_app.py`; PyInstaller build passed; public manifest returned version `3.2.5` and SHA256 `011c353169f1d154ce09152d600a57b8e0988add6819ef6dc3a7124d5af276bc`; public ZIP range request returned `206 application/zip`, `bytes 0-15/81276579`, magic `504b0304`; `py -3 scripts\diagnose-production.py --remote --require-worker-route` passed. | Success. | Ask affected users to download again from `/downloads/pixingyun-mate-portable.zip`; if their old companion cannot start, manual download is required. |
| 2026-07-27 17:52 Asia/Shanghai | Companion release | Codex | Pixingyun Mate public download + update manifest | Bumped Pixingyun Mate to `3.2.4`, rebuilt with PyInstaller into `desktop-companion/release-dist`, created a clean portable ZIP excluding logs, configs, local DB files, and browser profile data, backed up the old server ZIP, uploaded the new ZIP to `/opt/matrixflow/frontend-dist/downloads/pixingyun-mate-portable.zip`, and uploaded `/opt/matrixflow/frontend-dist/companion-updates/latest.json`. | Public manifest returned `200 application/json` with version `3.2.4`; public ZIP range request returned `206 application/zip`, `bytes 0-15/81277387`, magic `504b0304`; SHA256 `f5e8b8ad9383808678a80141571f5efab2f4781d28e9e758510ca24a95d09ff1`; `py -3 scripts\diagnose-production.py --remote --require-worker-route` passed with DB/API consistency ok. | Success. | Users on older companion versions will see the update when their companion checks the manifest; existing local saved data is excluded from overwrite by the updater. |
| 2026-07-27 12:00 Asia/Shanghai | Incident repair + reliability hardening | Codex | Production Cloudflare Tunnel + Worker | Public site returned Cloudflare `530/1033` and intermittent `502` while ECS origin, PM2, DB, Redis, and Nginx were healthy. Recreated `matrixflow-tunnel` through production compose; set `net.core.rmem_max=7500000` and `net.core.wmem_max=7500000`; pinned cloudflared to `--region us --edge-ip-version 4 --protocol quic` with Aliyun DNS resolvers; later restored `matrixflow-origin-proxy` Worker routes after a valid Cloudflare API token was provided. Worker uses `ORIGIN_HOST=ddddkiii.com` and `ORIGIN_RESOLVE_HOST=origin.ddddkiii.com`; direct ECS IP origin returned Cloudflare `1003` and was not kept. | `py -3 scripts\diagnose-production.py --remote --require-worker-route` passed; `ddddkiii.com` and `www.ddddkiii.com` homepage and health returned `200` with `x-matrixflow-entry=cloudflare-worker`; anonymous business endpoints returned `401`; companion ZIP range check returned `206`; `matrixflow-tunnel` healthy. | Success. | Tunnel guard remains installed but automatic tunnel recreation is disabled by default to avoid false-positive restarts from ECS-to-Cloudflare egress timeouts. |
| 2026-07-22 14:02 Asia/Shanghai | Production repair | Codex | Companion public download + diagnostics | Detected that `https://ddddkiii.com/downloads/pixingyun-mate-portable.zip` returned a `200` HTML fallback instead of a ZIP; rebuilt a clean portable package from `desktop-companion/dist/pixingyun-mate` excluding logs; uploaded it to `/opt/matrixflow/frontend-dist/downloads/pixingyun-mate-portable.zip`; hardened `scripts/diagnose-production.py` to request a byte range and require ZIP magic `PK\\x03\\x04`. | Remote file size `81592552`, SHA256 `0abe8f8caf573be21f436237d7241202165d82b6962f3c5497327251bf823fa7`, magic `504b0304`; public range request returned `206`, `application/zip`, `bytes 0-15/81592552`, Worker header `cloudflare-worker`; `py -3 -m py_compile scripts\diagnose-production.py scripts\deploy-cloudflare-worker.py`; `py -3 scripts\diagnose-production.py --remote --require-worker-route`; Chrome smoke for desktop/mobile `/` and `/login` passed. | Success. | Keep using ZIP magic/range validation for companion downloads; status-code-only checks are not sufficient. |
| 2026-07-22 13:36 Asia/Shanghai | Cloudflare route activation | Codex | Worker routes + Tunnel origin DNS | Created/updated `origin.ddddkiii.com` as a proxied CNAME to `750c57ed-6785-4c69-90c9-772c9043d96e.cfargotunnel.com`; deployed Worker `matrixflow-origin-proxy` with routes `ddddkiii.com/*` and `www.ddddkiii.com/*`; confirmed direct ECS IP HTTP path returns an ICP filing block and must not be used as Worker origin. | `npm exec -- wrangler deploy` succeeded and listed both routes; `py -3 scripts\deploy-cloudflare-worker.py --verify-only` passed; `py -3 scripts\diagnose-production.py --remote --require-worker-route` passed; public health `200` with `x-matrixflow-entry='cloudflare-worker'`; homepage/download `200`; anonymous business endpoints `401`; API/DB consistency `ok`. | Success. | Keep `origin.ddddkiii.com` pointed at the Tunnel target, not the ECS public IP. Future Cloudflare token should include Workers Scripts Edit, Workers Routes Edit, Zone Read, and DNS Read/Edit only when DNS automation is needed. |
| 2026-07-22 12:45 Asia/Shanghai | Diagnostics hardening | Codex | Production diagnostics + Cloudflare Worker deploy path | Added Worker marker-header checking to `scripts/diagnose-production.py`, including `--require-worker-route`; added production data sanity SQL to remote diagnostics; added `scripts/deploy-cloudflare-worker.py` for Wrangler dry-run/deploy/header verification; confirmed `--execute` is blocked by the current invalid Cloudflare token. | `py -3 -m py_compile scripts\diagnose-production.py scripts\deploy-cloudflare-worker.py`; `py -3 scripts\diagnose-production.py --remote` passed with data sanity counts (`13` accounts, `83` daily stats, `687` Doudian orders, all anomaly counters `0`); `py -3 scripts\diagnose-production.py --require-worker-route` failed as expected; Worker dry-run passed; `py -3 scripts\deploy-cloudflare-worker.py --verify-only` failed as expected. | Success for diagnostics and deploy path; Worker route still inactive. | Replace/fix `CLOUDFLARE_API_TOKEN` and rerun `py -3 scripts\deploy-cloudflare-worker.py --execute`, then require `x-matrixflow-entry: cloudflare-worker` in diagnostics. |
| 2026-07-22 12:49 Asia/Shanghai | Diagnostics hardening | Codex | Production API/DB consistency | Added read-only API/DB consistency checks to `scripts/diagnose-production.py`. The remote check creates an in-memory short-lived JWT from the server's current JWT secret, calls local production API endpoints, compares key dashboard/account/Doudian values with DB aggregates, and never prints the token. | `py -3 -m py_compile scripts\diagnose-production.py`; `py -3 scripts\diagnose-production.py --remote` passed. API values matched DB: overview accounts `13`, active accounts `5`, followers `5181340`, likes `22451828`, posts `5325`, published posts `5325`, accounts total `13`, Doudian store count `2`; Doudian API did not expose `profilePath`. | Success. | Keep expanding this check for new user-facing aggregate pages before relying on them in production. |
| 2026-07-22 12:35 Asia/Shanghai | Production infrastructure repair | Codex | Production DB/Redis, Cloudflare Tunnel, Worker diagnostics | Created PostgreSQL backup `/opt/matrixflow/backups/matrixflow-pre-portbind-20260722122520.sql.gz`; synced `/opt/matrixflow/docker-compose.yml`; copied Redis data from the old anonymous volume to stable `redisdata`; recreated `matrixflow-db` and `matrixflow-redis` with `127.0.0.1` port bindings; recreated `matrixflow-tunnel` with the same token/host network and no shell-form healthcheck; added backend npm test scripts; made production diagnostics UTF-8 safe on Windows; dry-ran the Worker bundle and checked Cloudflare Observability. | Frontend Vitest passed `7/7` files and `38/38` tests; backend Jest passed `27/27` suites and `285/285` tests; backend build passed; frontend build passed; `py -3 scripts\diagnose-production.py --remote` passed; DB/Redis were healthy on `127.0.0.1` only; Prisma migrations up to date; live `/` and `/login` Playwright smoke returned `200` with `pageErrors=0` and `requestFailed=0`; Wrangler dry-run passed; Worker Observability events for `matrixflow-origin-proxy` remained `0`. | Success for production site, DB/Redis binding, tunnel status, and diagnostics. | Cloudflare Worker route is still not active for `ddddkiii.com`; use a valid `CLOUDFLARE_API_TOKEN`/`CLOUDFLARE_ACCOUNT_ID` or Dashboard to attach `ddddkiii.com/*` and `www.ddddkiii.com/*`, then verify the `x-matrixflow-entry` header and non-zero Worker events. |
| 2026-07-19 00:51 Asia/Shanghai | Security hotfix + demo QA | Codex | Production backend + frontend + MCP keys | Masked MCP tokens in the page and config previews while preserving real copy actions; restricted MCP key/connection management to `SUPER_ADMIN`, `OWNER`, and `ADMIN`; deployed backend with `py -3 scripts\deploy-backend-safe.py --execute --skip-build`; deployed frontend with `py -3 scripts\deploy-frontend-fast.py --skip-typecheck --allow-dirty-source`; rotated `1` DB MCP key and `1` env MCP key exposed by the pre-fix screenshot. | MCP controller unit test passed `2/2`; full backend Jest passed `27` suites / `285` tests; backend build passed; frontend `npm run test:ci` passed `7` files / `38` tests; frontend build passed; production frontend ref matched `assets/js/index-CyJTUD2Q.js`; production MCP page returned `200`, `leakedTokenCount=0`, console/page errors `0`; `MEMBER` `GET /api/v1/mcp/keys` returned `403`; `py -3 scripts\diagnose-production.py --remote` passed. | Success. | Existing external MCP clients must use freshly copied keys because old keys were intentionally rotated. Avoid showing full copied config in chat/recording. |
| 2026-07-19 00:29 Asia/Shanghai | Demo readiness deploy | Codex | Production backend + frontend + auth/platform QA | Disabled public registration by default in production, hid the production register tab, added backend and frontend platform query propagation for dashboard overview/platform stats/comparison, added regression tests, then deployed backend and frontend. AI assistant remains outside the July 19 demo path. | Backend auth unit tests passed `24/24`; backend analytics unit tests passed `30/30`; full backend Jest passed `27` suites / `284` tests; backend build passed; frontend `npm run test:ci` passed `7` files / `38` tests; frontend build passed; production diagnose passed; public frontend ref matched `assets/js/index-DT8VBwB8.js`; public register returned `403`; login screenshot `login-registration-disabled.png`; authenticated `platform=DOUYIN` APIs returned only `DOUYIN`. | Success. | Demo only real data pages. Avoid production write/destructive buttons unless intentionally demonstrating a reversible flow. Keep DB/Redis container recreate and companion packaged-exe rebuild as post-demo hardening. |
| 2026-07-19 00:09 Asia/Shanghai | Demo readiness deploy | Codex | Production frontend + read-only QA | Removed the AI assistant's derived anomaly sample fill so the button only uses real follower trend data; kept backend AI unchanged because it is not part of the demo path. Deployed frontend with `PYTHONIOENCODING=utf-8 py scripts\deploy-frontend-fast.py --skip-typecheck --allow-dirty-source`. | Frontend `npm run test:ci` passed `7` files / `38` tests; frontend build passed; production frontend ref matched `assets/js/index-BZ3S_Tjz.js`; `py -3 scripts\diagnose-production.py --remote` passed; authenticated Playwright QA loaded dashboard, accounts, WeChat store, Doudian, team, permissions, profile, and admin without login redirects or page errors. | Success. | Do not click production write/destructive buttons during the demo. DB/Redis Docker containers still show internal `0.0.0.0` listens, but external probes to `5432/6379` fail; recreate containers after the demo to apply source `127.0.0.1` bindings. |
| 2026-07-18 22:34 Asia/Shanghai | Demo readiness hotfix | Codex | Production frontend + real-data QA | Allowed `SUPER_ADMIN` users to access team and permission routes, built and deployed the frontend with `PYTHONIOENCODING=utf-8 py scripts\deploy-frontend-fast.py --skip-typecheck --allow-dirty-source`. Added production diagnostic checks for the companion download package plus anonymous POST guards on Doudian upload/store creation and account cookie upload. | Frontend deployed and public ref matched `assets/js/index-GZd5vnNY.js`; frontend Vitest passed `7` files / `38` tests; production diagnose passed; authenticated read-only QA returned `13` accounts, `2` Doudian stores, public companion package `HEAD 200`; Playwright screenshots saved under `artifacts/qa-20260718-production`; account CSV export downloaded `账号列表_2026-07-18.csv`. | Success. | Use a real user login for the final human walkthrough; DB/Redis still show Docker public listen lines but external TCP probes to `5432/6379` fail and source compose now binds future recreates to `127.0.0.1`. |
| 2026-07-18 22:08 Asia/Shanghai | Reliability hotfix    | Codex | Production backend                | Stabilized backend health memory check to use process RSS versus system memory instead of volatile V8 heap ratio, added regression tests, fixed `scripts/deploy-backend-safe.py` to retry health after PM2 restart, then deployed with `PYTHONIOENCODING=utf-8 py scripts\deploy-backend-safe.py --execute --skip-build`. | Deploy script completed successfully and printed public health `data.status=ok`; `PYTHONIOENCODING=utf-8 py scripts\diagnose-production.py` passed; anonymous Doudian business-data endpoints returned `401`; backend Jest passed `26` suites / `277` tests; frontend Vitest passed `7` files / `38` tests. | Success. | Continue authenticated real-data UI validation with a fresh login; production Postgres/Redis port exposure remains an infrastructure hardening item. |
| 2026-07-18 22:00 Asia/Shanghai | Security hotfix       | Codex | Production backend                | Deployed backend with `PYTHONIOENCODING=utf-8 py scripts\deploy-backend-safe.py --execute` to remove anonymous Doudian browser cache access and stop returning companion profile paths. The script uploaded the archive, backed up remote backend files, ran Prisma generate and migrate status, and restarted PM2 `matrixflow`; it exited non-zero because the immediate local health curl hit startup timing (`exit 7`). | Follow-up remote diagnose showed PM2 `matrixflow` online on port `3000`, migrations up to date, local/origin/public health HTTP `200`; public health body returned `data.status=ok`; anonymous `GET /api/v1/doudian-browser/stores`, `shop/products`, and `shop/summary` returned `401` instead of the pre-deploy `200`. | Success after follow-up verification; deploy script reported a false-negative health timing failure. | Consider increasing the post-restart wait/retry loop in `scripts/deploy-backend-safe.py`; remaining infra risk: production Postgres/Redis Docker ports are bound on `0.0.0.0`. |
| 2026-07-17 11:50 Asia/Shanghai | Reliability deploy    | Codex | Production backend + frontend     | Ran deployment gate checks first: `git diff --check`, `npx tsc --noEmit --pretty false -p backend\tsconfig.json`, full backend Jest, backend build, frontend Vitest, frontend typecheck, and frontend build. Deployed backend with `PYTHONIOENCODING=utf-8 py scripts\deploy-backend-safe.py --execute`, then frontend with `PYTHONIOENCODING=utf-8 py scripts\deploy-frontend-fast.py --allow-dirty-source`. No DB migration or production dependency install was required. | Backend Jest passed `25` suites / `271` tests; frontend Vitest passed `7` files / `38` tests. Backend deploy verified PM2 `matrixflow` online and public health `HTTP 200` with version `1.0.0`. Frontend deploy matched local/remote/origin/public ref `assets/js/index-CTElgN0V.js`. Independent smoke checks returned public health `200` and homepage `HTTP 200`. | Success. | Monitor normal user flows; no immediate follow-up. |
| 2026-07-17 11:37 Asia/Shanghai | Reliability deploy    | Codex | Production backend + frontend + companion token | Deployed backend with `PYTHONIOENCODING=utf-8 py scripts\deploy-backend-safe.py --execute`, then deployed frontend with `PYTHONIOENCODING=utf-8 py scripts\deploy-frontend-fast.py --allow-dirty-source`. Refreshed the local Pixingyun companion saved token after backend deployment and started the rebuilt local companion exe. | Backend deploy printed `jwt_env=JWT_ACCESS_EXPIRES=1h JWT_REFRESH_EXPIRES=365d` and `jwt_dist=365d defaults verified`; remote read-only check showed `ENV_REFRESH=365d`, `DIST_7D_MATCHES=0`, `DIST_365D_MATCHES=2`, PM2 `online`; public health returned `200`; public frontend served `assets/js/index-CTElgN0V.js`; local companion refresh token now expires `2027-07-17 11:37:32 +08:00` (`365.0` days); local companion health returned `status=ok`, version `3.2.1`. | Success. | Run the next scan-bind from the currently started rebuilt companion and confirm the target account shows a fresh collection time after completion. |
| 2026-07-03 Asia/Shanghai       | Incident repair       | Codex | Production backend                | Rebuilt broken server dependencies, ran Prisma generate, restarted PM2 app `matrixflow`. Did not reset git, did not touch DB data, did not deploy frontend.                                                                                                                                                                                                                                                  | `https://ddddkiii.com/api/v1/health` returned `200`; PM2 app was online; nginx origin health returned `200`.                                                                                                                                                                   | Site recovered from API 502.                             | Add safe backend deploy script, disable automatic legacy deploy workflows, document the node_modules corruption failure mode.                 |
| 2026-07-03 Asia/Shanghai       | Governance change     | Codex | Local repository only             | Added `scripts/diagnose-production.py`; added `scripts/deploy-backend-safe.py`; converted legacy ECS deploy workflows to manual confirmation; tightened frontend deploy target.                                                                                                                                                                                                                              | `python -m py_compile scripts\diagnose-production.py scripts\deploy-backend-safe.py scripts\deploy-frontend-fast.py`; `python scripts\deploy-backend-safe.py --plan`; `python scripts\diagnose-production.py`; public health `200`. No production deployment command executed. | Deployment workflow now has safer official entry points. | Continue repo cleanup in small batches; do not delete files without explicit approval.                                                        |
| 2026-07-03 01:22 Asia/Shanghai | Incident repair       | Codex | Production frontend               | Public site was serving stale frontend bundle `assets/js/index-uL8uFGV0.js` and Doudian section was missing. Redeployed current frontend via `python scripts\deploy-frontend-fast.py --skip-typecheck --allow-dirty-source`.                                                                                                                                                                                 | Public `/?verify=front`, `/doudian?verify=front`, and `/monetization?verify=front` all served `assets/js/index-DatxT-TX.js`; entry bundle contained Doudian route and `DoudianView-DkKp-eK0.js`.                                                                               | Success.                                                 | Keep `LATEST_DEPLOYMENT.md` current before and after future deploys.                                                                          |
| 2026-07-03 01:33 Asia/Shanghai | Incident repair       | Codex | Production backend                | Doudian endpoints returned `Cannot read properties of undefined (reading 'findMany')` because the safe backend deploy script copied new `prisma` into the existing remote directory instead of replacing it, leaving the old schema active. Fixed `scripts/deploy-backend-safe.py` to delete remote `prisma` before copy, then ran `PYTHONIOENCODING=utf-8 python scripts\deploy-backend-safe.py --execute`. | Remote schema contains `DoudianStore*` models; remote Prisma Client exposes `doudianStore`, `doudianStoreOrder`, `doudianStoreProduct`, `doudianStoreAftersale`; public Doudian stores/products/orders/aftersale endpoints all returned `200`.                                 | Success.                                                 | Do not manually copy backend `prisma` with `cp -a` into an existing directory; use the fixed safe deploy script.                              |
| 2026-07-03 01:37 Asia/Shanghai | Data cleanup          | Codex | Production Doudian store registry | Deleted stale server-managed Doudian store `cmr0et26q07xect7mtmg9g1wh` directly with Prisma after public DELETE returned `401`. Guard required `profilePath` under `/opt/matrixflow/backend/doudian_profiles/`, `lastSyncedAt=null`, and zero orders/products/aftersales.                                                                                                                                    | Store counts before deletion were all `0`; after cleanup `GET /api/v1/doudian-browser/stores` returned only companion stores `cmr0qai7f1188xz13dm8ywxvk` and `cmr3fkxxc635cr5jhwi1q4rls`, both `syncStatus=ok`.                                                                | Success.                                                 | The deleted store had no business data; keep companion stores with `profilePath=companion:*`.                                                 |
| 2026-07-03 Asia/Shanghai       | Version cleanup check | Codex | Production frontend               | Checked retired public frontend bundles without changing production.                                                                                                                                                                                                                                                                                                                                         | Old refs `index-DkOeEzzm.js`, `index-CQ1-QhGH.js`, `index-vojAo1n-.js`, `index-uL8uFGV0.js`, and `index-BO3yyIfS.js` returned `404`; current `index-DatxT-TX.js` returned `200`.                                                                                               | Old public frontend versions are no longer served.       | Finish repository cleanup separately: commit/remove stale tracked `frontend/dist` entries and keep legacy deploy scripts blocked or archived. |
| 2026-07-03 Asia/Shanghai       | Repo cleanup          | Codex | Local repository only             | Removed legacy deployment and repair scripts plus legacy GitHub production deploy workflows from runnable repo paths. Did not SSH to production, did not deploy, did not restart services, and did not delete server files.                                                                                                                                                                                  | Official scripts still compile; removed representative files returned `False` in `Test-Path`; public health returned `200`.                                                                                                                                                    | Success.                                                 | Commit these deletions with the current deployment docs so the old entry points do not return.                                                |
| 2026-07-03 11:28 Asia/Shanghai | Safety backup         | Codex | Production backup                 | Created manual server backup at `/opt/matrixflow/backups/manual/20260703112845`. Included `frontend-dist.tar.gz`, `backend-runtime.tar.gz`, `postgres-pg_dumpall.sql.gz`, PM2/Docker/port snapshots, health snapshots, manifest, and SHA256 checksums.                                                                                                                                                       | `sha256sum -c SHA256SUMS.txt` passed; backup size about `3.0M`; frontend entry was `assets/js/index-DatxT-TX.js`; DB container was `matrixflow-db`; public health returned `200`.                                                                                              | Success.                                                 | Keep this backup until the next successful cleanup/deploy cycle has been verified.                                                            |
| 2026-07-16 19:02 Asia/Shanghai | Incident repair       | Codex | Production backend                | Deployed backend rate-limit repair with `py scripts\deploy-backend-safe.py --execute`. The script completed the remote build upload, backup, PM2 restart, and health check, then hit a local Windows GBK output-encoding error while printing remote output. No frontend deploy or DB migration was performed.                                                                                               | `npm run build --workspace=backend` passed; `https://ddddkiii.com/api/v1/health` returned `200`; PM2 app `matrixflow` was online after restart; local/origin health returned `200`; response headers showed `X-RateLimit-Limit-short: 100`; Prisma migrate status was up to date. | Success.                                                 | Consider making deploy script stdout encoding-safe on Windows so successful remote deploys do not report a local `UnicodeEncodeError`.          |
| 2026-07-16 19:13 Asia/Shanghai | Feature repair        | Codex | Production backend + frontend     | Added super-admin password reset for users, normalized auth/admin emails with `trim().toLowerCase()`, and deployed with `PYTHONIOENCODING=utf-8 py scripts\deploy-backend-safe.py --execute` plus `PYTHONIOENCODING=utf-8 py scripts\deploy-frontend-fast.py --allow-dirty-source`. No DB migration was required.                                                                                              | Backend and frontend builds passed; PM2 app `matrixflow` restarted online; backend health returned `200`; frontend public ref matched `assets/js/index-DlEMPpGR.js`; local/origin/public refs all matched.                                                                        | Success.                                                 | Use the new user-table `重置密码` action when a created user cannot log in or forgets their password.                                         |
| 2026-07-16 19:18 Asia/Shanghai | Feature repair        | Codex | Production backend + frontend     | Relaxed super-admin create/reset password policy to require only 8+ characters, fixed newly added admin password-reset messages, added frontend placeholder/client check, and redeployed backend plus frontend. No DB migration was required.                                                                                                                                                                  | Production logs showed the failed reset was a `400` password-policy rejection; backend and frontend builds passed; PM2 app `matrixflow` restarted online; backend health returned `200`; frontend public ref matched `assets/js/index-Casje4AU.js`.                            | Success.                                                 | Refresh the browser before retrying reset password so the latest admin bundle is loaded.                                                       |
| 2026-07-16 19:49 Asia/Shanghai | Feature deploy + incident | Codex | Production backend + frontend + tunnel | Added all-user dashboard issue alerts, readable frontend error-message fallback, super-admin login diagnosis and next-step prompts, then deployed backend and frontend. Backend deploy reached PM2/origin health but public verification failed; frontend deploy reached remote/origin bundle `assets/js/index-WuBRnYTC.js` but public verification failed. Replaced the tunnel container with the existing config/HTTP2 launch path while diagnosing. | `npm run build --workspace=backend` passed; `npm run build --workspace=frontend` passed; mojibake scan and `git diff --check` passed; PM2 app `matrixflow` was online; origin health returned `200`; origin frontend served `assets/js/index-WuBRnYTC.js`; public `https://ddddkiii.com` and `/api/v1/health` returned `530`; tunnel logs show `Unauthorized: Invalid tunnel secret`. | Origin deploy succeeded; public site blocked by Cloudflare Tunnel credential failure. | Regenerate or restore the valid Cloudflare Tunnel credential for tunnel `750c57ed-6785-4c69-90c9-772c9043d96e`, update `/root/.cloudflared`, restart `matrixflow-tunnel`, then recheck public health and frontend bundle. |
| 2026-07-16 22:34 Asia/Shanghai | Incident repair       | Codex | Production Cloudflare Tunnel      | Re-ran Cloudflare login with a writable container mount, installed `/root/.cloudflared/cert.pem`, fetched a fresh token for tunnel `750c57ed-6785-4c69-90c9-772c9043d96e`, backed up old token files, updated `/root/.cloudflared/tunnel-token.env`, `/root/.cloudflared/tunnel-token.txt`, `/opt/matrixflow/.env`, and `/opt/matrixflow/backend/.env`, then recreated `matrixflow-tunnel` with `--protocol http2`. Removed temporary login and quick tunnel containers. | Tunnel registered four HTTP/2 connections; recent tunnel logs no longer show `Invalid tunnel secret`; `https://ddddkiii.com/api/v1/health` returned `200`; `https://ddddkiii.com/` returned `200` and served `assets/js/index-WuBRnYTC.js`; origin health still returned `200`. | Success; public site recovered.                          | `www.ddddkiii.com/api/v1/health` returned `403`; main domain is healthy, but www should be checked separately if users rely on it.              |
| 2026-07-16 22:41 Asia/Shanghai | Reliability hardening | Codex | Production Cloudflare Tunnel      | Installed `/opt/matrixflow/scripts/tunnel-health-check.sh`, `/opt/matrixflow/scripts/tunnel-recover.sh`, and `/etc/cron.d/matrixflow-tunnel-guard` with `py scripts\install-tunnel-guard.py --execute`. Disabled legacy Cloudflare config/credentials files under `backups/legacy-disabled-*`, added `/root/.cloudflared/README.matrixflow`, and kept `/root/.cloudflared/tunnel-token.env` as the canonical tunnel credential source. | `py -m py_compile scripts\install-tunnel-guard.py` passed; manual health guard run logged `OK public=200`; cron file runs every minute; `/root/.cloudflared` contains only `cert.pem`, `README.matrixflow`, `tunnel-token.env`, and `tunnel-token.txt`; public health returned `200`; public frontend served `assets/js/index-WuBRnYTC.js`. | Success.                                                 | If public health fails 3 times and origin is healthy, check `/opt/matrixflow/logs/tunnel-health.log`; for `Invalid tunnel secret`, run `/opt/matrixflow/scripts/tunnel-recover.sh --login` then `--apply`. |
| 2026-07-16 22:56 Asia/Shanghai | Deep stability check  | Codex | Production backend + tunnel + www | Ran layered checks across public main domain, public `www`, origin, tunnel, PM2 backend, Docker containers, PostgreSQL, Redis, Nginx, cron guard, disk, and memory. Fixed backend asset packaging by changing `backend/nest-cli.json` asset paths so `sensitive-words.json` is included in `dist`, redeployed backend with `PYTHONIOENCODING=utf-8 py scripts\deploy-backend-safe.py --execute`, routed `www.ddddkiii.com` to tunnel `750c57ed-6785-4c69-90c9-772c9043d96e`, and upgraded the guard to log `secondary=200` for `www`. | Main and `www` health returned `200`; main and `www` frontends served `assets/js/index-WuBRnYTC.js`; origin main and origin `www` returned `200`; PM2 app `matrixflow` online; `matrixflow-tunnel` running; PostgreSQL `pg_isready` passed; Redis authenticated `PING` returned `PONG`; Nginx config test passed; guard log showed `OK public=200 secondary=200`; remote `sensitive-words.json` exists and loads. | Success.                                                 | The recurring cloudflared DNS resolver warning is non-blocking while public/origin health stay `200`; keep watching the guard log for actual public failures. |

## Entry Template

| Time                | Type                                  | Actor | Scope                           | Commands / Actions    | Verification                             | Result                     | Follow-up   |
| ------------------- | ------------------------------------- | ----- | ------------------------------- | --------------------- | ---------------------------------------- | -------------------------- | ----------- |
| YYYY-MM-DD HH:mm TZ | deploy / rollback / incident / repair | name  | frontend / backend / DB / infra | exact command summary | health checks, hashes, PM2/docker status | success / failed / partial | next action |
