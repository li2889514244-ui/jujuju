- **环境配置**: 必须设置 `JWT_SECRET` 和 `JWT_REFRESH_SECRET`（长度≥32且不同），以及 `DATABASE_URL`。
- **启动流程**: 执行 `npm run db:generate` 生成 Prisma 客户端，`npm run db:migrate` 同步数据库结构，随后通过 `npm run dev` 或 `npm run start` 启动服务。
- **文档访问**: 开发环境下自动启用 Swagger UI，访问路径为 `/api/docs`。