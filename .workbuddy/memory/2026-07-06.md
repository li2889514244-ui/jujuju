# 2026-07-06

## 新建微信小程序（只读数据查看）
- 用户要求把 MatrixFlow 网站做成"个人微信小程序"，只查看数据，不动源文件。
- 新建独立文件夹 `weixinxiaochengxu/`（原生微信小程序，非 uni-app），全部新建文件，源文件零改动。
- 技术栈：原生 WXML/WXSS/JS，4 个 tab 页（概览/账号/报告/洞察）+ 登录页。
- 后端复用：POST `/api/v1/auth/login`（邮箱+密码→accessToken）+ JWT Bearer 鉴权；数据走 `/api/v1/analytics/*`、`/api/v1/accounts`。
- 备案未知：先用直连 `https://ddddkiii.com/api/v1`，project.config.json 已关闭 urlCheck 便于联调。正式发布二选一：① ddddkiii.com 已备案→后台加 request 合法域名；② 未备案→改 BASE_URL 为微信云函数中转。
- 注意：用户明确强调"千万不要改源文件，一定要新建文件夹"——后续相关改动只动 `weixinxiaochengxu/`。

## 改为云函数中转（无需备案）
- 用户决定走微信云函数中转，避开 request 合法域名/ICP 备案限制。
- `utils/config.js`：`USE_CLOUD_PROXY=true`，`CLOUD_ENV` 待填（占位 `your-cloud-env-id`），`CLOUD_FUNC='proxy'`，`BACKEND_URL='https://ddddkiii.com/api/v1'`。
- `utils/request.js`：双模式——云函数模式走 `wx.cloud.callFunction({name:'proxy', data:{path,method,data,headers}})`；直连模式保留。
- `app.js`：onLaunch 里 `wx.cloud.init({env})`（env 非占位时才初始化）。
- 新增 `cloudfunctions/proxy/index.js` + `package.json`：云函数转发 `/api/v1/*` 到后端，依赖 wx-server-sdk + axios。需在开发者工具右键「上传并部署：云端安装依赖」。
- AppID 已填：`wx73b0bb2bd7eaf634`（用户给的，AppSecret 未存储）。
- 用户待办：① 微信公众平台开通云开发拿到 env ID 填进 config；② 部署 proxy 云函数；③ 真机联调验证字段。

## 确定使用范围 + 上线策略
- 用户明确：小程序只给自己和几个朋友用 → 走「体验版」，**免备案、免微信类目审核、免内容审核**。
- 体验版机制：开发者工具上传代码 → mp.weixin.qq.com 后台「成员管理→体验成员」添加朋友微信号 → 朋友扫体验版二维码即用。长期有效，只有体验成员能开。
- 用户要求"全部由 AI 搞定，制定计划按计划执行"。已建任务清单(5项)：代码核查✅、部署手册(已写 `weixinxiaochengxu/部署步骤.md`)、用户开通云开发给env ID、填env ID+部署云函数、上传体验版+加朋友。
- 物理限制（诚实告知）：开通云开发、部署云函数、上传体验版、加体验成员均需用户本人在微信开发者工具/GUI 操作（需微信登录态+扫码），AI 无法远程代点。但代码侧已全部完成，用户侧动作已压到 4 个极简步骤。
- 交付物：`weixinxiaochengxu/` 全量代码 + `部署步骤.md` 傻瓜手册。AI 侧已收尾，等用户给 env ID 即可继续。

## 最大化自动化：一键部署脚本
- 用户坚持"都你来"。但微信开发者工具的登录/扫码/开通云开发/上传必须在用户本机 GUI + 微信登录态完成，AI 远程沙箱无法代点（硬隔离，已诚实告知）。
- 查官方 CLI：支持 `cli cloud functions deploy --env <ID> -r --paths <dir> --project <path>`（部署云函数+云端装依赖）和 `cli upload --project <path> -v <ver> -d <desc>`（上传体验版）。
- 新增 `weixinxiaochengxu/一键部署.bat`（纯 bat，不依赖 node）：自动定位 cli.bat → islogin 检查 → 用户输入 env ID → 用 PowerShell 写 config.js 的 CLOUD_ENV → 部署 proxy 云函数 → 上传体验版。
- 用户动作压到最小：①一次性装工具+扫码登录+开通云开发+开服务端口（GUI/扫码无法脚本化）；②双击 bat 粘 env ID 回车。其余全自动。
- 手册 `部署步骤.md` 已重写，指引用脚本。源文件零改动。
