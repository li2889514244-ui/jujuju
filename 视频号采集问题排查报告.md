# 视频号采集数据缺失问题排查报告

## 问题描述

5 个活跃视频号账号中，3 个账号的**累计点赞、评论、分享数据为 0**，但累计播放量和每日新增数据（新增点赞、新增评论等）正常。

| 账号 | 累计播放 | 累计点赞 | 累计评论 | 累计分享 | 状态 |
|------|----------|----------|----------|----------|------|
| 卢慧来了 | 8648万 | 77万 | 8.6万 | 148万 | ✅ 正常 |
| 晶哥来了- | 2.83亿 | 118万 | 19.7万 | 117万 | ✅ 正常 |
| 卢慧-高维破局 | 1.8万 | **0** | **0** | **0** | ❌ 缺失 |
| 心理学博士卢慧 | 4.9万 | **0** | **0** | **0** | ❌ 缺失 |
| 慧氏幸福觉醒 | 4.5万 | **0** | **0** | **0** | ❌ 缺失 |

## 根因分析

### 采集流程（串行，每个账号独立浏览器）

```
账号1 → 开浏览器 → 采集仪表盘 → 采集数据中心 → 采集视频列表 → 关浏览器
账号2 → 开浏览器 → 采集仪表盘 → 采集数据中心 → 采集视频列表 → 关浏览器
...（串行，非同时）
```

### 数据来源说明

- **累计播放/点赞/评论/分享**：来自视频号 `post_list` API，该 API 返回每个视频的累计数据，求和后得到账号累计总量。
- **每日新增（新增播放/点赞等）**：来自数据中心 `new_post_total_data` API，返回昨日/近7天/近30天的增量数据。

### 失败点：post_list API 被反自动化检测拦截

通过抓取原始 API 响应确认，3 个失败账号调用 `post_list` API 时，微信返回 HTTP 200 但 body 中 `errCode=300334`（反自动化检测拦截），API 不返回任何视频数据。

**原始 API 响应示例：**
```json
// 成功的账号（卢慧来了）
{"errCode": 0, "data": {"list": [{...}, {...}, ...]}}  // 正常返回视频列表

// 失败的账号（卢慧-高维破局 等 3 个）
{"errCode": 300334, "errMsg": "request failed"}  // 被反爬拦截，无视频数据
```

### 代码层面的 3 个缺陷（已修复）

| # | 缺陷 | 影响 | 修复方式 |
|---|------|------|----------|
| 1 | JS 代码只检查 HTTP status≥400 才退出，不检查 JSON 中的 `errCode` | `errCode=300334` 返回 HTTP 200，代码误认为成功但拿到空数据，静默失败 | 增加 `errCode` 检查，非 0 时返回错误信息给 Python 层 |
| 2 | API 失败后无重试机制 | 首次被拦截直接放弃，不重试 | 增加 3 次重试，间隔 5 秒，每次重新导航刷新 session |
| 3 | API 失败后累计值不写入，用 0/空覆盖旧数据 | 前一次采集成功的累计值被清零 | API 失败时从本地数据库读取上次的累计值保留，不覆盖 |

### 为什么只有 3 个账号失败

通过单独测试每个账号的 `post_list` API，发现：

| 账号 | post_list API 结果 | 原因推测 |
|------|-------------------|----------|
| 卢慧来了 | ✅ errCode=0，返回 1122 条视频 | session 有效 |
| 晶哥来了- | ✅ errCode=0（生产环境中成功） | session 有效 |
| 卢慧-高维破局 | ❌ errCode=300334 或重定向到登录页 | session 较弱/已部分失效 |
| 心理学博士卢慧 | ❌ 同上 | 同上 |
| 慧氏幸福觉醒 | ❌ 同上 | 同上 |

**关键发现**：所有 5 个账号的 `state.json` 中 `finder_login_token` 的 **值都是空字符串**（key 存在但 value 为空）。这说明登录时 `storage_state()` 保存的时机不对——该 token 由页面 JavaScript 在运行时动态填充，保存 state 时可能还没值。

但 2 个账号仍能成功，是因为它们的 Chrome profile 目录中保留了有效的 cookie/session 缓存。另外 3 个账号的 profile session 可能已部分过期，导致 API 级别被拦截。

## 已修复内容

1. **errCode 检测**：JS 代码增加 `errCode != 0` 检查，不再静默吞掉 API 错误
2. **3 次重试**：API 失败后等 5 秒重试，重新导航刷新 session
3. **旧值保留**：API 全部失败后从本地数据库读取上次的累计值，不用 0 覆盖
4. **详细日志**：每次尝试写入 `dc_debug.log`，包含 attempt 编号、errCode、返回视频数

## 仍需解决的问题

### 1. 3 个失败账号需要重新扫码登录

这 3 个账号（卢慧-高维破局、心理学博士卢慧、慧氏幸福觉醒）的 Chrome profile session 较弱，`post_list` API 被反自动化检测拦截。**建议在披星云伴侣中重新扫码添加这 3 个账号**，刷新 profile 的 session。

### 2. finder_login_token 保存时机问题

登录完成后保存 `storage_state()` 时，`finder_login_token` 的值为空。该 token 由页面 JS 动态填充，需要在页面完全加载、API 调用成功后再保存 state，或者通过 CDP 在运行时抓取 token 值写入 state.json。

**影响**：当前保活检查（`session_keepalive.py`）只检查 key 是否存在，不检查 value 是否非空，所以这 3 个账号仍被标记为"活跃"。但实际上 token 为空，API 认证不通过。

### 3. 采集顺序对 session 的影响

采集是串行的，但先采集的账号（如卢慧来了）打开浏览器、访问页面后关闭，可能影响后续账号的 Chrome profile session（共享 cookie jar 或 localStorage 竞争）。建议确认每个账号的 `user_data_dir` 完全独立，无交叉。
